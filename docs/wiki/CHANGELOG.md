# Wiki — Changelog

The audit trail, so you can trust me running aggressively. This is a pointer, not a duplicate:
**[../log.md](../log.md) IS the changelog** — dated, append-only, already exists, already has 1300+
lines of real history. Renaming or forking it would break every existing cross-reference and give
you two sources of truth. This file exists only so the "three nav files" convention
(INDEX / CHANGELOG / QUESTIONS) has a literal `CHANGELOG.md` to find.

Read [../log.md](../log.md) directly for the trail. Newest entries at the bottom.
