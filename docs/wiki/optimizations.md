# Optimizations + memory + isolated computer

full: [../raw/optimizations-memory-vm-dealpilot-plan-2026-07.md](../raw/optimizations-memory-vm-dealpilot-plan-2026-07.md)

- Existing dev token system = good static foundation: wiki→raw→code, CODEMAPS, nav index, skill scoping, <4k orient target.
- Missing = runtime Optimizations package. Add token/cost receipts + budgets first; lossless payload compression + retrieval next; semantic/adaptive compression only behind eval gates.
- TASK-022 slice built: every completion names `cheap|default|reasoning`; router selects tier-capable provider, never registration slot. Local request still cloud-fail-closed.
- Anthropic caches explicit stable system prefix; volatile turn stays outside breakpoint. Completion returns provider token/cache counts. CoS cloud calls pass Authority + Plane + policy as Egress Agent/on-behalf-of member; every successful branch appends prompt-free receipt.
- Live cache hit still needs provider minimum prefix (Haiku 4.5: 4,096 tokens; Fable 5: 512). Deterministic adapter proves protocol; no paid-key live claim.
- Quality-adjusted cost/success = north star. Originals + source anchors survive. Financial/legal evidence defaults lossless. Taint survives compression.
- Memory: Bridge design stronger than pi-hermes-memory on governance/provenance/planes; Hermes stronger now because retrieval, session search, correction/failure learning, consolidation, secret scan, lifecycle flush exist.
- Target = policy-only memory retrieval; scoped global/workspace/Element Memory; correction/failure categories; aging/consolidation; secret+injection scan; pre-compact/shutdown flush; procedural Memory→governed Skill proposal.
- Account allow/deny = CredentialBroker policy. Use least-isolated target: governed host → browser profile → container/microVM → full VM.
- VM high ROI: account proof, untrusted code/files/sites, destructive deps, cross-app work, reproducible OS, long jobs, tenant compute, parallel-agent isolation. VM never replaces permissions/egress/audit.
- Open Computer pattern: visible QEMU agent desktop + human intervene/ask-user. P3 sandbox adapter; P4 optional visible Isolated Computer package; remote pools only on proven demand.
- DealPilot: sourcing+triage → evidence room → diligence workbench → IC room → execution → governed learning. Every claim opens source; every red flag maps to transaction action.
- Source candidates added to Commons intake: OpenHuman, pi-hermes-memory, Open Computer, PE-DD skill, financial-services plugins, deal-evaluator, PE fund-selection ML, SmallPE. All gated by license/provenance/security.
