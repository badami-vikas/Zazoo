# Input packs (per-class, canon)

Canon: [capability-input-packs-2026-08](../raw/capability-input-packs-2026-08.md)
(ADR-238, AP-156, TASK-042). Evidence:
[per-class regeneration test](../../outputs/2026-08-14-regeneration-test-per-class-packs.md).

Pack = 6 layers: constitution · contracts+registry (refresh registry at use) · mandate
· Layer M · Layer B · class harness. Validated per class against shipped ground truth,
rubrics pinned before generation, amend-until-zero-WORSE.

**Verdicts.** Skill: harness + contracts/registry suffice (boundary-conditions
hypothesis HOLDS; constitution/mandate = polish). Module: mandate LOAD-BEARING
(legal/counterparty/trade-dress context no arm derived unnamed). Integration: mandate
LOAD-BEARING for purpose (obligations alone made a safe design that deleted the
product). Harness pins SAFETY at every class; mandate value scales with non-derivable
context; contracts/registry stops island rebuilds at every class.

**Validated amendments baked in:** per-call provider resolution (dynamic health);
recipient-wrote-first send gate + near-identical refusal + warm-up; trade-dress named
in Module mandates; key-space-lookup render-time identity join.

Adopt-candidates from the test (SubstanceGate, receipt-in-return-type, encrypted local
store, outbox, Secret type, metadata-only scopes, …) recorded in the report — NOT
enacted, each needs its own pull-forward.

Unblocks K9 rung 4 (TASK-053 structure synthesis).
