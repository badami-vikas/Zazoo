# Stack (wiki)

full: [../raw/STACK.md](../raw/STACK.md) · primitive names for stack terms → [ontology](ontology.md): ritual engine (Hatchet/`RitualExecutor`) runs the **Automation** primitive; `tool-kit`/ToolManifest = implementation surface for the **Workspace** primitive; integrations = **Integration** primitive.

Scale: 30k canonical / <100 hot → virtualize 30k surfaces, WebGL graph, E2EE cheap.

- **Core**: React18+Vite+TS+Tailwind4+shadcn · Fastify5+tRPC11+Drizzle · Supabase (pgvector+pg_trgm+RLS+Storage+Realtime) · ULID · Supabase Auth + workspace/team day 0.
- **UI libs** (permissive): glide-data-grid (30k dir — canvas renderer for the `table` view kind past 400 visible rows; DOM `TableView` below, one contract, ADR-160), @xyflow (ritual builder), @antv/g6 WebGL (map), maplibre+deck.gl, tiptap (notes + @mention links), dnd-kit (Taskade reparent), vis-timeline, qrcode, **react-big-calendar (MIT, calendar render — behind `CalendarView` port) + ical.js (MPL-2.0, recurrence/ICS) + ical-generator (MIT, .ics feed) + Luxon (tz)** — see [calendar](calendar.md).
- **Runtime**: BUILD thin agent runtime (Agno scope + LangGraph interrupt/checkpoint). Hatchet (Postgres) ritual engine. BullMQ queue. `RitualExecutor` seam, Temporal deferred.
- **Models**: `ModelProvider` seam — NOW REAL (2026-07-06): port in `@bridge/core` ports.ts (types only, zero-deps) + `@bridge/models` impls: `OllamaProvider` (local plane, OLLAMA_URL) · `AnthropicProvider` (cloud plane, ANTHROPIC_API_KEY/ANTHROPIC_MODEL, default claude-fable-5) · `createModelRouter(bindings)` resolves tool-kit `modelBinding` honoring planeDefault — local-default binding NEVER falls to cloud (capture/sensor rule); cloud-default may fall to local (privacy-safe direction). Wired in apps/api wiring.ts both modes (in-memory = `EchoModelProvider`, no network). prod=user-configurable (Claude default · OpenAI · Gemini · Bedrock · local). Tiers reasoning/default/cheap per workspace. Gateway later: Vercel AI SDK (+ LiteLLM proxy prod). Local model = trust feature (inference in-tenant).
- **Sensors**: `@bridge/sensors` = Sensor SPI = context-provider registry ([clients](clients.md)): 9 kinds (apps…emails), per-surface subsets, providers = optional capabilities (manifest + COMPUTED risk via capability module, origin built_in, state draft). Raw vs derived split AT TYPE LEVEL — consumers see `ContextObservation` only; raw local-plane, cloud read refused. Every ingest → Memory entry (CaptureLedger → timeline_entries) + `sensor.capture` event (blink tell). Kernel runs with ZERO providers.
- **Desktop shell**: `apps/desktop` = Tauri v2 scaffold. Hosts apps/web (devUrl :5173 / frontendDist ../../web/dist). Rust `sensor_bridge` = typed stubs (sensor_list/start/stop, capture_screenshot_on_demand) naming their macOS APIs (NSWorkspace/AX/CGWindowList) — capture core = Phase-0 follow-up. JS build/test no-op → turbo stays green without Rust.
- **Embeddings**: model NOT freely swappable (`vector(N)` fixed dim) → pin one canonical embedding model/dim, decouple from chat model. dev Ollama (768) vs prod OpenAI (1536) differ → re-embed on switch.
- **API**: tRPC for app; add REST/OpenAPI later for MCP/external agents.
- **Pilot hosting**: Render free Virginia Docker API + static site. Public scope only. Supabase Postgres/Auth. No Render disk/vault/private Local Plane. Sleep after 15m accepted.
- **Migrations**: append-only Drizzle journal. Divergent parent histories reconcile with a newer idempotent data migration; never lower or rewrite an applied high-water timestamp (ADR-126).
- **Avoid licenses**: AGPL/SSPL/BUSL/fair-code/commercial → see [oss](oss.md).
