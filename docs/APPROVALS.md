# APPROVALS — canon gate and audit ledger

Bridge governs its product with draft-then-approve. This repo governs **its own canon** the same way. Changes to canonical/plan docs are **proposed here first** and only become authoritative once the user (the sole approver today) flips them to APPROVED. This is the doc-layer analogue of the platform's `propose → decide → commit` pipeline.

This ledger gates or records decisions; it is not an execution queue. A proposed approval is referenced by the relevant canonical item in [`docs/TASKS.md`](TASKS.md) and blocks that item when required.

## What requires an approval row (canon + plan changes)
- Editing a **locked/canonical doc**: `docs/wiki/vision.md`, `docs/wiki/decisions.md` (strategic one-liners), any `requirement` doc (verbatim user text — also never-edit).
- **Flipping a plan's `status`** (e.g. `draft` → `active`/`approved`, or marking a Track/Phase **DONE**) or changing the roadmap **sequencer order** (`roadmap-6month-2026-h2.md`).
- **Reversing or superseding a decision** already recorded in an ADR.
- Marking any consolidation-plan track as executed.

## What does NOT (routine — just do it, then log to `docs/log.md`)
- New `docs/raw/` drafts; wiki summaries of existing raw; `docs/log.md` / `docs/BUGS.md` / `docs/dummy.md` evidence; TASKS evidence/verification bookkeeping that does not alter canonical scope/order; CODEMAPS regen; code changes with passing tests.

## Protocol
1. Agent appends a **PROPOSED** row below (+ the concrete diff/patch as a proposal — do NOT apply it to the canon doc yet, and do NOT mark anything DONE).
2. User reviews, sets status **APPROVED** or **REJECTED** (may edit the proposal first).
3. On APPROVED: agent applies the change, sets status **APPLIED**, fills the commit SHA. Never treat a proposal as canon before it is APPROVED.
4. Append-only. Superseded rows stay for history.

`Never mark a plan/track DONE unless the repo actually contains the edits AND this ledger shows it APPROVED.` (Critique conflict #1 resolution, ADR-044.)

## Ledger

| ID | Date | Proposer | Scope (canon doc / decision) | Change summary | Status | Decided-by · date | Applied-commit |
|----|------|----------|------------------------------|----------------|--------|-------------------|----------------|
| AP-152 | 2026-08-13 | user directive | K10 — the five ADR-176 constitution mechanisms as executable obligations (ADR-234; TASK-043 done); `port-allowlist.json` canon-gated file | User directive: *"start on next tasks fr AI harness"* (K10 follows K7/K9 under the same directive). **What this row approves**: (1) the new approval-gated `packages/core/port-allowlist.json` itself (E1) — the closed port-set the Learning surfaces (`core/learning`, `core/memory`) may reach outside their own scope, structurally scanned by `port-allowlist.test.ts`; changing it without a review row is exactly the drift the mechanism exists to catch; (2) E2 shown-text acceptance hash + reviewed/unverified audit distinction; (3) E3 deterministic red/amber content gate (health, protected-characteristic, psychological/relationship-conclusion, financial-distress never-propose classes; amber renders as observed fact with evidence, raise-only); (4) E4 claim→evidence enforcement, checked per clause; (5) E5 paraphrase-robust rejection fingerprints with 30d→90d→permanent backoff, deletable Memory as the un-suppress affordance. **Two real defects found and fixed during this row's own verification, not by inspection**: `acceptance-audit.ts`'s original `node:crypto` import broke the web app's production build (Rollup cannot bind a named import from a Node builtin even for unreachable code) — fixed via `globalThis.crypto.subtle`; the live walk then surfaced a genuine E5 tier-consistency bug (a rejection's write can fall back to the lexical tier while a later check tries the semantic tier first and finds nothing, silently never falling back) plus an adjacent hash collision in the shared 128-dim lexical embedder (`"cet"`/`"ist"` land in the same bucket) — fixed with a dual-tier check plus a dedicated, higher-dimension, distinctly-id'd lexical embedder for rejection fingerprints specifically. **Verified**: `pnpm verify` 77/77; LIVE durable-boot walk 11/11 over the real server + pglite Local Plane (red content refused, no-evidence/compound refused, amber/green framing correct, acceptance audit reviewed-vs-unverified split, rejection fingerprint suppresses a reworded repeat while an unrelated claim stays proposable, both the suppression and the audit stamps survive a real process RESTART). K11 (TASK-054) is next but stays blocked on the user's still-unmade keystroke content-vs-events decision. | APPLIED | user · 2026-08-13 | (this commit) |
| AP-151 | 2026-08-13 | user directive | K9 rung 3 — the Capability Builder drafts automation steps from ledger evidence; rung 4 recorded blocked on TASK-042 (ADR-231, TASK-053 partial) | User directive: *"start on next tasks fr AI harness"* (K9 follows K7 under the same directive). **What this row approves**: (1) rung 3 as DERIVATION, not generation — no model call: `draftStepsFromEpisodes` reads the modal governed shape (action/resourceType/dataScope) out of the affirming HUMAN ledger episodes (approve/edit; veto excluded as evidence AGAINST) behind the accepted pattern, constrained to the live skill registry, one step per draft (multi-step = rung 5 behind K10); episode envelope structurally payload-free, same posture as the K1 miner; (2) the `drafts.proposeSteps` lane — Human-only, flight-gated, canonical `parseAutomationSteps` validation, draft SAVED still as draft (executor blind; activation unchanged); structured refusals (`pattern_not_skill_shaped`/`skill_not_registered`/`no_episodes`/`action_unrecognized`) that write nothing; (3) the honest scope split: TASK-053's rung 4 depends on TASK-042 (per-class input packs), which has never been executed — the TASK row now records rung 3 done + rung 4 blocked instead of claiming readiness. **Verified**: core 6/6 + api 4/4 over real buildWiring, four mutations seen RED (registry gate dropped, veto-as-affirming, registry bypass in the lane, silent activation on save); `pnpm verify` 77/77 (one earlier run flaked on the pre-existing culture-research cancel test under load — passes in isolation, filed); LIVE durable-boot walk 9/9 on real captured K7 focus data (rhythm → proposal → accept → structured Builder refusal, stable across restart, empty draft refusing activation); real-browser drafts-card walk ("Draft steps from my decisions" + verbatim refusal). **Honest limits**: positive-path live evidence is suite-level until the first organic accepted skill pattern exists; rung 4 blocked on TASK-042. | APPLIED | user · 2026-08-13 | (this commit) |
| AP-150 | 2026-08-13 | user directive | Companion point-of-use visibility for screen sharing, Research, and spoken-response text (ADR-233; TASK-027/TASK-028 evidence) | User reports, verbatim: *"I dont see the screen share feature and research agent feature Can you pull the latest"* and *"also talk the response is also not shown now"*. This approves: explicit Ask/Research controls in Companion rather than phrase-only discovery; Share screen control at the question point while preserving the existing persisted consent key and governed egress path; successful spoken answers always retain visible text in a named live response card brought into view; panel copy says Companion because it now hosts both Ask and Research. No new Research executor, capture path, provider, or top-level nav entry. | APPLIED | user · 2026-08-13 | (this commit) |
| AP-149 | 2026-08-13 | user directive | Reopen TASK-027 at P0; restore one-click Avatar screenshot analysis and fail-closed Screen Recording behavior (ADR-232) | User report, verbatim: *"One of the other repo owners has pulled the latest and using the bridge desktop app. For him the screenshoot and analyze is not working can you check that?"* Follow-up: *"Also chief of staff and avatar model are different?"* This approves: TASK-027 `done/P2` → `in_progress/P0` while the blocking regression is repaired; Observe reuses the existing consented `CompanionAsk` vision job instead of a second capture/egress path; missing Screen Recording permission refuses wallpaper-only output before capture; missing Groq vision capability is stated rather than faked by text-only fallback; capability-fit model separation remains (Chief of Staff local Qwen or Cloud Groq text; Avatar Groq vision), with the shared Groq credential but no forced model-id unification. | APPLIED | user · 2026-08-13 | (this commit) |
| AP-148 | 2026-08-13 | user directive | K7 — app-focus capture live: fifth "apps" consent source, @bridge/sensors hub wired into the API, AX window-title fail-closed, drain loop consent-driven at the source (ADR-230, TASK-051 done) | User directive: *"start on next tasks fr AI harness"* (K7 is the next READY rung; K9/K10 follow under the same directive). **What this row approves**: (1) "apps" as the fifth capture-consent source under unchanged K2 machinery — default OFF, Human-only, kill switch, fail-closed; (2) the @bridge/sensors SensorHub hosted by the API composition root (lazy per-user; provider capability-manifested with COMPUTED risk = informational; natural-key check-before-insert registration so re-boots reuse the row and never resurrect a suspension; learning-loop consumer writes the ONE observed-signal Memory per focus, idempotent per shell-minted focusId); (3) the narrow AX title read (app → AXFocusedWindow → AXTitle, linear CF ownership) that FAILS CLOSED to app-names-only without the Accessibility grant — suppression is an absent attribute plus a recorded redaction, never an empty string; (4) the desktop drain loop reconciling consent → sensor_start/stop so the toggle and kill switch stop the poller itself, with the route's consent gate as defense in depth; declines are structured verdicts, never errors; (5) the retired-vocabulary allowlist entry for Apple's AX symbol names in accessibility.rs (foreign linker symbols, ADR-171 mechanism). **Verified**: core 23/23, sensors 13/13, api 6/6 over real buildWiring, cargo 163/163 — six mutations seen RED across the four gates; `pnpm verify` 77/77; LIVE durable-boot walk 10/10 (consent matrix, kill switch + restore, same-boot duplicate, RESTART-surviving idempotency, K6 brief showing `apps: 3`); row-level pglite read (3 rows exactly, suppressed row carries NO windowTitle key, sensor/untrusted taint with origin ref); real-browser Settings walk (card + toggle round-trip with fresh provenance). **Honest limits**: signed .app rebuilt + bundle-verified but NOT launched (the user's own desktop app was running from another checkout; a second instance contends for its Local Plane — ADR-229's surface); the one-time Accessibility grant to the signed Bridge.app remains the user's step, with ADR-228's byte-identical DR as the durability mechanism; blink rides TASK-027's existing sensor:capture listener. | APPLIED | user · 2026-08-13 | (this commit) |
| AP-147 | 2026-08-11 | user directive | K7 unblock — stable local signing identity makes TCC grants rebuild-durable; TASK-051 blocked → ready (ADR-228) | User directive, verbatim: *"k7 unblock"*. **What this row approves**: (1) a self-signed "Bridge Dev Signing" code-signing certificate as this machine's local signing identity (created via openssl, imported to the login keychain, key material deleted; NO trust-store or admin change — codesign signs with it untrusted, empirically verified); (2) `build-tauri.mjs` auto-detection — env `APPLE_SIGNING_IDENTITY` always wins, explicit `-` forces ad-hoc, absent cert = previous ad-hoc behavior, so CI/other machines are unaffected; (3) hardened runtime deliberately OFF for the local-identity build (self-signed = no Team ID = library validation would reject our own dylibs; the release path keeps it); `isReleaseSigningIdentity` excludes the local CN. **Verified**: test:bundle 8/8 (1 mutation seen RED); two full signed builds both passing the deep bundle verifier; DR byte-identical across builds (`identifier "ai.bridge.desktop" and certificate root = H"48868b88…"`) while CDHash changed — and the ad-hoc counterfactual measured as `designated => cdhash H"…"`, the exact ADR-184 failure. **Honest limit**: the final human confirmation — granting Accessibility once to the signed .app and watching it survive a rebuild — is the user's step and becomes K7's live-walk evidence; `tauri dev` stays non-durable by nature. | APPLIED | user · 2026-08-11 | (this commit) |
| AP-146 | 2026-08-11 | user directive | K8 — browser-extension capture: domain/title only, default-deny domain policy, private windows structurally excluded (ADR-227, TASK-052) | User directive: "Start on K8" (fresh explicit start per the K-ladder rhythm). **What this row approves**: (1) "browser" as the fourth capture-consent source under the unchanged K2 machinery (default OFF, Human-only, kill switch, fail-closed), refined by a per-domain policy that is DEFAULT-DENY (empty allowlist captures nothing), deny-wins, label-boundary subdomain matched, and fail-closed on parse — policy edits Human-only with malformed entries refused loudly, never silently dropped. (2) A structural no-URL guarantee: the extension reduces the URL to a bare hostname in-browser, no envelope/schema/signal field can hold a URL, and a path-bearing "domain" fails normalization server-side; titles taint-labeled `browser_capture` (web/untrusted/instruction_like) at the boundary. (3) One verdict function on both sides: the MV3 extension bundles `browserCaptureVerdict` from @bridge/core via a new dependency-free subpath export, and the API re-evaluates the same function on arrival. (4) Private windows excluded by construction — manifest `"incognito": "not_allowed"`, no content scripts, no scripting/debugger, capability surface = tabs/storage/alarms + localhost only, all pinned by a structural test. (5) Declined captures return structured verdicts (never errors, no retry-loop bait); writes idempotent per extension-minted visitId. Chassis-only reuse of the first-party recon-salvage extension; its scraping machinery deliberately NOT reused. **Verified**: core 19/19 (3 policy mutations seen RED), api 6/6 over real buildWiring (2 mutations seen RED), extension 5/5 incl. the manifest structural test (2 mutations seen RED), `pnpm verify` 77/77 with the new package inside the gate; LIVE durable boot — full verdict matrix over the extension's exact wire requests, restart durability, kill switch draining the extension policy to dormant, K6 brief showing `browser: 2` activity; REAL-browser walk of the Settings card (toggle + consent provenance, domain-editor round trip, loud refusal naming the bad entry live). **Honest limits**: not yet loaded into a real Chrome profile (load-unpacked is a documented one-step user action; wire contract proven byte-identically over HTTP); paste-your-own-token config (pairing flow = candidate follow-up); no retry queue; Avatar blink lands with K7; title pattern-redaction belongs to K10 hardening. | APPLIED | user · 2026-08-11 | (this commit) |
| AP-145 | 2026-08-10 | user directive | K6 — morning brief + commitment detection, suggested-then-accepted into the existing commitment substrate (ADR-225, TASK-050) | User directive: "start" (fresh explicit start per the K-ladder rhythm). **What this row approves**: (1) deterministic, precision-biased commitment detection over the owner's OWN chat prose in the send path — an in-conversation capability riding the learning flight, NOT the K2 chat toggle (whose contract is envelope-only signals; detection writes a suggestion quoting the user's own sentence, never a signal, never a Commitment); ambient WhatsApp/email prose scanning deliberately excluded pending its own consent surface. (2) suggestion lifecycle on CAS lineage per normalized sentence (rejected = never re-proposed), annoyance caps 2/run + 5 outstanding, over-cap DEFERRED not dropped. (3) Accept = a Human act that flips the lineage first (no double-mint) then materializes through the SAME governed `relationship_commitment_mutation` the manual surface uses; person linkage is the human's choice (hint preselects only on a unique match). (4) `brief.morning` + the Home `MorningBriefCard`: owner-wide commitment read (`listCommitmentsForOwner`), calendar-day buckets, suggestion counts, approvals nudges, 24h signal activity, deterministic next actions — buckets/approvals render regardless of the flight; learning sections gate; empty mornings say so honestly. **Verified**: core 9/9 (3 seen RED under cap-never-binds and negation mutations), db 22/22 (new owner-wide read crosses People, filters status, stays private), api learning-commitments 5/5 (2 seen RED under flight-gate-stripped and bucket-collapse mutations), full API suite green, `pnpm verify` green, and a LIVE walk on a durable boot INCLUDING a real browser: prose turn → suggestion with counterparty + resolved due → Accept in the Brief card (person preselected via unique hint match) → bucket updated, queue drained, restart durability held; a live-caught grammar defect in next-action copy fixed before ship. **Honest limits**: small English due vocabulary at 17:00 local; detection on chat sends only; no morning notification (the user opens Home); the machine's managed-model gate fails the assistant reply after the user turn + detection persist (no local model installed — existing behavior). (RENUMBERED same day from AP-143: the parallel branch's c135da0 took AP-143/144 while this rung was in verify; later-arriving side renumbers per precedent.) | APPLIED | user · 2026-08-10 | (this commit) |
| AP-144 | 2026-08-10 | user directive | `ui-architecture-rules` §3a gains the presence-vs-content standing rule; §10 gains failures 6–9 (second-order diagnosis); Second Brain's rail entry retired (ADR-224) | User directives, verbatim: *"I dont see the Add row option in few tabes and in some it is present. I want the UI elements same for all modules and only the data displayed should be different."* · *"I asked for a diagnosis on why I'm forced to repeat the issues."* · *"Can you confirm if all task manager, job manager, deal manager and network manager point to same UI kit?"* · *"I asked for second brain to appear inside intelligence but I still see it in left nav bar. Correct it."* **What this approves:** (a) **§3a standing rule** — a standard control's EXISTENCE is never conditional on a page prop; pages configure behaviour and copy, and a control that cannot act is disabled with a stated reason, never absent. The add-row is the worked example. (b) Gates now assert RENDERED behaviour, not just that a page mounts the shell — the two new tests failed on first run (OrganizationPage + SecondBrainPage; DealPilot), which is the evidence the old structural gate was checking the wrong thing. (c) Second Brain's rail and mobile-drawer entries are deleted and `/second-brain` redirects to `/intelligence`; one renderer, one entry point, pinned by test. (d) DealPilot's bespoke `StatCard` is folded into `DashboardRow` as optional `icon`/`tone` — one insights component for every Module. **Audit answer (the user's direct question):** all four named Modules — Task Manager, JobPilot, DealPilot, Relationship — route through `ModuleSurfaceLayout` + `DataViews`, i.e. the same kit, and did so BEFORE this change while still looking different. That is the finding, not a reassurance: shared mounting was never sufficient. **Verified:** `tsc --noEmit` clean; `node --test` 166/166 including 3 new conformance gates; vocabulary gate clean; UI Kit lab shows the add-row present in all five panels with exactly one disabled and carrying its reason. Three BUGS rows filed with evidence (add-row divergence, duplicate Second Brain entry, and the stale-`dist/` false alarm that made origin/main look typecheck-red). | APPLIED | user · 2026-08-10 | (this commit) |
| AP-143 | 2026-08-10 | user directive | Graph visual encoding is canon: `ui-architecture-rules` §5c gains a Visual-encoding block; wiki companion updated (ADR-223) | User directive, verbatim: *"Yes, label the relations. Color code nodes."* — chosen from the two items ADR-221's cross-tool research named as the cheapest large wins. **What this approves:** (a) node colour is categorical by Database/type from a fixed ordered 12-hue `GRAPH_PALETTE`, assigned by stable sorted position of the distinct types present, replacing the `hsl(hash % 360)` hue hash that made two Modules render the same colour; (b) a legend is MANDATORY and shares one `colorOf` with the canvas and the selection panel; (c) colour is assigned over the whole resolved scope, before the `MAX_RENDERED_NODES` truncation, so the cut cannot recolour survivors; (d) every edge draws its Relation label ON the edge, rotated and normalised into (-90°, 90°] so it is never upside down, in a pill sized to its text (the old fixed 88px box overlapped constantly); (e) text fades with zoom — edge labels before node labels — a label wider than the gap between its nodes is withheld, selection always keeps its label, and suppression is stated on screen per §3a. **Accepted cost, stated:** ordered assignment means a type's colour can move when the scope changes; the legend is what makes that safe, and the stable-hash alternative re-admits collisions. **Not built:** node size by degree (radius currently encodes selection), and the local depth-1–2 graph that ADR-221 recommends as the version people actually use. **Verified:** `tsc --noEmit` clean; `node --test` 160/160 including 7 new `graph-visual` assertions; vocabulary gate clean; rendered in the UI Kit lab — legend reads Communities 1 / Deals 1 / People 2 in three distinct hues, three Relation labels draw upright along their edges, and the fourth (`worked with`, on a short edge between two adjacent nodes) is correctly withheld instead of landing on the node labels.  **Renumbered 2026-08-10** from AP-142/ADR-222 — origin/main landed K5 under those ids while this branch was open. | APPLIED | user · 2026-08-10 | (this commit) |
| AP-142 | 2026-08-10 | user directive | K5 — the Google integration emits metadata-first capture signals, post-approval, under a "google" consent source (ADR-222, TASK-049) | User directive: "start on k5" (fresh explicit start per the K-ladder rhythm). **What this row approves**: (1) `"google"` as the third `CAPTURE_SOURCES` member — ONE account-shaped toggle per the plan's "per-account toggle" (the pilot holds one connected Google account per user); default OFF, kill-switch, fail-closed parse, Human-only flips all inherited from the K2 state machine. (2) Emission at POST-APPROVAL materialization via both `google.onApproved` call sites — the human approval of the intake row is the emission warrant; vetoed records never signal; deterministic per-source-record ids make decide replays no-ops; emission failure logs and never fails an applied approval. (3) Metadata-only envelope mappers in core: thread → sender/subject/time, event → summary/attendees/time; `snippet`/`bodyText`/`description` structurally inexpressible; signals carry the intake entity's own taint label; the staged calendar payload gains the invite-list emails (additive, passthrough-safe). (4) `BuildWiringOptions.googleGateways` composition-test seam (runtime stays env-only, fail-closed). **Verified**: core 13/13 (4 seen RED under google-defaults-on and subject-dropped mutations), api google-capture 4/4 over the REAL wiring + fixture gateway (2 seen RED under consent-gate-stripped and snippet-leak mutations; consent-off silence, veto silence, replay idempotency, deletability all asserted), integrations-google suite green, full `pnpm verify` green (one unrelated culture-research socket-abort pair timed out under full parallel load, 57/57 in isolation, green on rerun), and a LIVE durable-boot walk over HTTP: fresh plane shows google OFF beside chat/whatsapp → Human flip records who/when → `google.syncGmail` fails CLOSED without OAuth config (no fake gateway) → pause silences with consent kept → resume restores verbatim → restart proves the consent row durable. **Honest limits**: no live end-to-end with a real Google account on this machine (ADR-215's real OAuth client registration is the standing blocker); attendees may include the owner's own address (self-exclusion deferred to K6); `timeOfDay` uses the API's local clock. (RENUMBERED same day from AP-141: a parallel branch's f8be8d6 reached main first with AP-141/ADR-221; later-arriving side renumbers per precedent.) | APPLIED | user · 2026-08-10 | (this commit) |
| AP-141 | 2026-08-10 | user directive | UI kit standardisation batch: `ui-architecture-rules` new §6b (SUPERSEDES §6a for View surfaces), §5g amended (macOS header row IS the titlebar, supersedes ADR-187's separate strip), §5c amended (Second Brain is Intelligence's first tab); wiki companion updated | User directives, verbatim across two turns: *"If table is empty show empty table, no text. Same for artefacts. I want the visual aesthetics retained even if empty."* and *"The header row is the one with traffic lights. I asked for workspace name (Test) not to repeat below traffic light. ALso the toggle should appear in same row as traffic light and so should Chief of Staff text. Also the avatar logo next to chief of staff isnt updated. It should be same as avatar. Make second brain the first toggle page inside intelligence."* **What this approves:** (a) **§6b supersedes §6a for any standard View** — a table/board/calendar/graph/Artefacts surface at zero records keeps header row, 40px grid pitch, add-row and aggregate footer and renders NO message; filler rows are `aria-hidden`, carry no cells and are not Records, so AP-002 is untouched. §6a still governs non-View surfaces. (b) The View's scroll box hugs its content (`max-h-full`, never `h-full`) so the add-row and aggregate footer land at the END of the section — the `h-full` + percentage-spacer attempt was measured over-stretching (box 203px, table 238px) and reverted. (c) **ADR-187's separate macOS titlebar strip is removed**: the rail's own `h-14` header row carries the drag region and pads past the 78px traffic-light gutter, so traffic lights, workspace name, centre toggle and the chat panel's Agent name share one line and the workspace name renders exactly once; the mac collapsed rail widens to gutter+44 so AppKit cannot paint over the switcher. ADR-187's actual invariant (one shared reservation, never per-column) is preserved — the reservation is now zero. (d) Avatar prefs become reactive (a listener set in `avatar-store`, `useAvatarPrefs`), fixing the stale Chief-of-Staff figure at its shared root rather than in the one component that reported it. (e) Second Brain becomes Intelligence's FIRST tab via `SecondBrainPage embedded` — one renderer, two entry points. **Verified:** `npx tsc --noEmit` clean, `node --test test/*.test.mjs` 153/153, `check-retired-vocabulary.mjs` clean, and the UI Kit lab (`/uikit.html`) measured at 1202/560/380px: `scrollWidth === clientWidth` on every toolbar row (never wraps) with the Filter label dropping to icon-only at 560px. | APPLIED | user · 2026-08-10 | (this commit) |
| AP-144 | 2026-08-10 | user directive | Cloud Plane per-message disclosure banner (`chat.turn.prepareCloud`/`confirmCloud`, `useChat.ts`) auto-confirmed instead of blocking on a second click; same standing consent extended to the new `point.rs` screen-locate feature (no separate screen-share prompt) | I flagged that the disclosure grant was a deliberate boundary — an older "caller-confirmed" cloud-egress path was explicitly retired (router.ts `chiefOfStaff.converse`) in favor of this single-use, `contextDigest`-pinned grant — and asked whether to remove it. User: *"The user chooses the model and thus consider it an explicit consent to send data to cloud provider."* Separately, asked whether "point at X" (new — locates a named UI element via a screenshot sent to Groq's vision model, `point.rs`) should show its own one-time screen-share confirmation or reuse the same standing consent; user: *"Same standing consent as chat."* **What this approves:** (a) selecting a Cloud Plane thread (i.e. picking a cloud model for that thread) IS treated as standing consent to send that thread's messages to its provider; `useChat.ts`'s `send()` now calls `chat.turn.prepareCloud` and immediately submits with the returned `grantId` instead of surfacing the amber disclosure banner for a second click. The grant mechanism itself is UNCHANGED — still fetched per message, still single-use, still pinned to the exact disclosed context — only the extra confirmation click is removed. `cloudDisclosure`/`confirmCloud` remain in `useChat.ts` and the banner JSX remains in `ChatView.tsx` (now unreachable via `send`, available to a future caller that still wants to show it). (b) `point.rs`'s `point_at_start` sends a screenshot to Groq with no separate per-request prompt — same reasoning extended to screen capture, not just message text. The Privacy Guard (`companion::guarded_frontmost_app`, never screenshot a password/credential-looking frontmost window) is UNCONDITIONAL and unaffected — that is a safety block, not a consent gate. Local Plane threads and the existing `CompanionAsk` screen-share checkbox are unaffected by either change. **Verified:** `cargo check` and `npx tsc --noEmit` clean. (RENUMBERED same day from AP-142: a parallel branch's K5 work landed under that id while this branch was open.) | APPLIED | user · 2026-08-10 | (this commit) |
| AP-140 | 2026-08-10 | user directive | K4 — fusion feeds every run through the one context door, Layer B memory budget, semantic embedder as Local-Plane default (ADR-220, TASK-048) | User directive: "start on K4" / "Now resume on k4" (fresh explicit start per the K-ladder rhythm). **What this row approves**: (1) `MEMORY_SLOT_BUDGET_CHARS` = 12,000 as the Layer B ceiling at `assembleRunContext`, tighten-only override, ranked-prefix truncation, always-reporting `trace.memoryBudget` meter; (2) the `@communications` and `@agent` converse runs retrieve via the same fusion machinery as chat, per-run flight + provider-plane gated (local memory never enters a cloud prompt); (3) the semantic embedder as the Local-Plane default, resolved from a dedicated Ollama adapter at the `semanticEmbedder` seam (NOT a local-mode completion provider — that first attempt hard-failed the converse suite, since completions have no per-call fallback; the embedding path degrades per ADR-213), with explicit provider overrides and the public-cloud boundary opted out. **Verified**: core 6/6 (4 seen RED under a budget-never-binds mutation), api 4/4 (2 seen RED under a fusion-never-feeds mutation; capture provider proves the accepted claim text reaches the @agent and @communications system prompts and vanishes with the flight off), full `pnpm verify` green, live durable boot's indexer pass logs the ADR-213 degrade warn naming the configured Ollama space — the default resolved and honestly downgraded with no daemon installed. Also repaired at the same time: the stranded-branch recovery's ADR-194/195+AP-114/115 number collisions (→ ADR-218/219, AP-138/139) and the stale Helpdesk-retirement assertion its own gates never ran. | APPLIED | user · 2026-08-10 | (this commit) |
| AP-137 | 2026-08-09 | assistant proposal | TASK-006's OAuth/Source-credential gate: self-hosted Google OAuth app vs. Composio-hosted broker (ADR-215, TASK-006) | Assistant drafted ADR-215 comparing two ways to close TASK-006's standing OAuth/Source-credential blocker: (A) route Google OAuth through Composio's hosted broker (faster — skips Bridge's own Google verification queue — but custodies live Gmail/Source tokens on a fourth-party's infrastructure, in tension with the Local Plane residency principle and TASK-038's plaintext-token cleanup direction); (B) Bridge registers its own Google Cloud OAuth app and stores tokens in the existing local AES-256-GCM vault pattern (`encrypted-file-credentials.ts`, the reuse target TASK-038 already names) — slower (Bridge owns Google's app-verification process) but keeps custody entirely on the Local Plane. **User decision: Option B** (Bridge's own OAuth app + local vault). No hosted OAuth broker is to be introduced. (RENUMBERED 2026-08-10 from this session's AP-135: a parallel branch's AP-135 — the TASK-036 vocabulary-allowlist batch — reached main first in 5d93778; later-arriving side renumbers per precedent.) | APPLIED | user · 2026-08-09 | (pending — implementation not yet started) |
| AP-136 | 2026-08-09 | user directive | K3 — the claim substrate ships in the minimal second-brain cut, with Claim as the engine vocabulary (ADR-216, TASK-047) | User directive: "Ok start ok K3, complete all of them k3a k3b and k3c", following the reviewed plan (`outputs/2026-08-09-k3-knowledge-substrate-revised-plan.md`: minimal cut; one substrate, two projections; sensor inputs arrive at later rungs unchanged). **What this row approves**: (1) engine vocabulary **Entity + Claim** — `check:vocabulary` retires the `knowledge` family, so the plan's "knowledge substrate" stays prose while code/schema/API speak claims (`claim_entities`+`claims` tables, `learning.claims.*`, resourceType `claim`); `@bridge/facts` is the acknowledged precursor, its consumer migration a named follow-up. (2) the fourth flight `BRIDGE_CLAIM_SUBSTRATE`, default off, double-entered with drop-tests on BOTH pilot surfaces (the hosted blueprint gained flight drop-tests for all four flights — K0 gap closed). (3) the closed red-class list (health, protected_characteristic, psychological_conclusion) structurally unproposable. (4) Local-first live proof; hosted migration 0040 lands only when TASK-036 unreds `supabase-migrate` (acknowledged, not worked around). (5) the minimal-cut scope narrowing of TASK-047 — model-backed distiller with evidence spans, fuzzy dedupe refusal band, and decay/archive automation become pre-agreed follow-ups. **Verified**: core 7/7 (mutation RED ×3), db 4/4 + migration no-op pin advanced, api 8/8 (pipeline-reject RED path proves direct writes unreachable), turbo 72 tasks green, vocabulary byte-identical to main at the 90 tracked findings, live durable-boot walk 10/10 over HTTP (propose→accept with decisionRef→graph.full projection→contradiction supersedes→history keeps lineage→forget deletes→reject suppresses), flight-off boot fails closed live, and the Second Brain UI walked in a real browser (propose→accept→claim row + claim-of edge rendered). | APPLIED | user · 2026-08-09 | (this commit) |
| AP-135 | 2026-08-09 | TASK-036 execution | `retired-vocabulary-allowlist.json` — batch 3 (WhatsApp Tool cluster + onboarding generic English); two categories of vocabulary flagged by `check:vocabulary` that cannot be renamed right now: **(1) WhatsApp Tool cluster** (`modules/whatsapp/src/tools.ts`, `modules/whatsapp/src/index.ts`, `modules/manifests/src/index.ts`, `modules/manifests/test/`, `modules/whatsapp/test/`, `apps/web/src/app/pages/WhatsAppPage.tsx`, `apps/web/src/app/pages/whatsapp/`, `apps/web/src/app/routes.tsx`) — the WhatsApp module's "Tools" tab is a module-scoped sub-page listing WhatsApp-specific capability panels; structurally different from the retired global Bridge Tools surface; renaming `whatsapp.tool.*` capability IDs during active branch work would require rebasing three in-flight WhatsApp branches simultaneously; TIME-BOXED — TASK-037 follow-up renames these after branches land. **(2) Generic English**: "A ticketing tool" / "A pipeline tool" in `apps/web/src/app/onboarding/questions.ts` — user-facing copy describing third-party product categories, not a Bridge product noun. | APPLIED | TASK-036 · 2026-08-09 | (this commit) |
| AP-134 | 2026-08-09 | user directive | K2 — chat + WhatsApp stores emit learning signals behind a per-source consent surface that defaults off (ADR-214, TASK-046) | User directive: "Keep doing all the tasks until I manually stop you… keep implementing and pushing to main" (standing continuation of the AP-131 K-ladder sequencing), then "After K2 stop". **What this row approves**: (1) new `@bridge/core` `learning/capture-consent.ts` — per-source consent (`chat`, `whatsapp`) with a global pause kill switch; the parser FAILS CLOSED (missing/corrupt/non-boolean/unknown-key all read OFF), pause silences everything without rewriting consent, every flip records who/when; stored in the Local Plane state store beside the data it governs. (2) new `learning/source-emitters.ts` — envelope-only mappers that structurally cannot see message text; WhatsApp inbound and cloud-plane chat turns map to null; attributes are `timeOfDay` + `surface`/`chatKind` only; signals carry the source's own taint label. (3) emission wired into `chat.turn.send` (user turn only, never the assistant's) and `whatsapp.ingestMessages` (outbound only), deterministic ids so replays are no-ops. (4) `learning.capture.status/setSource/setPaused` — status always answerable; the mutations are HUMAN-only (non-user identities FORBIDDEN); Settings gains `CaptureConsentCard` so the consent decision exists for the person, not just the API. **Verified**: core 9/9 with FIVE invariants seen RED under mutation; api 7/7 with the consent-gate bypass (4 RED) and a body-leak at the one full-row call site (privacy RED); full turbo **72/72**; **live over HTTP on the durable Local Plane**: default all-off → consent-OFF ingest produced nothing → Human flip recorded → consent-ON ingest produced exactly the send×3/chatKind pattern with no message text → evidence signal deleted live → pause silenced with consent intact → chat consent produced converse×3/chat_panel. **Honest limits**: live chat turns' model step failed (no local model; the user turn + signal persisted first); Settings card typecheck-verified, not browser-walked; WhatsApp signals attribute to the ingesting identity (ADR-212's named single-tenant gap); timeOfDay uses the API's local clock. | APPLIED | user · 2026-08-09 | (this commit) |
| AP-133 | 2026-08-09 | user directive | K1 — generic ledger→signal miner; the per-module DealPilot learning mapping is deleted forever (ADR-212, TASK-045) | User directive: "start" (the K-ladder's next phase after K0, sequenced by AP-131). **What this row approves**: (1) new `@bridge/core` `learning/ledger-miner.ts` — a Human's approve/veto/edit on any governed proposal becomes a generic `ObservedSignal` attributed by `skill` (the capability-attribution key); payload fields are structurally inexpressible to the mapper; `auto` decisions are never mined (learning from the machine's own calibration amplifies the machine, not the Human); deterministic ids make re-mining a no-op. All three boundaries mutation-checked RED. (2) DELETED: `modules/dealpilot/src/learning.ts` (+test, +exports), `learning.recordDealDecision` (which no web surface ever called — the loop's whole input side was API-reachable only), and every `default("dealpilot")` across eight learning procedures — the generic Settings surface had been silently showing one Module's learning while claiming to show the user's decisions. Lists go all-modules when unnamed; digest/promotions fan out across Modules with signals; archetypes make the Module REQUIRED. (3) The scheduled digest Skill mines first, then digests per discovered Module. **Verified**: core 6/6 (three seen-RED mutations), api learning 21/21 incl. the K1 exit e2e (three real pipeline approvals → suggestion, zero module code), full turbo **72/72**; **live over HTTP on the durable Local Plane**: 3 automation triggers → 3 `pending_review` halts → 3 approvals → digest mined 3 signals → suggestion "You have chosen approve 3 times when skill is learning.observationDigest" listed with evidence ids → second digest mined 0/proposed 0. **Honest limits**: the ledger's decision row inherits the proposer's actor, so decisions attribute to the mining owner — single-tenant-correct, named gap for multi-user; K9 rung 3 is unlocked, not built. | APPLIED | user · 2026-08-09 | (this commit) |
| AP-132 | 2026-08-09 | user directive | K0 — the three learning flights run live for the pilot; RunContextAssembler becomes the only context door (ADR-211, TASK-044) | User directive, verbatim: "start on AI harness". **What this row approves**: (1) `BRIDGE_LEARNING_OBSERVATION`, `BRIDGE_RETRIEVAL_FUSION`, `BRIDGE_COMMONS_ARCHETYPES` set to `1` in `render.yaml` and the desktop sidecar env, each surface with a failing-on-removal test — a dropped flag silently turns the harness off for every user because the API fails closed per procedure and nothing ever looks broken. (2) `chiefOfStaff.converse`'s three ad-hoc context paths (@communications, @agent via `invokeAgent`, intent classification) now project their system prompts through `assembleRunContext`/`projectToSystemPrompt`; `invokeAgent`/`classifyIntent` take `{ provider, runCtx }` so a model cannot be supplied without the seams to assemble its context; the old prompt builders are deleted, and the Communications + classifier prompts gain the kernel-invariants layer they never carried. `invokeAgent` gains the `memory` slot K4 will feed. (3) **The first live boot with the flight on found a shipping-blocking defect**: the digest Automation's id was a dotted key, the durable `automations` table is uuid-typed, and the API DIED at boot — which would have taken down the hosted pilot on the next push (Render deploys regardless of CI, ADR-195) and every desktop boot. Fixed to the fixed-UUID scheme with a seen-RED regression test. Lesson recorded: a flight that has never run live is not "built" — the flags-on boot IS the test. (4) The active-task index now renders dependency TASK-ids only (4953→3208 bytes, back under the 4096 agent-context budget); canonical prose stays in TASKS.md; the budget was NOT raised. **Verified**: full `turbo run typecheck test:coverage build` green (see TASKS evidence for counts); Rust sidecar 15/15; core 616/616 with the door mutation-checked RED; live Local-Plane boot serving `{"enabled":true}` from both status procedures and a real converse turn through the assembled context. **Not claimed**: model-backed converse paths live-exercised only via unit captures (no local model here); hosted flags take effect on next deploy. | APPLIED | user · 2026-08-09 | (this commit) |
| AP-131 | 2026-08-09 | user directive | AI Harness plan canonized; K-ladder becomes the sequenced next workstream (ADR-210; TASK-044..TASK-054; `docs/raw/ai-harness-plan-2026-08-09.md`) | User directive, verbatim: "start on AI harness" — the explicit gate the 2026-08-08 session record was waiting for ("Canonize: write the full docs/raw plan with task mapping, add the APPROVALS row (reorders roadmap priorities — needs explicit user gate), open TASK rows for the new phases"). **What this row approves**: (1) `docs/raw/ai-harness-plan-2026-08-09.md` is canon (status `approved`), with wiki companion `docs/wiki/ai-harness.md`; the session record under `docs/Progress from Manish/` stays verbatim as history. (2) Eleven new task rows TASK-044..TASK-054 map phases K0–K9 and K11 one-to-one; K10 IS the pre-existing TASK-043 (constitution enforcement), which gains a harness-gate note — nothing in K11 turns on before it. The ladder follows the closed TASK-021 reopened scope (ADR-196..209) as the roadmap's next priority. (3) The stale "TASK-040 = morning brief" pointer in the session record is corrected: TASK-040 has named LA3 Phase 2 since the 2026-08-04 renumbering wave; the brief's canonical home is TASK-050. (4) **Unified-learning spec reconciliation** (the plan's own open decision 2, "do not leave parallel"): `docs/superpowers/specs/2026-08-03-unified-learning-capability-design.md` is FOLDED IN — the harness plan is the ordering authority, the spec remains a design source for the research-source consolidation it specifies, its items enter execution only by mapping to a K-phase TASK row, and approving its designs remains a separate user gate. (5) The KG technology decision from the same session is recorded as ADR-210: Postgres substrate for K3, LangGraph rejected as wrong-category, Graphiti's bi-temporal/invalidation ideas borrowed behind our own port, Kùzu pre-agreed as a derived-index-only escape hatch. **Left open, deliberately**: the K11a keystroke content-vs-events decision is the user's, required before TASK-054 starts, and TASK-054 sits behind the K10 gate regardless. | APPLIED | user · 2026-08-09 | (this commit) |
| AP-130 | 2026-08-09 | user directive | Slice 14 — TM6's remaining deliverables: the projection stops lying about dependencies, ships its agent template, and the Commons install is PERFORMED (ADR-209, TASK-021) | User directive, verbatim: "Implement the open items". **What this row approves**: (1) the projection's `- Dependencies:` field was **hardcoded to `none`** from TM0 — true while the schema had no edges, a lie to the ledger's only reader once ADR-204 shipped them. It now reports real live blockers by path and title, distinguishes **`not read`** from `none` (a caller that did not load the edges has not established that a Task waits on nothing), and reports the cleared count separately. The edges are projected CONTENT, so they enter the drift hash and every emit site was threaded consistently; the Drizzle store reads them through its own transaction rather than opening a second one outside the row locks. (2) New `emitAgentLedgerTemplate`, written as `AGENTS.md` beside the projection — TM6's named per-repo deliverable. `tasks.md` says what the work is and never said how to work it; an external coding agent arrives knowing nothing about proposals, drift or evidence. Every fact is derived from the constant that enforces it, so the template cannot describe a contract the server would refuse. (3) TM6's exit criterion PERFORMED: a real Commons install (propose → Human decision → promote) followed by TM1's prototype test on the installed Module and a no-personal-data audit over what Commons holds AFTER the install and real Task work — the check that could catch an install path phoning home, which auditing the authored entry could not. **Three real behaviours the performed install found**: a root Module install refuses a `needId`; `installPropose` resolves an existing row for a version already held; and installing this Module stops for a Human rather than auto-activating (governance working, now asserted). **Honest scope**: "fresh Organization" is a fresh STATE inside the one permitted pilot id — `assertPilotOrganization` rejects every other — so the tenant boundary is not exercised. **Verified**: core `task-agent-ledger` 9/9 with three invariants mutation-checked (restoring the hardcoded `none` failed 6 tests); api `commons` 18/18 and `task-manager` 2/2; full `turbo run typecheck test:coverage build` **72/72**; `check:agent-context` exits 0; `check:vocabulary` clean for every file in this change set. One pre-existing db test was CORRECTED rather than loosened — it computed its before-hash without edges, the exact inconsistency this change makes impossible in production. No migration, no manifest bump. | APPLIED | user · 2026-08-09 | (this commit) |
| AP-129 | 2026-08-09 | user directive | Slice 13 — the planning edit, the dependency edge and the routing assignment become human-reachable (ADR-208, TASK-021) | User directive, verbatim: "Implement the open items". **What this row approves**: the Task Record Page gains three sections — planning Playbooks with a review-and-edit surface (ADR-200's decision, which every slice since recorded as UI-unreachable), Agent assignment showing the routing verdict and the Governance gate's band/calibration (ADR-207), and dependency edges with create/remove (ADR-204). The rules live in a pure `task-proposal-review.ts` because that is the part that can be wrong and the Node runner cannot assert `.tsx`; `EDITABLE_CONTENT_KEY` is IMPORTED from the materializer rather than copied, since a private copy renders an empty list the moment core adds a kind. Only the fields a kind materializes from are shown and sent (`.strict()` schema). A refusal is data, not an exception — a pre-mortem stays readable with no edit button. **A live check found a defect the unit tests could not**: the surface showed "Playbook scaffold" with no methodology and no explanation, because this module read an invented `questions` key while the real payload uses `prompts` — and the unit test asserted the same invented key, so it passed. A test written against an assumed shape cannot fail on the shape being wrong. Fixed to read `prompts`, surface the Skill's `note` and the Playbook's `methodology`, with the test now asserting a payload captured verbatim from a live Run. **Verified live against a running API**: a routing proposal approved through the UI wrote `assignedAgentId` and left the Task's status untouched; a dependency edge was created; a cycle was refused with its reason rendered as a sentence. Web logic tests 9/9 with three invariants mutation-checked. No API change, no migration, no manifest bump. **Not claimed**: the drafted-entry edit path was exercised live only in scaffold mode — no local model is installed in this environment. | APPLIED | user · 2026-08-09 | (this commit) |
| AP-128 | 2026-08-08 | user directive | TM3/TM4 slice 12 — the routing DECISION surface, binding `agent-task-routing-on-assign` (ADR-207, TASK-021) | User directive, verbatim: "Implement the open items". **What this row approves**: the last Automation this Module declared with no runtime binding gets one, together with the decision surface that is why it stayed last. `routeTaskByRequiredSkill` has existed since TM0 and `taskManager.route` exposed it as a QUERY — it answered "who is eligible" and nothing could act on the answer, because no write path set `assignedAgentId` after creation. New: `route` as a stageable proposal kind, pure `applyApprovedRoutingProposal` called by BOTH stores, and `taskManager.assign` running the routing as a Chief of Staff Agent Run and the ADR-202 band/calibration as a Governance one. The candidate set is resolved server-side (`TASK_ROUTING_CANDIDATE_AGENTS`) rather than supplied by the caller — on a path that writes, a caller naming one Agent manufactures the unambiguous answer ADR-107 forbids defaulting to. `human_assignment_required` stages NOTHING to approve. Routing across Modules can never be minor, so it always stops for a Human. The calibrated `auto_apply` branch is honoured through the same two steps a clicked approval takes, marked `calibrated: true` with the counts it was granted on. An approved routing assigns and does not start the Task. `agent-task-routing` narrows to `record:read`; Chief of Staff gains it in its allow-list — the grant ADR-201 deliberately withheld until something called it. **Verified**: core `task-materialize` 19/19 with three invariants mutation-checked individually (version staleness, required-Skill staleness, "assignment does not start the work" — each SURVIVED nothing, all three bit); api `task-manager-automations` 22/22 with the Chief of Staff attribution and the "stages nothing to approve" refusal mutation-checked; full `turbo run typecheck test:coverage build` **72/72**. Manifest `1.8.0`→`1.9.0` and its pinned assertion updated deliberately. No migration. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-127 | 2026-08-08 | user directive | CI repair — the in-app companion uses the shipped Zazoo renderer, and three of the four `check:vocabulary` families are cleared (TASK-021 / TASK-036) | User directive, verbatim: "Also there is one pipeline error in github repo. Can you check that and fix that also at the end and push those changes also?". **What this row approves**: (1) `AvatarOverlay.tsx`'s `AvatarFigure` now renders the shipped `zazoo/ZazooAvatar` instead of ~190 lines of its own hand-drawn SVG. `apps/web/test/avatar-prototype.test.mjs` was **added RED** in commit `42ff30f` (2026-08-08, another author) and had been failing on main ever since; `OverlayApp` already drove Zazoo through the same director, so the product shipped two avatars that could disagree about what "listening" looks like. The status→pose map is now defined once in `zazoo/status-performance.ts`, `triggerBlink` finally reaches this surface ("Avatar blink is the tell"), and reduced motion gets the static `ZazooCompact` head — the figure it replaced hardcoded its motion ON, which is the inconsistency `CompanionZazooFace` was written to stop copying. Props unchanged, so every call site keeps working. `AvatarStyle` maps to a Zazoo species by `kind`, falling back to the default rather than inventing a rig. (2) Two `check:vocabulary` findings that were THIS session's own prose (`brain`, `package`) rewritten. (3) A reviewed allowlist entry for `element` in `grid-theme.ts` — `document.documentElement` is the DOM's own name for the root node, a foreign contract Bridge does not own, which is exactly the allowlist's stated criterion. **Deliberately NOT done, filed instead**: the remaining 90 `check:vocabulary` findings are the WhatsApp Module's retired "Tool" primitive. They cannot be allowlisted — the allowlist's own contract says "Never exempt Bridge's own vocabulary: rename that instead" — and the rename is blocked on a real contradiction now written down in `docs/BUGS.md`: the manifest already models each one as a **Skill** capability, while the behaviour is a named UI surface with view modes invoked by a human click, and the glossary says a Human never invokes a Skill directly. Deciding it is a Tier C canon call about another Module's vocabulary, needs its own ADR, and changes persisted `whatsapp.tool.*` capability ids across ~90 sites; guessing the noun would ship a rename its owner has to undo. Tracked under TASK-036, which is the designated absorber for CI blockers found by other tasks. **Verified**: `@bridge/web` 135/135 (was 134/135 with this exact failure); full `turbo run typecheck test:coverage build` **72/72**, the first fully green turbo gate this session; `check:agent-context` exits 0. `check:vocabulary` still exits 1 on the tracked `tool` family alone. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-126 | 2026-08-08 | user directive | Task Manager TM6 slice 11 — a Playbook is DECLARED but is not a capability, and the Commons entry becomes findable (ADR-206; under the already-`done` TASK-021) | User directive: "Complete all the remaining ones before AI Harness. Validate , push and merge the changes". **What this row approves, and it deliberately DIVERGES from TM6's own wording**: TM6's deliverable reads "Module + Playbooks as signed Commons **capabilities**", but ADR-180 established that a capability is the thing that is GOVERNED — permissions and a trust lifecycle — which is exactly why `view` stopped being one. A Playbook holds NO permissions; the Skills it names hold them all, and giving a Playbook permissions would create a second place authority could widen unnoticed. TM6's line predates that boundary, and following it literally would re-open the question ADR-180 closed. **So**: new `ModulePlaybookBinding` on `ModuleSurfaceManifest`, parsed and validated by `parseModuleManifest`, derived in the Task Manager manifest from `TASK_PLAYBOOKS` — declared, auditable, and NOT capability-typed; `CapabilityType` gains no member. **It still has to be declared** because a Playbook is shipped, signed, versioned content that shapes what a model is asked to do: a manifest omitting it hands a fresh Organization five methodologies its own manifest never mentioned, so nothing installed could be audited against what arrived — the whole point of signing. **Validated, not merely recorded**: a Playbook must name at least one Skill (one that may run nothing is a description, not a methodology) and every Skill it names must be one this Module declares, or a fresh install runs a methodology whose Skills never arrived. Derived rather than restated, the same anti-drift rule ADR-197 set. **The Commons entry becomes findable**: Commons exists so someone with a NEED can find the capability that meets it, and an entry tagged only `built-in` + kind is shelfware; Task Manager now carries capability tags plus three `need:` tags, each naming something it actually ships. **The no-personal-data audit is over the SERIALIZED entry**, not hand-picked fields — it rejects e-mail markers, the pilot identity, and ANY UUID at all, because every id in a published package is a stable kebab-case manifest id and a UUID there is a runtime row that escaped; a leak arrives in whatever field nobody thought to check. Manifest `1.7.0`→`1.8.0`. **Deliberately NOT done and recorded**: the per-repo agent-ledger template and a LIVE fresh-Organization Commons install, both named in TM6's deliverables — the audit here covers what WOULD be published, not a performed install; `agent-task-routing-on-assign` is the last unbound Automation; no UI reaches the planning edit decision or creates a dependency edge. **Verified**: manifests catalog 10/10 with both new tests mutation-checked (shipping no Playbooks, and stripping the `need:` tags, each failed exactly one test); api 463 total / 462 pass / 1 skipped / 0 fail; core 597/597; turbo typecheck+build 48/48. One pinned version assertion updated deliberately. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-125 | 2026-08-08 | user directive | Task Manager TM5 slice 10 — the execution queue reaches Second Brain, Graph view becomes eligible on the Task Page, and every Automation entry names its runtime id (ADR-205; under the already-`done` TASK-021) | User directive: "Complete all the remaining ones before AI Harness". **Three things were wrong, and only the first was written down.** (1) **Second Brain had no Tasks**: ADR-110 settled that Second Brain IS Graph view at `scope: full`, and `graph.full` composes Records/People/Communities/Events/Modules/Agents — but Tasks live in their own table, so the one Database the user works out of daily was the one thing the full-scope graph could not show. (2) **Graph view was structurally IMPOSSIBLE on the Task Page**, not merely unbuilt: `computeEligibleKinds` requires a non-parent relation column and the Task Database had only `parentTaskId`, so this had to follow ADR-204 rather than precede it. Calendar was already eligible (`scheduledFor` is a date column) and Tree already was too — Graph is the one that needed the edge. (3) **Nine Automation manifest entries were lying**: `automationId` came from a four-branch ternary listing the bindings as of ADR-198, so every binding added across ADR-201..204 shipped an entry claiming no runtime Automation stood behind it — exactly the "declared, not built" signal this workstream exists to make trustworthy. **What this row approves**: `graph.full` composes Task nodes plus BOTH edge kinds; the Task Page gains a read-only `dependsOn` relation column; `automationId` derives from `resolveModuleAutomationRuntimeId`, the one function that owns runtime ids, so a new binding cannot land without the manifest following. Manifest `1.6.0`→`1.7.0`. **Both edge kinds because they are different questions** — the tree says where work SITS, the dependency says what it WAITS ON; collapsing them leaves Second Brain unable to answer either. **The queue honours the same node cap and drops CLOSED work first** — a queue reliably outgrows every other Database, and an archived Task is history rather than context; `hasMore` accounts for what was left out. **`dependsOn` is read-only in the grid** because an edge can close a cycle and a cycle is refused server-side with a reason — an editable cell would hide that refusal behind a control that silently reverts. **Deliberately NOT done and recorded**: `agent-task-routing-on-assign` is the last unbound Automation; no UI reaches the planning edit decision or creates a dependency edge (both API-reachable, not yet human-reachable); TM6 untouched. **Verified**: api 463 total / 462 pass / 1 skipped / 0 fail with a new end-to-end graph test, mutation-checked twice (omitting Tasks from the graph and collapsing the two edge kinds each failed exactly one test); web 134/135 with the one pre-existing avatar failure reproduced at base; turbo typecheck+build 48/48. One pinned version assertion updated deliberately. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-124 | 2026-08-08 | user directive | Task Manager TM3/TM4 slice 9 — Task dependency Relations, dependency-aware sequencing, and `dependency-unblock-notifier` (ADR-204; migration 0039; under the already-`done` TASK-021) | User directive, verbatim: "Cover all the dependencies also" (following "Complete all the remaining ones before AI Harness. Validate , push and merge the changes"). **What this row approves — includes a SCHEMA MIGRATION**: `0039_task021_task_dependencies` adds the `task_dependencies` table the plan has specified since TM0 and the schema never had; new pure `packages/core/src/task-dependencies.ts`; `listDependencies`/`addDependency`/`removeDependency` in BOTH stores; `proposeQueueSequence` and `analyzeTaskImpactFit` become dependency-aware; new Chief of Staff Skill `task-manager.dependency-analysis`; `dependency-unblock-notifier` bound with tRPC procedures `dependencies`/`addDependency`/`removeDependency`/`runDependencyUnblockNotifier`. Manifest `1.5.0`→`1.6.0`. **ONE edge kind, not two**: `blocked_by` is the same edge read from the other end, so storing both directions would let them disagree — the row is always "A depends on B" and the reverse is a query. **A cycle is refused at WRITE time** because it is unsatisfiable rather than unwise — every Task in it waits forever and no amount of finishing work clears it; the API answers **409, not 500** (the request was well-formed and the answer is no); the one-hop case is additionally a CHECK constraint, and the Drizzle store takes `FOR UPDATE` on the edge set before validating because two concurrent edges could each look acyclic alone and close a loop together. **What stops blocking**: a blocker that is `done`, `abandoned` or GONE. `abandoned` counts as satisfied deliberately — a blocker nobody will do can no longer block, and leaving the dependent waiting forever is the rot the edge exists to expose. **"Unblocked" means the wait ended**, not that there never was one; each finding names what cleared it. **The notifier NOTIFIES** — a blocker landing does not make the dependent Task started, and flipping its status would decide for the Human that the work is theirs to pick up. **Opt-in vs mandatory, deliberately different**: `proposeQueueSequence` takes the blocked-set optionally (a caller with no store to read gets the status-only ordering rather than a silently wrong claim), while `dependency-analysis` THROWS without an edge list (a missing input and a clean graph must not look identical). **Migration safety**: purely additive, no backfill, no change to `tasks`, so `DROP TABLE "task_dependencies"` reverses it exactly; composite FKs on `(organization_id, id)` make a cross-tenant edge inexpressible; RLS with the standard tenant predicate. **Deliberately NOT done and recorded**: `agent-task-routing-on-assign` is the last unbound Automation and needs a routing decision SURFACE applying the ADR-202 gate, which is product work rather than a binding; no UI reaches the planning edit decision or the dependency graph; TM5/TM6 untouched. **Verified**: core 597/597 with all four invariants mutation-checked one at a time (dropping cycle detection, treating an abandoned blocker as blocking, counting a dependency-free Task as newly unblocked, and ignoring the edges in sequencing each failed exactly one test); api 462 total / 461 pass / 1 skipped / 0 fail with two new end-to-end tests; db 234/234 including the migration-metadata no-op check; turbo typecheck+build 48/48. Three pinned assertions updated deliberately: the migration head 0038→0039, the manifest version, and the catalog-size floor 18→21. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-123 | 2026-08-08 | user directive | Task Manager TM3/TM4 slice 8 — the four Internal Strategist Automations get runtime bindings (ADR-203; under the already-`done` TASK-021) | User directive: "Complete all the remaining ones before AI Harness. Validate , push and merge the changes". **What this row approves**: one shared `runTaskManagerAgentAutomation` helper plus runtime ids and Skill bindings for `task-created-impact-analysis` (→`impact-fit-analysis`), `task-tree-restructure-proposal` (→`task-tree-restructure`), `target-change-reopen-prompt` (→`impact-fit-analysis`) and `goal-review-cadence` (→`queue-guard`, filtered to `goal_review_due`); Internal Strategist's allow-list gains exactly those three Skills; manifest `1.4.0`→`1.5.0`. **The subtle part**: three of the four ALREADY raised a governed proposal that halted for Human review, so the governance story was true — what was false was the ATTRIBUTION. Each proposal's actor was `ctx.identity` (the Human who triggered it) with `skill: KERNEL_PASSTHROUGH_SKILL`, so the wiki's load-bearing rule "every Task/Goal create triggers Internal Strategist impact-fit-analysis" described an analysis that was real but had no Agent Run behind it and no Skill invocation to point at. **This is MORE governance, not a convenience**: an Agent actor is subject to the allow-list and capability scope a Human actor bypasses, which is why the Skill grants had to be written down and the pinned `allowedSkills` deepEqual updated deliberately rather than loosened. **A moved target is an impact-fit question** — `target-change-reopen-prompt` needed no Skill of its own; "should this reopen because what we want changed" IS "does this still fit", so the Skill's output becomes the reasoning attached to the prompt, and the Run's proposal shares the reopen proposal's id so one decision resolves both. **`goal-review-cadence` stays Internal Strategist's** even though `queue-guard` is Governance-owned by default: whether a goal is due for review is a planning question, not a control one (ADR-107's split applied to a guard), and one Skill resolving for two eligible Agents is the shape ADR-104 was built for. **Deliberately NOT done and recorded**: `agent-task-routing-on-assign` and `dependency-unblock-notifier` stay unbound — the first needs a routing decision surface that applies the gate, the second needs schema Relations that do not exist; no UI reaches the planning edit decision; TM5/TM6 untouched. **Verified**: api 460 total / 459 pass / 1 skipped / 0 fail with three new tests, both attribution claims mutation-checked — reverting the impact analysis to a kernel-passthrough proposal failed 4 tests, and running `goal-review-cadence` as Governance failed 1; core 592/592; db 234/234; turbo typecheck+build 48/48. Two pinned assertions updated deliberately. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-122 | 2026-08-08 | user directive | Task Manager TM3/TM4 slice 7 — the queue guard and the approval gate become Skills, and the gate calibrates on real decision history (ADR-202; under the already-`done` TASK-021) | User directive: "Complete all the remaining ones before AI Harness. Validate , push and merge the changes". **What this row approves**: two new Governance-owned Skills — `task-manager.queue-guard` (runs `evaluateTaskGuards`) and `task-manager.change-gate` (runs `classifyTaskChangeBand` + `calibratedTaskChangeDecision`) — both of which existed as core code with NO Skill id, which is why four Automations declared since TM0 (`wip-breach-detector`, `unverified-done-challenger`, `reschedule-approval-gate`, `routing-approval-gate`) had nothing to run; runtime ids for all four; procedures `taskManager.runQueueGuard` and `taskManager.runChangeGate`; Governance's allow-list moved into one shared `GOVERNANCE_ALLOWED_SKILLS`. Built-in manifest `1.3.0`→`1.4.0`. **The load-bearing change**: a gate that trusts the caller's account of its own track record is not a gate. `runChangeGate` counts REAL approve/veto decisions from the ledger, matching on `skill === "task-manager.change-gate"` (the capability-attribution key) and the change kind, bounded to 200 rows — only the gate's own prior decisions count, because a decision on some other proposal says nothing about whether this Human's reschedules have been sound. The pre-existing `taskManager.approvalBand` query keeps taking client-supplied counts and is unchanged — it is a read-only what-if with no authority behind it — but it is no longer the only path and the Automation path does not use it. **Two Skills not one, and each Automation reports only its own finding kind**: the state of the queue and whether one change may proceed are different questions; and one evaluator with several callers is not the same as one Automation dumping every finding under whichever name you invoked it by. **Least privilege made a real mismatch visible**: both new Skills only read, so both declare `record:read` alone; the first run REJECTED them because Governance has no `record:write` and the generic Task Manager manifest shape was handing every Skill both — fixed by narrowing the Skills, not by widening the Agent. **ADR-073 lineage held**: the kernel decides and the Agent explains; the gate never applies the change, and an Agent-proposed change always needs a Human whatever the history says. **A finding recorded, not papered over**: `unverified-done-challenger` has no reachable path through `taskManager.transition` because the store already refuses `done` without verification evidence — the guard covers rows reaching `done` by projection reconcile, which is why it exists, and its API test asserts an honest empty result rather than manufacturing an impossible row. **NOT covered by a test, recorded**: the router's one-line `ctx.identity.type`→`actorType` mapping is not separately exercised (every test caller is a user), though the rule it feeds is asserted directly in core; the positive calibration direction (3 vetted approvals ⇒ `auto_apply`) is likewise core-only, not end-to-end. **Verified**: api 457 total / 456 pass / 1 skipped / 0 fail with two new tests, both mutation-checked — unfiltering the guard findings and swapping the ledger history for caller-supplied numbers each failed exactly one test; db 234/234; turbo typecheck+build 48/48. Two pinned version assertions updated deliberately. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-121 | 2026-08-08 | user directive | Task Manager TM3/TM4 slice 6 — Chief of Staff gets a governed runtime Agent identity, and its two cadence Automations run (ADR-201; under the already-`done` TASK-021) | User directive, verbatim: "Ok, build the rest other than AI Harness" and "Complete all the remaining ones before AI Harness. Validate , push and merge the changes". **What this row approves — this is the canon change**: Chief of Staff gains a physical governed-pipeline identity (`CHIEF_OF_STAFF_AGENT_RUNTIME_ID`, `ensureChiefOfStaffGovernance`, in-memory + both persistent seeds), reversing a TASK-007-era `wiring.ts` comment that read the glossary line "its routing role is a product composition, not an architectural requirement" as "CoS never acts as a pipeline actor". That reading was made when nothing needed CoS to act; ADR-107 then deliberately gave it ownership of routing/dispatch and this Module's manifest declares four Automations and three Skills under it. An Automation starts an Agent Run and only an attributable Agent may invoke a Skill, so **an owner with no runtime identity owns nothing that can run** — the ADR-107 split was decorative. The glossary sentence is about how chat routing is composed, and its own first clause already calls Chief of Staff a "default coordinating Agent". The old comment is amended, not deleted, so both the earlier reasoning and its supersession stay readable. **The scope is narrow on purpose**: `signal:write`/`record:read`/`record:write`, deliberately WITHOUT `record:archive` (CoS coordinates and reports; Governance archives); the Skill allow-list is exactly one entry (`progress-synthesis`) — `agent-task-routing` and `habit-scaffolding` are CoS-owned in the manifest and deliberately NOT granted, because authority with no reachable behaviour is authority for nothing. One shared `CHIEF_OF_STAFF_ALLOWED_SKILLS`, same anti-drift shape as Internal Strategist's. **Also approved**: `standup-brief` and `stale-task-review` gain runtime Automation ids and one procedure `taskManager.runQueueBrief`; both REPORT and stop (no Task-Manager proposal, step action `read`, no proposed status — making a Human approve being told about their own Tasks would be theatre, and what a rotting Task needs is a judgement); one Skill behind both because `progress-synthesis`'s `stalled` list IS the staleness question over a longer window, so a nineteenth Skill would have been a roster entry not a capability; `evaluateTaskGuards` gains the `stale_task` finding it never had, sharing `isStalenessEligible` with `synthesizeProgress` — narrower than `taskIsOpen` because a `candidate` and a `parked` Task are SUPPOSED to sit untouched, while `blocked` IS included; staleness is SKIPPED rather than guessed when no clock is passed (a pure function must not report findings nobody can reproduce); the WIP limit becomes a parameter instead of a hardcoded 1. Built-in manifest `1.2.0`→`1.3.0`; the 1.2.0 comment corrected in passing (it wrongly claimed `standup-brief` gained a runtime id under ADR-198). **Deliberately NOT done and recorded**: CoS's other two Automations stay unbound — `agent-task-routing-on-assign` belongs with the routing gate and `dependency-unblock-notifier` needs schema Relations that do not exist; seven further declared Automations still unbound; no UI reaches the planning edit decision; TM5/TM6 untouched. **Verified**: core 592/592 with four invariants mutation-checked one at a time (the status exclusions, the missing-clock skip, the WIP parameter, and the shared `stalled` definition each failed exactly one test); api 455 total / 454 pass / 1 skipped / 0 fail, with the Chief-of-Staff attribution mutation-checked by re-running the brief as Internal Strategist (2 failures); db 234/234; turbo typecheck+build 48/48. A first mutation attempt SURVIVED and exposed real duplication — the guard inlined the status check instead of calling the shared helper — fixed, then the mutation bit. A second survived because the API-level candidate-exclusion assertion was vacuous (every row created through the API is new), so that assertion moved to core where `updatedAt` can be set. Two pinned version assertions updated deliberately. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-120 | 2026-08-08 | user directive | Task Manager TM3/TM4 slice 5 — a reviewer may correct a planning draft instead of only accepting or rejecting it (ADR-200; closes the residual AP-119 named first, under the already-`done` TASK-021) | User directive, verbatim: "Ok, build the rest other than AI Harness". **What this row approves**: `candidate` joins `projection_reconcile` as an editable proposal kind in `taskManager.decideProposal`; new pure `mergeEditedPlanningPayload` in `packages/core/src/task-materialize.ts`; new `taskManager.proposal` read query (an edit surface with no read surface is unusable — nothing could fetch a Task-side draft to show a reviewer); a decided `candidate` proposal's Automation Run is finished again with the decision and result, matching what `archive_sweep` already does. **The edit is an allow-list MERGE, not a payload replacement**: only the ONE content key the staged kind materializes from (`children`/`candidates`/`opportunities`/`outcomes`) comes from the Human; `kind`, the run id, the model receipt and the Playbook's questions carry over verbatim. `kind` is absent from the wire schema entirely, because it selects which branch of the materializer runs — an edit able to carry a new one could turn a reviewed pre-mortem into an unreviewed decomposition. `EDITED_PLANNING_ITEM` is `.strict()`, the merge re-validates against the kind, and the materializer validates every entry again: three checks, because a human's edited text is exactly as untrusted as a model's output. **Two deliberate refusals**: an edit that materializes nothing is rejected rather than approved (an approval that writes nothing is a veto wearing an approval's clothes — the ledger would record consent to a plan the queue never gets); a pre-mortem refuses an edit outright, because approving one writes nothing by design. **Provenance survives**: the Agent's original draft is kept on the proposal row under `agentDraft`, since the queue-side row is where a reviewer looks and an edit that erased the model's draft would destroy the provenance the Playbook exists to produce. **Other kinds stay approve-or-veto on purpose**: `archive_sweep` and restructure operations are computed plans over specific rows and versions checked for staleness at decision time, so an edited one is a different unchecked plan, not a corrected draft. **Deliberately NOT done and recorded**: no UI reaches the edit — `TaskManagerPage` renders approve/veto for an `impact_fit` proposal only and a planning-proposal review surface does not exist, so this decision is API-reachable and not yet human-reachable; `standup-brief` still needs a Chief of Staff runtime Agent; nine declared Automations still unbound; dependency-aware sequencing still needs absent schema Relations; TM5/TM6 untouched. **Verified**: core 588/588 (5 new; all four new invariants mutation-checked one at a time — dropping the empty-edit guard, the pre-mortem guard, the `agentDraft` provenance, and letting the edit supply the kind each failed exactly one test); api 453 total / 452 pass / 1 skipped / 0 fail, with the two new API tests seen RED twice (restoring the ADR-199 refusal fails 2; ignoring the edited payload fails 1); db 234/234; turbo typecheck clean; `check:agent-context` clean. Two gate failures reproduced at the unmodified base commit `dc46636` and therefore NOT from this change set: `check:vocabulary` (WhatsApp Module + `modules/manifests` "Tool" terminology, already tracked under TASK-036/AP-096) and one `@bridge/web` test ("the in-app browser companion uses the shipped Zazoo renderer"). | APPLIED | user · 2026-08-08 | (this commit) |
| AP-119 | 2026-08-08 | user directive | Task Manager TM3/TM4 slice 4 — approving a planning proposal materializes it, and the planning model call becomes governed (ADR-199; closes the two residuals AP-118 named first, under the already-`done` TASK-021) | User directive, verbatim: "Close the next open items" — the two ADR-198 recorded first. **What this row approves**: new pure `packages/core/src/task-materialize.ts` (`applyApprovedPlanningProposal`), called by BOTH `decideProposal` implementations (in-memory and Drizzle) so the two durability backends cannot materialize an approved plan differently; `TaskProposalKind` already carried `candidate` and `kind` is a `text` column, so **no migration was needed** — `stageProposal`'s narrowed union is widened; `runPlanningPlaybook`/`runOpportunityScan` compute the proposal id before the run and pass it as the pipeline `proposalId`, so the ledger proposal and the Task-Manager `candidate` row share one id and one human decision resolves both; `RunCtx` gains an optional `modelProvider` so `runPlanningPlaybook` can hand the Skill a `createGovernedModelProvider`-wrapped local provider and return a real `modelReceiptLedgerId`. **The limits are the design, not omissions**: generated Tasks land as `status: "candidate"` (approved means "worth having in the tree", not "start these"); dot-paths are RECOMPUTED against the current queue, never taken from the payload, because a queue that moved between draft and approval would otherwise get two Tasks at one address; an approved pre-mortem writes nothing on purpose; approved outcomes never arrive as the north star; each scan candidate lands under the Task its own finding named and a finding whose source row is gone is dropped rather than re-homed; generated Tasks are owned by the APPROVER, never the drafting Agent; drafting caps are re-applied at materialize because the payload is editable and therefore untrusted; an empty scan raises no proposal at all. **Plane unchanged**: the planning binding stays `planeDefault: "local"` — governance is not consent, and the private queue does not reach the Cloud Plane because the call is now audited. **Deliberately NOT done and recorded**: `decideProposal` still refuses `edit` for every kind but `projection_reconcile`, so a reviewer can approve or veto a planning draft but not edit it in place (the materializer already re-validates the payload as untrusted human text, so the gap is API surface, not safety); `standup-brief` still needs a Chief of Staff runtime Agent; nine declared Automations still unbound; dependency-aware sequencing still needs absent schema Relations; TM5/TM6 untouched. **Verified**: core 583/583 (9 new materializer tests; the candidate-status, north-star and finding-provenance invariants each mutation-checked individually); api 450 pass / 1 skipped / 0 fail including a full end-to-end test — local model double drafts, the governed Run halts for review, approval creates the child Tasks as candidates under the right parent with the right exit tests, and a veto writes nothing — seen RED by disabling the materialize branch and again by dropping the governed provider; db 234/234; turbo typecheck 44/44; `check:agent-context`, `check:no-dummy-runtime` and `check:vocabulary` clean for this change set. `runPlanningPlaybook`/`runOpportunityScan` now require `expiresAt`, matching the other two Task Manager Automation procedures. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-118 | 2026-08-08 | user directive | Task Manager TM3/TM4 slice 3 — the last ten echoing Skills get real logic, the echo fallback becomes a throw, and two declared-not-built Automations get runtime bindings; built-in manifest `1.1.0`→`1.2.0` (ADR-198; completes AP-116/AP-117 under the already-`done` TASK-021) | User directive, verbatim: "implent automation and skills" — following AP-117's recorded residual that eight Skills still echoed and nothing invoked the planning ones. **What this row approves**: new `packages/core/src/task-execution.ts` (`verifyTaskEvidence`, `synthesizeProgress`, `scaffoldHabits`, `scanForOpportunities`); `exit-test-authoring` added to `task-playbooks.ts` under the existing `measurable-review` Playbook rather than a sixth Playbook, keeping the plan's v1 cap of 5; `wiring.ts`'s `runTaskExecutionSkill` binding those four plus the five whose logic already existed in `@bridge/core` and had never been bound (`applyTaskRestructure`, `routeTaskByRequiredSkill`, `classifyTaskChangeBand`+`calibratedTaskChangeDecision`, `emitTasksMarkdown`/`detectTaskProjectionDrift`, `planCompletedBaySweep`); **the echo fallback replaced by a throw**, so a registered `task-manager.*` id with no implementation fails loudly instead of looking like a working Skill; two new tRPC procedures (`taskManager.runPlanningPlaybook`, `taskManager.runOpportunityScan`) running through `automationExecutor` as attributable Internal Strategist Runs whose proposals halt at `pending_review`; built-in Task Manager manifest `1.1.0`→`1.2.0` for the new `planning-playbook` Automation and the runtime id now behind `proactive-scan-cadence`. **Agent authority**: Internal Strategist gains exactly six allow-listed Skills (the five a Playbook run invokes, plus the scan) — it owns more than that and the unused ones are deliberately not granted; **capability scope is unchanged**; the list moves to one shared `INTERNAL_STRATEGIST_ALLOWED_SKILLS` in `@bridge/db` so the in-memory and persistent seeds cannot drift. **Deliberately NOT done, named rather than implied**: `standup-brief` gets NO runtime binding — it is Chief of Staff-owned and CoS has no governed runtime Agent identity, so binding it to Internal Strategist would make it run by discarding ADR-107's ownership split; approving a planning proposal materializes nothing yet (a decomposition does not create child Tasks, a scan finding does not create a candidate Task — both stop at the Review inbox); the planning Skills still return a `modelReceipt` without routing through `createGovernedModelProvider`; nine further declared Automations still have no runtime binding; dependency-aware sequencing still needs schema Relations that do not exist; TM5/TM6 untouched. `completed-bay-sweep`/`ledger-projection` accept two input shapes so the externally certified procedures that pass a pre-computed plan keep working — the caller-supplied branch re-derives the projection content hash and rejects a sweep plan missing any record version, so it validates rather than echoes. **Verified**: core 574/574 (11 new, with the verifier-naming and non-goal-habit invariants mutation-checked individually); api 447 pass / 1 skipped / 0 fail including 4 new Automation tests and a catalog-wide no-echo test across all 18 Skills, seen RED against a restored echo fallback; db 234/234; turbo typecheck 44/44; `check:agent-context`, `check:no-dummy-runtime` and `check:vocabulary` clean for this change set. Two pinned assertions updated deliberately (manifest version). One pre-existing `@bridge/web` failure ("the in-app browser companion uses the shipped Zazoo renderer") reproduced in a clean worktree at the same base commit — not from this change set. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-117 | 2026-08-08 | user directive | Task Manager TM3 slice 2 — the four model-backed planning Skills stop echoing, and the five Playbooks get real content to parameterize them (ADR-197; continues AP-116 under the already-`done` TASK-021) | User directive, verbatim: "finish the model-backed planning Skills task" — the residual AP-116/ADR-196 named as open. **What this row approves**: new `packages/core/src/task-playbooks.ts` carrying (a) `TASK_PLAYBOOKS` — the five already-registered playbook ids (`outcome-key-results`, `backward-planning`, `clarify-organize`, `measurable-review`, `pre-mortem`) now with methodology, intent, the technique's questions, Bridge-authored system-prompt guidance, and the Skills each supports — and (b) the four Skills `frameGoalOutcomes`, `generateCandidateTasks`, `runPremortem`, `decomposeTask`; `TASK_MANAGER_PLAYBOOKS` in `task-manager.ts` re-derived from that content so roster and content cannot drift; `wiring.ts`'s `runTaskAuthoringSkill` so the four execute through the governed registry, declared `executionClass: "authority_bearing"` because a Skill that calls a model is not pure data. **Model when configured, Playbook questions when not**: with no model the result is `source: "playbook_scaffold"`, zero drafted items, and a `note` saying why — never a bare empty list, which would be indistinguishable from "the methodology found nothing"; the same degrade catches prose, malformed JSON, and entries that fail validation. **Local Plane only**, matching the manifests' own `plane: "local"`: `planeDefault: "local"` makes the router fail rather than fall through to cloud, so a planning Skill cannot egress the private queue to write its plan; resolution is per call, never a boot snapshot, because the desktop managed model turns healthy ~30s after wiring. **Structure the model may not author**: child dot-paths computed from the parent and highest direct sibling (an invented path could collide with a live Task or reorder the queue), generated options pinned to `status: "candidate"`, no outcome ids, at most one north star, missing `parentPath` throws. **IP**: techniques are public-domain concepts, named as techniques and written from scratch; the two associated with trademarked book brands are deliberately not named after them and none of their text is reproduced (plan §7). **Recorded as NOT done rather than implied**: no Automation or tRPC procedure invokes these four yet (reachable through the registry, not yet on a scheduled or UI path); these Skills return a `modelReceipt` for the caller to ledger but do not route through `createGovernedModelProvider`, which needs an `ApiContext` the skill registry does not have — acceptable only because the binding cannot egress, and it belongs with the invoking Automation; eight `task-manager.*` Skills still echo; dependency-aware sequencing still needs schema Relations that do not exist; TM5/TM6 untouched. **Verified**: core 563/563 with the four load-bearing new assertions mutation-checked (scaffold guessing `isGoalProposed`, trusting the model's north-star claim, letting the model choose `status`, and letting a model-supplied path through each made their test fail); api 442 pass / 1 skipped / 0 fail with the four new registry tests seen RED against the restored echo fallback; turbo typecheck 44/44; `check:agent-context` clean; `check:vocabulary` clean for every file in this change set, with the remaining failures the pre-existing `modules/whatsapp` + `modules/manifests` "Tool" terminology already tracked under TASK-036/AP-096 and confirmed on `origin/main`. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-116 | 2026-08-08 | user directive | Task Manager TM3 slice 1 — the registered-but-echoing planning Skills get real deterministic logic; `impact_fit` stops being a boolean (ADR-196; reopens implementation scope under the already-`done` TASK-021) | User asked for the current state of Task Manager, then whether a gap existed between its original plan and the implementation, then directed "can you start implementing these gaps first. Post this we can go with the AI harness one". The audit against `taskmanager-module-plan-2026-07.md` found TM0/TM1/TM2 and half of TM4 genuinely shipped and externally certified, while **TM3 was declared and not built**: all 18 `task-manager.*` Skill ids were registered `SkillManifest`s whose `run()` fell through to `return { proposedOutput: inputs }` (only `create-task` had behaviour), and `draftTaskCreate`'s `impact_fit` payload was the single boolean `requiresResequenceReview: true`. **This row approves closing that gap for three Skills and recording the rest as open**: new pure `packages/core/src/task-planning.ts` (`findDuplicateTasks`, `proposeQueueSequence`, `analyzeTaskImpactFit`), an `impact_fit` payload carrying real findings with pre-existing keys preserved, and `wiring.ts`'s `runTaskPlanningSkill` so the three Skills execute instead of echo. Deterministic with no model call, per the ADR-183/ADR-177 rule (runs on every create; must show the terms that produced a finding; must degrade to "nothing found" rather than a guess); duplicate bar deliberately higher than parent suggestion's because a false duplicate argues against work the user just chose. Skills stay `pure_data` — queue arrives as authorized input, and a missing queue throws rather than fabricating a clean result. **Deliberately NOT done and named in the ADR/TASKS rather than implied**: dependency-aware sequencing (needs `depends_on`/`blocked_by` Relations absent from the schema — `blocked` status honored instead), the four model-backed TM3 Skills, Playbook content (still an id list), `proactive-opportunity-scan`'s Automation, TM5 Second Brain/date wiring, TM6 Commons packaging. **Verified**: core 5/5 seen RED first (the `draftTaskCreate` assertion failed against the old payload before wiring), api 2/2, turbo typecheck+test:coverage+build 70/72 with the two non-green being a `@bridge/db` `spawnSync ETIMEDOUT` under load that passes in 637ms in isolation; no schema or migration touched. TASK-021 stays `done` for its original certified scope, with this slice and the residual gaps recorded as evidence on the row. | APPLIED | user · 2026-08-08 | (this commit) |
| AP-115 | 2026-08-07 | user directive | Reverse the manual production-deploy gate (render.yaml `autoDeploy`); add Supabase migration automation to CI (ADR-195) | User asked to make the service auto-deploy from Supabase and Render. `render.yaml` `autoDeploy: false` on both hosted services was a deliberate manual-review gate — recorded in AP-101 and TASK-006's Dependencies field as "a MANUAL production deploy... merging main does not publish", protecting the free-tier hosted pilot from an unreviewed merge going live. There was also no Supabase/Drizzle migration automation anywhere in CI; migrations were always applied by hand. Shown this tradeoff directly (two clarifying questions: whether to reverse the manual-deploy gate at all, and what "auto-deploy from Supabase" should mean given no automation existed), the user chose: (a) flip `autoDeploy: true` on BOTH `bridge-pilot-api` and `bridge-pilot-web` (not the web-only lower-blast-radius option also offered), and (b) add a CI job that auto-applies pending Drizzle migrations against the hosted Supabase project on push to main. Implemented: `render.yaml` both services `autoDeploy: true`; new `.github/workflows/ci.yml` job `supabase-migrate` (push-to-main only, gated on the `platform` job passing first, runs `pnpm migrate` against `MIGRATION_DATABASE_URL` — the existing owner/session-pooler credential, distinct from the API container's least-privilege `bridge_app` `DATABASE_URL`); TASK-006's stale Dependencies field corrected. **Residual risk recorded, not silently fixed**: Render's git-triggered deploy fires independent of GitHub Actions' outcome, so the new CI job orders against Render's build only in practice (seconds vs. minutes), not transactionally — a hard guarantee would need Render's Deploy API triggered from a post-migration CI step instead, which was not built (bigger lift than asked; ADR-195 records it as a rejected alternative, not a solved problem). `MIGRATION_DATABASE_URL` must be added to the repo's GitHub Actions secrets by the user directly — no credential was supplied in chat or written to any file. RENUMBERED from a local AP-102, which collided with TWO existing AP-102 rows already on main (a table-renderer decision and a regeneration-test decision, themselves an unresolved prior collision) — this session's row is the later arrival. | APPLIED | user · 2026-08-07 | (this commit) |
| AP-114 | 2026-08-06 | user directive | External agent access EA0–EA5 added to the roadmap as phase ADD-ONs (ADR-194); new `docs/raw/external-agent-access-plan-2026-08.md` + wiki companion `docs/wiki/external-agents.md`; `docs/wiki/roadmap.md` gains an ingest section; `docs/wiki/index.md` gains a pointer | User directive, verbatim: "Dont build anything, but add it to future roadmap plan and also note to refer and learn from avilo advisory implementation" — following a question about what architectural changes would let Claude Code or any external AI agent build Bridge Modules/Agents/Skills, at maximum and at optimal access, and how that compares to [runvendo/vendo](https://github.com/runvendo/vendo). **What this row approves:** roadmap rows EA0–EA5 as ADD-ONs to existing phases, explicitly **NOT a sequencer reorder** and explicitly **PLAN ONLY** — no code, and **no TASK rows created** (the roadmap scopes work; TASKS is the execution queue; rows follow a pull-forward through this ledger). EA1 import-graph isolation test → P0/security track · EA0 local stdio gateway → P1 · EA2 qualified restore → P1–P2 · EA4 shrink-the-code-bearing-surface → P3 (folds into BA0/BA1, adds no slice) · EA3 networked gateway → P3+ conditional · EA5 agent-legible machinery → P2 with the ecosystem/public-spec push deferred to P6. **The plan as recorded is the plan as CORRECTED by a sibling implementation**, not as first drafted: Avilo Advisory had already built substantially this design against an earlier draft and shipped it, and four of its corrections are adopted — transport determines the security requirement (stdio needs no token, so the H1 auth fix is un-coupled from this feature, **not cancelled**, and remains a blocker for the product and for EA3) · isolation belongs in the transitive import graph rather than a per-tool helper, including the vacuity guard that fails when the walker resolves too few files · the restore/propose asymmetry (Bridge had the append-only substrate and was missing only the permission conclusion) · the iframe-sandbox recommendation was overbuilt and is replaced by making the View/Database/Page/layout schema structurally incapable of holding code, with the BA0 sandbox kept strictly for capability bodies that execute. **Where Bridge deliberately diverges from that implementation** (recorded so it is not re-litigated): the taint lattice stays, because a lattice earns its cost when several differently-trusted sources flow into each other and Bridge has email/capture/scrape/Commons/MCP/external-agents where Avilo has one door; the loopback-API route is mandatory rather than merely preferred, because Avilo opened the same store file as its app and recorded that honestly as a code-level not OS-level guarantee, and Bridge already has the API and authority plane; and unattended restore carries a Bridge qualifier — references must still resolve identically and no referenced capability's trust band or credential grant may have moved, else it degrades to a proposal — because restore restores the document, not the world. **Vendo demoted** from proposed import (its MCP door + sandbox adapters) to pattern source only, a sixth alongside bolt.diy/Dyad/Budibase/Appsmith/ToolJet; **no reuse intake required** unless a specific import is later proposed, and its guard must never run as a second governance plane beside Bridge's authority plane. **Recorded as unverified, blocking:** whether a configuration version ledger equivalent exists in Bridge today — propose-only ships without it, EA0's list/get/restore tools do not. **Verified:** documentation-only change set; no code touched, no tests run because none apply. | APPLIED | user · 2026-08-06 | (this change set) |
| AP-139 | 2026-08-06 | user directive | Files Section becomes a file explorer (ADR-219, renumbered 2026-08-10 from the stranded branch's ADR-195); `docs/wiki/decisions.md` gains a one-liner. `ui-architecture-rules` §6a is UPHELD, not amended | User directive, verbatim: "I want the file explorer Ui to resemble file explorer." **What this approves:** (a) `ModuleFilesSection` gains breadcrumb folder navigation, an Icons (tile grid) view and a Details view with sortable Name/Size/Date-modified columns, filter-as-you-type, selection, and a status bar; (b) the folder tree is DERIVED from path separators the server already sends — no backend change, since `modules.files` already returns `size` and `modifiedAt`; (c) the derivation moves to a pure `file-explorer-model.ts` with 11 tests. **Explicitly upheld, not changed:** `docs/raw/ui-architecture-rules-2026-07.md` §6a — the zero state stays one line of text with "no folder icon grid"; the grid is the populated view only. **Recorded limit:** folders containing no File beneath them cannot be shown (the server returns files only, and `items` feeds `indexModuleFile`); adding directory entries is a separate change. **Noted for the record:** the screenshot attached to the directive is the native macOS Open panel, which Bridge does not own and cannot restyle. | APPLIED | user · 2026-08-06 | (this branch) |
| AP-138 | 2026-08-06 | user directive | Table renderer reversal — ADR-218 (renumbered 2026-08-10 from the stranded branch's ADR-194) supersedes the renderer choice in ADR-160/182/192/193; `docs/wiki/decisions.md`, `docs/wiki/stack.md`, `docs/raw/STACK.md` updated | User directive, verbatim: "I had asked for the UI of table with dashboard to be replica of avilo, why isnt it the case? Is it because I'm using glide? As I said, my priority is UI over others. I thought you'd figure out how to deliver on both fronts?" — then, choosing between options: "I agree with 1", where option 1 was replacing Glide with a DOM table plus virtualization. **What this approves:** (a) `@glideapps/glide-data-grid` is REMOVED and `GlideTableView.tsx` + `grid-theme.ts` deleted; `TableView` becomes a single DOM `<table>` styled with CSS; (b) row windowing above 100 rows via `@tanstack/react-virtual` (already a dependency — no new package) using spacer rows, so the semantic table, sticky header/footer and `aria-sort` survive; the threshold switches ONLY windowing, never markup or features, so the ADR-160 drift defect cannot recur; (c) the aggregate footer and right-aligned numerics — recorded in ADR-182 as permanently open on canvas — are restored, and the rich `renderCell` glyphs become reachable again; (d) two new palette tokens (`--color-line-soft`, `--color-row-hover`) promoted out of the deleted canvas theme with dark-mode values; (e) geometry declared in pixels rather than Tailwind rem utilities, because `html { font-size: 17px }` inflates every rem 6.25% and the row height is a contract the windowing estimate must match. **Reverses:** the renderer choice only. ADR-182's palette decision (Bridge tokens, never Avilo's hexes) is KEPT and is what preserves dark mode. **Deliberately not done:** no change to the view grammar, the registry, `DataViews`' ADR-193 height contract, or any governed write path; inline editing routes through the existing `onUpdate` pipeline, not a new one. **Reported, not fixed:** `StandardColumnMenuPanel` still paints `bg-white` (a dark-mode leak predating this change). | APPLIED | user · 2026-08-06 | (this branch) |
| AP-113 | 2026-08-05 | user directive | Left-rail + header/title-bar shell polish: shell-boundary shadows, hover-only resize handles, one shared macOS titlebar strip fixing the rail/main/chat header misalignment (ADR-187; amends `docs/wiki/ui-architecture.md` §Shell with a new §Shell boundaries) | User directive (relayed via a screenshot described in text): a native macOS window showed the left rail's own header (avatar + org name + collapse icon) landing visibly lower than the main-content page header and the chat-panel header — a "two underlines" artifact — plus asked for (1) borders→soft shadows between shell regions, (2) resize/collapse handles that only reveal on hover (keyboard focus preserved), (3) the workspace name next to the native traffic-light buttons if reachable without native/Rust changes, else a font-size fallback, and (4) the three header rows locked to one shared height so they cannot drift apart again. **Root cause, investigated not assumed:** `DesktopWindowChrome`'s `h-8` traffic-light spacer was stacked ABOVE the rail's `h-14` header ONLY — all three headers were already `h-14` (56px), so the bug was a per-column y-origin drift, not a height mismatch. **What this row approves:** (a) `DesktopWindowChrome.tsx` rewritten as `DesktopTitlebar` — one full-width `data-tauri-drag-region` strip mounted in `Layout.tsx` ABOVE all three shell columns (not inside the rail alone), so the rail/main-content/chat-panel headers structurally cannot drift apart again; (b) `title_bar_style: Overlay` was already set in `tauri.conf.json` (verified, no native change made), so the workspace name renders inline in that strip past a reserved 78px traffic-light gutter — the preferred outcome, not the fallback — while the rail's own interactive org-switcher stays untouched below it; (c) `globals.css` gains `--shadow-shell-right`/`--shadow-shell-left` (grepped first, confirmed no `--shadow-*` tokens existed before this) and `Layout.tsx`'s `<nav>` / `AgentPanel.tsx`'s `<aside>` swap `border-r`/`border-l` for them on the two shell seams only, leaving internal per-panel header `border-b` dividers unchanged; (d) `PanelControl.tsx`'s `ResizeHandle` arrow/hairline go `opacity-0` at rest, revealed via `group-hover`/`group-focus-within` (never unmounted) with a new `isDragging` prop forcing visibility for the whole drag so fast pointer movement can't flicker it away; keyboard reachability preserved because the separator itself is `tabIndex={0}` and is the `group`. **Deliberately not done:** no change to `tauri.conf.json` or Rust window-chrome code (none was needed — the overlay title bar was already configured); no change to `ChatView.tsx`'s composer internals (out of scope, a concurrent workstream's territory — see ADR-186/AP-108's composer redesign, reconciled by staying out of that file entirely). **Verified:** `npx tsc --noEmit` clean in `apps/web`. Live-measured via the Browser preview (`127.0.0.1:5173`, dev server already running) with `getBoundingClientRect()`: all three `h-14` headers report identical `{top, bottom}` — `{0, 59.5}` in plain browser mode and `{32, 91.5}` with `window.__BRIDGE_DESKTOP_PLATFORM__` forced to `"macos"` via a same-document client-side route change (a full reload would have reset the flag). Confirmed the shell-boundary `box-shadow` renders as a soft seam (not a hard border) in both light and forced-dark mode; confirmed the resize-handle arrow is `opacity: 0` at rest and `opacity: 1` on real pointer hover (Browser tool `hover` action) and on `.focus()` (`:focus-within` match); checked both collapsed and expanded rail states. **Not verified — needs the user's own eyes:** the real native traffic-light buttons and their exact pixel geometry only exist in a running bundled `.app` (none exists in this worktree, the same limitation ADR-184/AP-107 already recorded), so the reserved-78px-gutter layout was verified structurally but not against the actual AppKit-drawn buttons — confirm the workspace-name label doesn't crowd or overlap them on the real desktop app. | APPLIED | user · 2026-08-05 | (this change set) |
| AP-112 | 2026-08-05 | user directive | Chat stops forcing local-model-only: `chat.model.status` reports Cloud (Groq) availability with an ADR-181 restart-required distinction, and the composer's model choice becomes real (ADR-190; extends ADR-181/AP-104; reconciles with the concurrent composer redesign in ADR-186/AP-108) | User directive, verbatim: "instead of downloading local model, provide an option for user to choose model including the groq model from API." **Investigation finding:** the gap was NOT missing plumbing — a Chat thread's `plane: "local" \| "cloud"` already existed, `resolveChatModel` already picked a registered Cloud provider for `plane: "cloud"`, and `chat.turn.send`/`chat.turn.prepareCloud` already ran a full exact-context-consent Cloud send. The only real gap: nothing in the UI ever created a Cloud thread or told the user Cloud was an option, and `chat.model.status` could not distinguish "no Groq key saved" from "a key IS saved (Settings → API Keys, ADR-181) but this process's model-router snapshot predates it, restart needed." **What this row approves:** (a) `chat.model.status` gains `cloud.configured` / `cloud.restartRequired`, computed by reusing `ModelProviderKeyStore.list()`'s own `configured`/`active` fields (ADR-181) rather than re-deriving them; (b) `ModelSetup`'s copy and the empty-thread message in `ChatView.tsx` now offer BOTH paths honestly — set up local, or (if Cloud is available) switch via the model menu a concurrent composer-redesign workstream (ADR-186/AP-108) added, or (if a key is saved but inactive) restart Bridge, or (if nothing is configured) a direct `Link` to `/settings?section=api`; that composer `<select>` itself is NOT this row's work, reconciled in place rather than duplicated. **Deliberately not done:** no live model-router reload on key save (ADR-181/AP-104 already declined that increment), no second "which providers exist" endpoint (`chat.model.status` already carries `cloud` and is the endpoint `useChat` already polls), no mid-thread plane switch (plane stays fixed at thread creation per the existing server contract; switching models starts a new Chat). **Verified:** `apps/api` `tsc --noEmit` clean; `chat.test.ts` 19/19 including three new assertions for `chat.model.status`'s not-configured / configured-but-restart-required / active-Cloud cases against `ModelProviderKeyStore` via `InMemorySourceCredentialVault` (never the real OS keyring); `public-cloud-boundary.test.ts` 2/2 unchanged; `apps/web` `tsc --noEmit` clean; `apps/web/test/chat.test.mjs` 5/5 unchanged; a dev Vite server already running at `127.0.0.1:5173` in this worktree loaded the panel with the model select rendering and no new console errors beyond the worktree's pre-existing, previously-recorded `Failed to fetch` (the tRPC API is not reachable from this Vite-only preview). **Not verified:** the actual Cloud send round-trip and the rendered restart-required/not-configured composer states — both need a live API process with a saved/registered Groq provider, unavailable in this worktree's dev preview. | APPLIED | user · 2026-08-05 | (this change set) |
| AP-108 | 2026-08-05 | user directive | Chat composer redesign — rounded row with attachment/model/mic/send (ADR-186); `docs/wiki/ui-architecture.md` gains a composer entry; `docs/wiki/decisions.md` gains a one-liner | User directive, verbatim: "Keep the right hand AI chat bar UI similar to claude code UI with an option to add attachment, choose model, a voice icon for voice input and a miniature arrow acting as send button." **What this approves:** (a) `ChatView.tsx`'s composer becomes one rounded bordered container (textarea + a control row: paperclip · model pill · mic · circular arrow send) instead of a bare textarea beside a rectangular Send button, sized by the existing `compact` prop so the same JSX serves both the full right-hand panel and the avatar overlay's smaller panel; (b) the paperclip ships honestly disabled with a tooltip explaining no upload pipeline exists (AP-021) rather than as a fake no-op; (c) the model pill is a `<select>` that starts a fresh Chat on the chosen plane via the existing `chat.newChat(plane)` call (thread `plane` is fixed at creation server-side, so there is no live per-message swap to offer), and — reconciled with a concurrent workstream's `chat.model.status` change (ADR-181/AP-104) — its disabled Cloud option explains "restart to activate" vs "add a key in Settings" instead of being unexplained; (d) the mic button wires to `companion_transcribe` (Groq Whisper STT), the SAME app-wide Tauri command `avatar/CompanionAsk.tsx`'s push-to-talk already calls, gated on `window.__TAURI_INTERNALS__` so a plain-browser render (the common `apps/web` case outside Tauri) shows it honestly disabled ("Voice input is available in the Bridge desktop app") instead of faking capture; dictation fills the composer's draft for human review rather than auto-sending, since a Chat turn can trigger a governed Task proposal (ADR-183/AP-105). **Deliberately not done:** no new upload endpoint, no live per-message model swap, no change to send/task-creation/threading logic — visual/interaction shell only; voice-capture code is duplicated from `CompanionAsk` rather than extracted to a shared hook this pass. **Verified:** `tsc --noEmit` clean in `apps/web` and `apps/api` (after reconciling with the concurrent `chat.model.status` workstream landing in the same files during this change); live browser check against the Vite dev server confirmed all four controls render with correct `aria-label`s and stay within the panel's bounds at desktop width in both light and forced-dark, and that the attachment/mic buttons report `disabled: true` with the honest tooltip text outside Tauri (the worktree's tRPC API itself is unreachable — "Failed to fetch" — a known environment limitation, not a composer defect). **Not verified:** the `compact` avatar-overlay path live — that surface is a Tauri-only window with no reachable route in this browser preview, so it is verified by type-check and code review only, not watched on screen. | APPLIED | user · 2026-08-05 | (this change set) |
| AP-107 | 2026-08-05 | user directive | Onboarding asks for macOS Accessibility permission via an explicit, skippable button (ADR-185); `docs/wiki/decisions.md` gains a one-liner | User directive, verbatim: "The app should ask for accessibility permission while onboarding." **What this approves:** (a) a new Rust command `providers/accessibility::ax_request_permission` calling `AXIsProcessTrustedWithOptions` with `kAXTrustedCheckOptionPrompt` — the only supported way to raise macOS's own grant dialog, hand-bound via a narrowly-scoped one-call CoreFoundation dictionary (built, passed, released within the function, never held); (b) a "Grant Accessibility" button added to onboarding's EXISTING "trust" step (which already displayed the read-only grant state), fired only on click, never on mount; (c) copy that is honest about current scope: Accessibility gates NO shipped capability today, it is groundwork for one planned, optional feature (Fn-key global-summon customization, per ADR-184) that is not built yet, and the row stays fully skippable — onboarding's Continue never required a grant and still doesn't. **Blocker investigated and reported rather than papered over, per the brief:** the running desktop binary (`target/debug/bridge-desktop`) is a raw `cargo build` executable with NO `.app` bundle (`find` under `target/` returns no `.app`; `tauri.conf.json` has `bundle.active: true` and identifier `ai.bridge.desktop` but no evidence a `tauri build` has run in this worktree). macOS's TCC ties an Accessibility grant to a bundled app identity, so a grant obtained from today's raw binary is not reliably durable across the next `cargo build` — the same unbundled-binary limitation ADR-184 already recorded for Fn-key CGEventTap work and for computer-use screenshot verification. The button ships anyway because it reports a real, live-polled Y/N state rather than a fabricated one, and the exact same command becomes durable with zero code changes once the app is bundled (a packaging gap, not a logic gap) — this caveat is written into both the ADR and the row copy's framing, not hidden. **Verified:** `cargo build` clean, `cargo test accessibility` 1/1 (no test added for the prompt command itself — it triggers a real OS dialog, unsafe to run unattended), `tsc --noEmit` clean in `apps/web`, the existing 15-assertion `onboarding-learning.test.mjs` suite unchanged and green (this lives in the "trust" step, outside the five-question set that suite pins), and a fresh boot of the debug binary logged `api sidecar healthy` plus 15s of `200`-status traffic with zero `Error`/`panic` lines before being stopped cleanly. **Not verified:** the rendered onboarding screen and the live click → OS-dialog → poll-detects-grant round trip — no `.app` bundle exists for computer-use to attach to and screenshot the native window; needs the user's own eyes. | APPLIED | user · 2026-08-05 | (this change set) |
| AP-106 | 2026-08-05 | user directive | Zazoo's home moves to the MacBook notch; the notch-home performance contract (ADR-184) amends roadmap Z1's "peek HEAD-ONLY to the LEFT of the notch" and `docs/wiki/desktop-companion.md` gains the concealed-at-rest behaviour | User directive, verbatim in substance: Zazoo should have its home in the notch — hover it and Zazoo slides out on a bed, sits and meditates; click and it opens its eyes and the notch expands with a chat bar; dragged out it jumps from the notch to the bottom of the screen, compresses and expands and lands realistically, defaulting to meditation, with hover opening its eyes and a chat bar appearing above it, persisting on click. **Measured, not assumed:** a live `NSScreen` probe reports a 179x32 cutout at x=646 on a 1470x956 panel (Mac14,2), and the cutout has NO display behind it — so nothing may be drawn inside it, and the "expanding notch" is a black panel flush with the display top. **What this row approves:** (a) superseding Z1's head-only-peek detail with the slide-out-on-a-bed performance plus composer and drop; (b) the companion window being CONCEALED at rest in the notch home, woken by a Rust cursor poll rather than a hover target — which means nothing on screen advertises the companion's existence, a discoverability gap recorded in the ADR rather than designed away; (c) raising the docked panel above the menu bar (level 25). **Deliberately NOT done, with the blocker measured rather than guessed:** Fn-key shortcut customization is unimplemented — a consuming CGEventTap (needed to suppress the OS's own 🌐 action) was probed and returned DENIED without Accessibility, observation needs Input Monitoring, and the current raw `cargo build` binary has no `.app` bundle to hold either grant. Verified live: Rust reports the same cutout the Swift probe measured; a scripted cursor sweep drives hover true/false with correct top-left conversion and the 48pt pad (x=600 registers though the cutout starts at 646); the present/conceal count moves 0 -> 1 on notch entry; 6 drop-physics assertions and 4 Rust hot-zone assertions pass; `tsc --noEmit` clean. **Not verified:** the rendered bed, the eye-open, the composer and the fall are unwatched — the native panel cannot be screenshotted from here (no `.app` bundle for computer-use to attach to), so the choreography needs the user's eyes. Multi-display and the Z1 idle-CPU criterion are also unmeasured. | APPLIED | user · 2026-08-05 | (this change set) |
| AP-105 | 2026-08-05 | user directive | Chat -> Task node semantics: one Task node per Chat thread with appending follow-ups, plus a suggested-parent step before a new node is created (ADR-183; `docs/wiki/decisions.md` gains the one-liner; extends the TASK-026 governed Chat lifecycle) | User directive, verbatim: "Each time the user inputs, a new task node is created. Continue follow up conversations in same node. Also before adding a task node into task manager, see if this is a child node to any existing task nodes and map it accordingly." **What this approves:** (a) a Chat thread now owns exactly ONE Task node — a later `create_task` turn in the same thread is staged as `mode: "append"` against that node (one extra non-northStar Outcome, idempotent per turn) instead of a sibling Task, which CHANGES the behaviour TASK-026 shipped and required its lifecycle test to move each branch onto its own thread; (b) a new node carries a deterministic parent SUGGESTION (term overlap over open owner-visible Tasks + same-thread lineage) shown in the review card with the terms that produced it and up to five alternatives; (c) the `task-manager.create-task` input/output contracts gain `mode`, `parentTaskId`, `parentRationale`, `parentCandidates` (all defaulted, so proposals staged before this still parse); (d) the `docs/wiki/decisions.md` one-liner. **Governance kept intact:** nothing is parented or appended without a Human decision — a parent kept/cleared/swapped in the card travels as `edit`, never as a bare `approve`; the append target is re-verified against the thread's own accepted Task nodes inside `finishChatTaskDecision`, the single mutation choke point both the decide route and the thread-reload reconciler pass through. **Deliberately not done:** no LLM in the matcher (it would guess when no provider is configured; the deterministic matcher says "no parent suggested"), no silent auto-parenting (AP-021), no schema migration and no new table — the thread->Task reference is the already-deterministic proposal id plus its inputs. **Verified:** core `chat-task-planning.test.ts` 8/8, api `chat.test.ts` 18/18 including a new follow-up/append + parent-suggestion e2e, `wiring`/`router-decide`/`task-manager-manifest-normalization` and db `task-manager-store` green, `tsc --noEmit` clean in `apps/api` and `apps/web`. **Not verified:** no browser check of the new card controls (:5173 is held by another workstream). | APPLIED | user · 2026-08-05 | (this change set) |
| AP-104 | 2026-08-05 | user directive | Settings → API Keys becomes real: model-provider secrets stored in the existing Local Plane credential vault (ADR-181); `docs/wiki/decisions.md` gains a residency one-liner | User directive, verbatim: "I should ideally be able to add API [keys] in the API section of settings." Before this, the only path for a Groq key was the `GROQ_API_KEY` environment variable read once at API boot, and no `.env` file exists in the repo — so a fresh desktop install had no user-reachable way to configure a cloud model at all, while Settings → API Keys rendered the honest "key management doesn't exist yet" empty state. **Investigation first, per the brief:** a governed secret mechanism ALREADY exists — `SourceCredentialVault` with two approved adapters (`KeyringSourceCredentialVault` over the OS keyring, `EncryptedFileSourceCredentialVault` AES-256-GCM with key rotation and `0600` files), gated by `BRIDGE_DEALPILOT_CREDENTIAL_VAULT`, which fails closed unless it names an approved provider and is swapped for an always-throwing vault in public-cloud mode. No new secret store was built and no crypto was rolled. **What this row approves:** (a) reusing that vault for a NON-DealPilot purpose under scope `sourceId: "model-provider:<providerId>"`, which stretches a DealPilot-named module into shared infrastructure — the abstraction is already generic (`{organizationId, sourceId}`) but the package name is not, and renaming it to a shared package is deferred, not done; (b) the residency one-liner added to the locked `docs/wiki/decisions.md`; (c) recording ADR-181. **Where the secret lands at rest:** macOS Keychain / libsecret / Windows Credential Manager under service `com.bridge.dealpilot`, or `<BRIDGE_LOCAL_DIR>/credential-vault/*.credential` under AES-256-GCM — Local Plane only. Never Postgres, Supabase, `localStorage`, a plain file, a log line, or any tRPC response body; the Local Plane state store holds only the opaque vault *reference* plus a timestamp. Reads report `configured`/`updatedAt`/`fromEnvironment`/`active` and never the value or a mask of it, with a pinning test asserting the key and its last four characters appear nowhere in state or in any `list()` result. Mutations are Human-only (`identity.type === "user"`, matching `SourceCredentialService`) and refuse in public-cloud mode. **Honest limit accepted, not papered over:** `createModelRouter` snapshots its provider map at construction, so a saved key activates at the NEXT boot; the save response carries `activation: "restart_required"` and the Settings row reads "Saved · inactive — restart Bridge to activate" rather than a fabricated "connected" (AP-021). Live re-registration was considered and rejected for this increment — mutating a live kernel provider registry mid-completion is its own decision, and the cost of deferring is one honest sentence. **Also deliberately NOT done:** no `.env` file was created, the user's key was never requested, and only the Groq slot ships because Groq is the only provider `wiring.ts` can construct from a saved key — a slot without matching construction would be the fabricated capability AP-021 forbids. Verified: `tsc --noEmit` clean in `apps/api` and `apps/web`; 4 new assertions pass; `public-cloud-boundary` 2/2 and `dealpilot-durability` 5/5 pass unchanged after the credential-vault hoist above the model router. Not verified: no live run — boot-time registration, a real OS-keyring write, and the rendered section were not exercised against a running API. | APPLIED | user · 2026-08-05 | (this change set) |
| AP-103 | 2026-08-05 | user directive | Left-rail scope + Settings pinning + Control Panel dedupe (ADR-180; amends `docs/wiki/ui-architecture.md` §Shell nav list and §Toolbar "Control Panel ⚙ slot … moves into 3-dots" from AP-011; amends the ADR-152/ADR-154 nav canon) | User directive, verbatim in substance: Settings must sit at the absolute bottom of the left nav with nothing scrollable past it; "Research is an agent and should appear under Agents (or under Automations if it is actually an automation). It does not occupy the side nav bar, which is reserved ONLY for Modules, Settings, Intelligence and Second Brain, alongside profile at the top"; and "Don't we already have control panel elements visible upon scrolling? Why do we have a separate control panel inside the 3-dots menu? Merge any additional feature it has into the scroll-revealed one and drop it from the 3-dots." **Applied**: (a) the rail's scroll region gains `min-h-0` so the `shrink-0` footer holding Second Brain · Intelligence · Settings can no longer be pushed off-screen; (b) the Research rail entry is removed — investigation showed `web-research` is a **`skill`** capability bound to the Relationship Module's **Learning Agent**, not an Agent or Automation of its own, so `/research` is relocated behind that Agent through a new optional, manifest-declared `ModuleAgentBinding.runRoute` (same contract as the existing automation `run_route`), surfaced as a "Runs" action in Intelligence → Agents, the per-Module Intelligence Section, and Module Detail; the route is unchanged; (c) the 3-dots "Control Panel / Module Detail" item is removed from `JobPilotPage`, `DealPilotPage` and `SignalsPage` — it was the menu's ONLY item and pointed at the same route the scroll-revealed Intelligence Section already exposes as "Manage in Module Detail", so the trigger is removed with it rather than left opening an empty menu (actionability rule). Nothing needed merging: `app/dataviews/ControlPanel.tsx` is mounted nowhere and never was (already recorded in `pending-work.generated.json:1193`); it is left untouched, not deleted. **Reported, deliberately NOT acted on**: two remaining rail entries fall outside the stated allowed set — **Home** and the **"+New"** create button. Removing either is a materially larger IA change the directive did not ask for, so they await the user's call. The Organization switcher is read as the permitted "profile at the top"; the mobile bottom tab bar is a separate surface, untouched. **Verification**: `@bridge/core` build + 14 module-manifest assertions green, `@bridge/module-manifests` catalog suite green, `apps/web tsc --noEmit` clean for every file touched here (the residual `SettingsPage` `modelProviderKey` errors are a stale `apps/api` dist against another workstream's in-flight `router.ts`). No runtime/browser verification was possible in this worktree. | APPLIED | user · 2026-08-05 | (this change set) |
| AP-102 | 2026-08-04 | user directive | Table renderer canon — Glide becomes the SOLE `table` renderer; the DOM `TableView` path is retired (ADR-192; reverses the dual-renderer half of ADR-160 and the recorded deviation in AP-093) | User asked why the Task Manager table did not match the expected UI, supplied a reference screenshot, and asked for a standardized table build on Glide with a full-screen table, Files/Intelligence sections below the fold, and table-scroll-then-page-scroll handoff. Research established that `GlideTableView` has been live but unreachable since ADR-160 — `TableView.tsx:45` dispatches to it only past `GLIDE_ROW_THRESHOLD = 400`, and every real page pages at 25–50 rows, so the canvas renderer had **never once rendered in practice**. The two renderers carry disjoint feature sets (Glide has resize/reorder/freeze/inline-edit/virtualization; DOM has red flags and row actions), so the feature set silently changed with result-set size. Asked to choose, the user selected "Glide universal, threshold to 0" and "red-flag parity first". **What this reverses:** AP-093 recorded "the canvas path does NOT paint red-flag glyphs … the DOM path (which carries it) remains the default" as an accepted deviation; ADR-160 rejected "swap TableView wholesale for Glide" on the grounds it would drop `RedFlagControl`, `StandardColumnMenu`, ADR-155 glyphs, the Notion-style empty state, and DOM a11y. This row approves closing that gap rather than living with it: the AP-021 objection is answered by keeping the governed control as REAL DOM — `RedFlagControl`'s popover is already `position: fixed` and viewport-clamped, so it never needed to live inside the cell — with the canvas painting only a non-interactive state indicator. **Sequencing (increment 1 only is in flight):** red-flag canvas parity + container-driven grid height land first; dropping `GLIDE_ROW_THRESHOLD` to 0 and deleting the DOM renderer are a SEPARATE follow-up that must not land until `StandardColumnMenu`, row actions, the ADR-155 empty state and a11y parity are each demonstrated on canvas. **Accepted risks, stated plainly:** making Glide universal makes two known defects load-bearing on every page — the `glide-data-grid@6` → `marked@^4` peer mismatch (pnpm resolved `marked@18.0.5`; `STACK.md:58` "unresolved and now load-bearing") and the open HIGH advisory `brace-expansion` via Glide/Linaria (`BUGS.md:823`, attached TASK-018). Neither is resolved by this row. The eight-kind ViewKind grammar is unchanged — this remains a renderer choice, not a grammar widening. | APPLIED | user · 2026-08-04 | (this commit) |
| AP-101 | 2026-07-31 | user directive | Create TASK-030 "Pending Tests"; close TASK-022 + TASK-027 whose only blocker was a live test; correct three stale dependency fields | User asked why TASK-001/005 were pending (they are not — both `done`, as is TASK-015), noted TASK-006 looked finished, and challenged whether TASK-022 should be Anthropic-bound at all. Investigation confirmed all three points. **(a) Three `Dependencies` fields named already-completed tasks** — TASK-006 on TASK-001, TASK-018 on TASK-005, TASK-019 on TASK-005+TASK-015 — so those rows read as engineering-blocked when their real gates are, respectively, a MANUAL production deploy (deployed source pinned at `163562a`; merging main does not publish), seven HIGH production-dependency advisories, and an unapplied approval (AP-007 still PROPOSED, AP-008 inconsistency unreconciled). Corrected in place; none moves in the queue. **(b) TASK-030 created** to hold every verification blocked only on a live machine, real device, paid credential, or manual deploy: PT-1 Anthropic cache hit, PT-2 local tier separation, PT-3 companion validation sweep, PT-4 `research_locate` live check, PT-5 hosted cold-start evidence, PT-6 cross-platform release checks. It deliberately does NOT absorb build work or approval gates. **(c) TASK-022 and TASK-027 marked `done`** — the only two whose sole remaining blocker was a test. TASK-022's code was already implemented and independently reviewed; the harness is provider-agnostic by construction (`ModelProvider` carries tiers/models/pricing/routingHealth/plane; `MODEL_TIERS` = cheap|default|reasoning; four adapters), and tier routing, Anthropic cache wire-format parsing, and receipt construction are ALL already proven without a credential. Only a real second Anthropic call showing non-zero `cache_read_input_tokens` remained -> PT-1. That one clause cannot move to Ollama: it is an Anthropic-API accounting field, a provider feature behind an agnostic port, not a design gap. TASK-027 was shipped and live-validated (summon, consented capture, marks over a fullscreen app, Whisper push-to-talk); its residual V2/V7-V11 checks are live-machine only -> PT-3. **Explicitly NOT closed**, because a test was not their only blocker: TASK-006 (needs the deploy action first), TASK-018 (dependency advisories are real remediation), TASK-019 (needs your approval, not engineering), TASK-028 (overlay->kernel caller move and Run detail Page are build work), TASK-029 (durable credentials + vendor breadth are build work; rights re-verification is your decision). **Also fixed**: TASK-029 carried `Status: in-progress`, which is not one of the six valid tokens, so the generated task projection had been silently dropping it. Now `in_progress` and visible. All status tokens validated. **TASK-020 left `ready` and NOT closed**: it is unbuilt scope, not a testing gap — no browser extension exists anywhere in `platform/apps/` (api, desktop, web, website), and its Avatar clause wants production assets while current art is the procedural SVG rig. | APPLIED | user · 2026-07-31 | (this change set) |
| AP-102 | 2026-08-04 | user directive | Regeneration-test methodology + TASK-042 addition (ADR-175; renumbered from a colliding AP-099 — see main's AP-099 below, unrelated; `outputs/2026-08-04-regeneration-test-learning-agent.md`) | Run three parallel isolated plan-generation tests (constitution+contracts+mandate+harness / constitution+harness / harness-only) against the Learning Agent, triage divergences on merit (adopt better approaches rather than force-fit), and on success add a task extending the method to Skills, Modules, and Integrations. User directive verbatim: "I agree with plan A but instead of asking the agent to build, I want it to generate the plan which is a low cost proxy to validate if we are on right direction. If successful, add a task to repeat this for skills, capabilities, modules, etc so we have clarity on structure for each of these (if at all a structure is required, I believe a strong boundary conditions with autonomy within it might be better approach). For each divergence, evaluate the diverged approach, if the new approach is better, let's accept it instead of trying to fit it to our model." Constitution canonization NOT covered — stays a future gate. | APPLIED | user · 2026-08-04 | (this commit) |
| AP-103 | 2026-08-04 | user directive | Constitution canonized + 6 regeneration-test ideas incorporated + agent roster settled at 5 (ADR-176; renumbered from a colliding AP-100 — see main's AP-100 below, unrelated; `docs/raw/bridge-constitution-2026-08.md`) | User approved the draft constitution presented in-session ("Constitution approved"), directed incorporation of the six divergent ideas surfaced by ADR-175 ("Incorporate the 6 ideas, if validated to be better"), and settled the roster contradiction ("I guess internal strategist was the 5th agent. So its 5."). Canonizes the 15 invariants (14 approved + invariant 15 inference-sensitivity, promoted from idea 3), records the per-idea validation verdicts in ADR-176, and fixes the "1 of 4" wording in `learning-agent.md`. | APPLIED | user · 2026-08-04 | (this commit) |
| AP-105 | 2026-08-05 | user directive | Sub-module becomes a real manifest relation + collapsible nav; four Module display names set to the owner-declared set (ADR-178; `docs/raw/capability-surface-taxonomy-2026-08.md` §5.2 conflict closed) | User chose between the two options put to them — add a real `parentModule` field plus a collapsible nav renderer, or model sub-modules as ordinary Pages — and directed the first: "add a real parentModule field to the manifest plus a collapsible nav renderer and update it with modules and sub modules". The Module set is the one the user has now stated three times: **1. NetworkManager** (main module = People and Communities toggle; sub-modules = WhatsApp, Gmail, LinkedIn) **2. TaskManager 3. DealManager 4. JobManager**. Canon touched: `ui-architecture.md` rule 1.5 gains an implementation (it previously described behaviour no code provided); four Module display names change, which is copy governed by `glossary.md`. `name` and `route` identifiers are deliberately NOT renamed here — that is a vocabulary migration with its own deletion criteria, and 139 pre-existing violations are already open. **Two sub-modules in the declared structure do not exist and were not fabricated**: Gmail is currently an Integration (`/integrations/google`), not a Module, and LinkedIn has no implementation at all. Only WhatsApp — a real installed Module — nests today. **One consequence the user should know about**: Module Files live at `~/Documents/Bridge/<Organization>/<Module display name>/`, so these renames rename folders holding the owner's own documents. The rename is carried forward once, on both read and write paths, and stands down rather than merging when both folders exist. Verified: 25 turbo tasks green (core, module-manifests, api, web) including 9 new nav-tree tests, 4 new manifest-parse tests, and 3 new API tests; three independent negative controls run (nesting disabled, `parentModule` removed from the catalog, folder adoption disabled) — each failed the intended test before the fix was restored. | APPLIED | user · 2026-08-05 | (this commit) |
| AP-106 | 2026-08-05 | user directive | Capability model corrected + Scheduled Automation gets a runtime + retired-vocabulary allowlist extended (ADR-179, ADR-180; closes taxonomy OPEN #1 and #3) | User corrections, verbatim: "View is not a capability, its an Ui element"; "A module contains Databases (each having its own toggle page), Sub modules (sub page), agents (and skills which are subset of agents), automations and integrations (tools access is associated with either agent or automation)"; "Isnt same database with different columns also a list and not a toggle? Isnt toggle for different related databases?"; "Adopt the code's wider definition for capability. Fix the vocabulary and its violations. Add scheduler for Scheduled automation." Canon touched: `glossary.md` (Capability, View, Database, Page, List), `ui-architecture-rules-2026-07.md` §1 and §2 (the toggle/List rule), `capability-surface-taxonomy-2026-08.md`, `docs/wiki/taxonomy.md`. **A canon contradiction is resolved, not just a preference applied**: §1 said "different columns of the same table → TOGGLE" while §5a said Add page appears "iff the column's source is a database-backed entity/table". §5a is what the code implements; §1 named the symptom and mis-stated the cause. **Vocabulary: 139 → 88 violations.** 11 allowlist entries extend AP-096's already-reviewed LLM tool-calling exemption to the rest of its own data path (store, schema, router, two web surfaces) plus DOM `Element`, the English verb "project", and a NEGATIVE assertion that enforces the gate's own goal; 6 genuine Bridge-owned renames applied (`journey-tool`→`journey-skill`, `journey-workflow`→`journey-automation`, `data-knowledge`→`data-memory-held`, a test name, `KnowledgeLayer`→`MemoryLayer`, `suppressedCrossPlane`→`suppressedByPlaneGate`). **NOT done, and it needs its own row**: the remaining 88 are ~all WhatsApp's "Tools" Page → Skills. That rename touches a user-visible route (`/module/whatsapp/tools`), a `databaseId`, and governed `whatsapp.tool.*` capability ids; ADR-171/AP-096 explicitly declined it once already. It has its own exit test and exceeds the ~30% same-surface allowance, so folding it in here would break the one-task-one-outcome rule. Recommended target is **Skill** (they already carry `kind: "skill"` capabilities), consistent with the user's own statement that tool access is a grant to an Agent or Automation rather than a primitive. Verified: turbo typecheck 44/44; new tests — 11 pure trigger/due-calculation, 6 scheduler-through-real-wiring, migration 0038 applied; negative control run on the anti-storm property (interval check disabled → the intended test failed, then restored). | APPLIED | user · 2026-08-05 | (this commit) |
| AP-104 | 2026-08-04 | user directive | Input pack extended to six layers — Method (M) + Budget envelope (B) + installed-capability registry (ADR-177; renumbered from a colliding AP-101 — see main's AP-101 above, unrelated; `outputs/2026-08-04-people-research-regeneration-method-budget.md`) | User directive verbatim: "One issue I have is I want to see alignment with the execution plan not just architecture. Like research agent should look for opensource repos and follow a certain structure, etc." and "I want each agent to balance scope (mostly storage and memory), cost (mostly tokens) and time (build and runtime)." Both requirements were tested as controlled pack variants against the people-research problem before canonization; the layers are recorded in `docs/raw/bridge-constitution-2026-08.md`. | APPLIED | user · 2026-08-04 | (this commit) |
| AP-100 | 2026-07-29 | agent (user directive) | LA3 Phase 2: `SearchProvider` admission becomes an explicit deployment policy; first Tier-2 credentialed adapter; ADR-157; TASK-027 | User asked whether the earlier Parallel.ai competitor survey (the 178-candidate Tier-1/2/3 audit) was on the roadmap for the Learning/research Agent, how far along it is, and to "include and build" if not. **Finding: it was already on the roadmap and had never been tasked.** `learning-agent-roadmap-2026-07.md` §7 defines a three-phase rollout; Phase 1 shipped under TASK-023/AP-069 (anonymous Parallel Search MCP only); Phase 2 ("add 2-4 proven Tier-2 providers once Tier-1 coverage proves insufficient") had **no TASK row and no code**, and was blocked by two concrete things the survey did not name: the router *hardcoded* `tier === 1 && access === "free_direct"`, and `CredentialBroker` is in-memory so there is no durable place for a key. This row proposes the canon-moving parts. **(a) TASK-027** opened as `in-progress` for Phase 2 — a new TASKS row is a scope change and needs this gate. **(b) ADR-157** recorded: admission policy extracted to `@bridge/core` with `FREE_DIRECT_SEARCH_ADMISSION` as the unchanged default and `FREE_CREDENTIALED_SEARCH_ADMISSION` as opt-in; the router refuses any policy admitting `paid`/`self_hosted` at construction so widening can never become spend; `FreeDirectSearchProviderRouter` renamed to `RightsVerifiedSearchProviderRouter` (no alias) because the old name asserted a policy that is now a parameter; first credentialed adapter targets the **same already-reviewed vendor** to isolate the credentialed-path variable from the new-vendor variable; shared response parsing extracted so the two adapters cannot drift on security-critical citation/URL/taint/truncation checks. **(c) `learning-agent-roadmap-2026-07.md` §7 `rollout.phase_2` and `docs/wiki/learning-agent.md` updated** to record Phase 2 as mechanism-complete for one provider rather than unstarted. **Two gates the user must close — I explicitly did not self-certify either.** (i) **Rights**: I verified the API's technical behaviour and that the terms/privacy/quickstart URLs return 200; that is *not* verification that Parallel's customer terms permit credentialed automated use at Bridge's intended volume. A human must confirm before this is enabled in production. The adapter's `rights.verifiedAt: 2026-07-29` starts the 90-day expiry clock, so it self-disables on 2026-10-27 pending re-review. (ii) **Durable credentials**: the key comes from `PARALLEL_API_KEY` env, not a governed store, because `InMemoryCredentialBroker` has no durable backing. Recorded as open, not faked. **Also NOT built, deliberately**: the remaining Phase-2 breadth (2–4 additional Tier-2 vendors from the 33 surveyed — Exa, Tavily, Brave Search, Linkup et al.). Each needs its own independent rights verification, and shipping four unverified adapters to look thorough is exactly what ADR-141 exists to prevent. **Security note for the user, not a proposal**: the API key was supplied in chat and is therefore in the session transcript; it should be rotated at parallel.ai. It was not written into any repository file. Verified: platform typecheck 40/40; `@bridge/models` 42/42 with 6 new tests (default admission refuses Tier-2; credentialed admission accepts Tier-2 but rejects paid; key sent as header and absent from serialized output; empty key and unknown taint both blocked before network; HTTP 429 → retryable `rate_limited`; anonymous→credentialed failover correctly attributed in `attempts`); all 9 pre-existing Parallel/router tests pass unchanged after the shared-module refactor; live end-to-end against the real `api.parallel.ai/v1beta/search` returned 3 citations with `providerTier: 2`, `providerAccess: free_credentialed`, real `providerRequestId`, `trustOrigin: untrusted_external`, citation taint `trust: untrusted`/`source: web`/`instructionRisk: data`, and no credential in the result. The `@bridge/db` Drizzle-metadata test failure is pre-existing and unrelated (no schema or migration touched). | PROPOSED | — | — |
| AP-099 | 2026-07-29 | agent research (user directive + clarifying answers) | Adopt `docs/harness/primitives.md` as the canonical Engine-primitive reference; record ADR-156; open TASKS rows for the P0 roadmap; correct four canon docs | User asked for an analysis of harness architecture across pi.dev, n8n, OpenClaw, Codex, Claude, Relay.app, Cursor, Devin, opencode, CrewAI, AutoGen and peers plus research publications, and for three deliverables in a new `docs/harness/` folder. Clarifying answers scoped this to the **Bridge Engine as a product harness** (not the `.claude/` dev harness) with **governed-canon** treatment. The three documents, the index, the wiki companion, and the `docs/log.md` entry have **already landed** — this ledger classifies new `docs/raw/`-style drafts and wiki summaries as routine. This row proposes only the parts that actually move canon, none of which have been applied. **(a) Adopt `docs/harness/primitives.md` as the canonical per-primitive reference for the Engine**, so future work cites it rather than re-deriving state from code. **(b) Record ADR-156** — "The Bridge Engine is documented as a harness; its unstated propose→decide→commit advantage and its three unbacked canon claims are made explicit" — capturing: the decision to keep "kernel decides, agent explains" as a *deliberate rejection* of the LLM-as-approver pattern both Codex and Cursor shipped in 2026 (Cursor's own docs disclaim theirs as "not a security boundary"); the decision to state the no-re-execution property of the Action Pipeline as an invariant with a pinning test, since LangGraph's `interrupt()` re-runs nodes from the top and duplicates side effects while Bridge is structurally immune; and the rejected alternatives (add an LLM approver, add a free-text instruction file, open Commons before a sandbox is wired). **(c) Open TASKS rows for the five P0 items**: feed the eval harness (persist `EvalStore` + write `executionSnapshot`, which no code writes and for which no column exists); wire one real `SandboxProvider` (`grep -n "sandbox" wiring.ts` returns nothing; the container adapter throws); read `trust_grants` and persist activation budgets and the Kill Switch (hardcoded `[]` at `router.ts:12951`/`:13423`/`:4213` while the table exists unread, leaving Trusted Status inert); replace the context-overflow **throw** in `boundedConversationHistory` with governed compaction; and resolve the scheduler question. **(d) Correct four canon docs to match verified code**: `docs/wiki/governance-agent.md:5-6` claims Governance is the "sole audited exception to agent-floor approve-DENY" — `pipeline.decide` floor-denies **every** agent unconditionally (`pipeline.ts:643-667`) and the real carve-out is lifecycle-only and outside the ledger, so GA3 as specified is **not shipped** and the code is the safer state; `docs/wiki/rituals.md:20` claims "exec engine = Hatchet+BullMQ" — neither dependency exists anywhere in `platform/`; `docs/wiki/rituals.md:20` claims "explainability = `decision_traces`" — that table has zero readers and zero writers (the web declares a matching interface in `data/governance.ts` but ships **empty** arrays behind real loaders, so this is a missing feature rather than a dummy-data violation; `docs/dummy.md:253` already covers the file, and only its stale "Governance mock data" header needs fixing); `docs/wiki/ontology.md:43` claims the Engine performs "bounded retry, timeout, idempotency, rollback/compensation, circuit-break" — none of it exists in `pipeline.ts` or `automation-executor.ts`. **Canon-claim decision required from the user, not from me**: three glossary primitives currently have no referent — Run is defined as "replayable" with no replay driver, **Scheduled Automation** is defined with no scheduler anywhere in `platform/`, and **Plan**/**Planner** have zero implementation. Each is *build or remove*; leaving them weakens the glossary's guarantee that a term names a durable distinction. Related open architectural question, flagged not decided: whether an Agent gets a tool loop (today every kernel model call is one-shot and `AnthropicProvider` uses tool-calling only as a structured-output constraint) — this is one decision spanning three roadmap items. No code, schema, task-state, or canon-doc edits were made. | PROPOSED | — | — |
| AP-089 | 2026-07-29 | agent proposal | TASK-028 BR1: DEFER bundling a Chromium binary; read pages through the existing Tauri webview first | The user asked what a bundled browser engine buys before approving it. Bundling Chromium adds ~150-200 MB to a ~349 MB bundle, makes every upstream security update Bridge's patching duty, and enlarges the signing/notarization surface that already blocks TASK-018 — for a benefit (byte-identical engine on every machine) the prototype does not yet need. Proposal: BR1 ships page reading WITHOUT a new binary, using the Tauri webview the app already embeds (navigate a hidden window, run the page's own JavaScript, extract visible text), with plain HTTP + HTML-to-text as the cheap path for static pages. Revisit a bundled engine only if real objectives hit pages the webview cannot handle, and record that evidence before spending the megabytes. No approval is required to NOT add a dependency; this row exists so the deferral is auditable and so a later reversal is a visible decision rather than a drift. | PROPOSED | — | — |
| AP-088 | 2026-07-29 | user directive | TASK-028 BR3: grant the background research agent click/type authority on web pages, behind a Proposal gate | The user was shown BR0-BR5 and replied "Im good with BR3", explicitly authorizing the agent to click and type on real pages. This is the AMBER tier of the plan's authority model, not a blanket grant: every click, keystroke, form interaction, or navigation to an origin carrying the user's session emits a **Proposal** and parks the Run until a **Decision**, reusing the existing inline Proposal → Decision → Run → Result surface (TASK-026). The RED tier is untouched and stays refused outright with no proposal path: credentials, payment, purchases, account settings, posting or sending anything, CAPTCHA solving, and any page Privacy Guard flags. The agent runs in its own browser context with no imported cookies by default, so touching a logged-in origin is an explicit, visible escalation. Injection defense (learning-agent-roadmap §3.3) remains load-bearing: page text stays quarantined as `untrusted_external` and can never reach the planner's instruction channel, so a page that *asks* for a click cannot obtain one. See docs/raw/autonomous-browser-research-agent-plan-2026-07.md §3. | APPLIED | user · 2026-07-29 | (pending implementation) |
| AP-087 | 2026-07-28 | user directive + clarifying answers | DealPilot Deals page redesigned as a triage table with real, editable signal columns | User supplied a target mockup and asked for the Deals page to "look like this with the table view … aligned." Most columns mapped to real cloud fields (DEAL/STAGE/REVENUE/EBITDA/ASK-EV; MULTIPLE derived = ask÷EBITDA; SHOWING/TOTAL EV computed), but **FIT, R/Y/G, P0-flags, and a numeric evidence %** had no cloud field (Local-Plane projection only) and the hosted DB held three sparse demo deals. The user chose (via clarifying questions) to **make those real editable columns** and to **keep the three real tabs (Deals/Sources/Theses) restyled** rather than add the mockup's OVERVIEW/WORK/REPORTS/RELATIONSHIPS/PLAYBOOKS (five unbacked; OVERVIEW removed under AP-084). Adds six nullable editable columns to `dealpilot_deals` (migration `0034`: `rag`, `fit_score`, `evidence_score`, `p0_flags`, `thesis_tag`, `source_channel`) wired through `DealRecord`, all three stores, and `createDeal`/`updateDeal`; renders richness via an **opt-in** `@bridge/tables` `ColumnSpec.display` hint (badge/rag/meter/currency/multiple) on the shared `TableView` (no hint = unchanged, so the one-renderer canon holds); adds stat-card + Add Deal chrome to `DealPilotPage`; and expands the demo seed to eight signal-populated deals, idempotently and edit-safe (backfills a legacy row only when it has no signals, never overwriting a human edit). **Refines AP-023 (not a reversal)**: the green/yellow **feedback-flag** ban stands; the R/Y/G column is a user-entered **domain** signal (FIT stays a plain number), and the guard was refined to keep its real invariants. Demo data tracked in `docs/dummy.md`. See ADR-155. Verified: web suite 106/106, dealpilot 93/93 + 9/9 core (new signal round-trip) + 5/5 durability, migration-0034, boundary 2/2, typechecks clean; table screenshotted in a temporary harness then removed. | APPLIED | user · 2026-07-28 | (this change set) |
| AP-086 | 2026-07-28 | user directive + clarifying answers | Intelligence becomes a top-level cross-Module capability inventory (supersedes AP-081's Settings deep-link) | User reported "intelligence is pointing to modules". Confirmed: the left-nav Intelligence entry linked to `/settings?section=intelligence`, whose section rendered **only a list of installed Modules** each linking to `/module/:name` — the capability-inventory "overview" ADR-152/AP-084 had just demoted — while its own subtitle promised Agents, Skills, and Automations that it never rendered. Asked the user; they chose **(a)** show Agents/Automations/Skills and **(b)** give it its own top-level page. Adds route `/intelligence` + new `IntelligencePage`: the cross-Module capability inventory flattened from every installed Module's manifest into four tabs — **Agents · Automations · Skills · Integrations** — with the Module rendered as a provenance badge (still linking to manifest-driven Module Detail), never as the payload. Skills are listed **through their consuming Agent** ("Invoked by: …"), per the canon that only an attributable allowed Agent invokes a Skill. The Settings "Capabilities" section is **deleted** (ten sections → nine) and `?section=intelligence` **redirects** so old deep links keep working; left-nav + mobile menu retarget. Manifest-sourced and read-only (`modules.list`) with honest empty states — no new backend, boundary, or persistence surface. This **supersedes AP-081** ("Intelligence deep-links Settings → Capabilities") and the matching UI-architecture Shell line, and reintroduces a `/intelligence` route that VOCAB6 deleted — but the deleted surface was the prototype marketplace/tools hub on hardcoded fixtures; a test pins that this one is manifest-driven with no `agentsData`/`marketplace`/`AgentDetail` fixtures and that `path: "marketplace"`/`path: "tools"` stay absent. See ADR-154. | APPLIED | user · 2026-07-28 | (this change set) |
| AP-092 | 2026-08-02 | user directive | TASKS.md — new TASK-031 (platform bloat cleanup + table-renderer standardization) | User directive in-session ("Add it as the next task in task list") after the 2026-08-02 eight-agent bloat audit (`outputs/2026-08-02-platform-bloat-audit.md`). Approves queuing TASK-031 as the next task after in-progress TASK-027/028: dead-code sweep (prototype-port leftovers, superseded components, unused deps; keep roadmap-ahead eval/sensors substrate and doc-referenced schema tables), duplication consolidation (orgProcedure guard, migration-test helper, graph-store verb helpers, DealPilot factories, @bridge/credentials, single scopePermits), monolith splits (router.ts/wiring.ts/graph-store.ts), `pipeline.ts:104` taint-sink fix, and an explicit Glide-vs-DataViews ADR. Glide decision itself and schema drops remain separate approval rows when executed. | APPLIED | user · 2026-08-02 | (this commit) |
| AP-093 | 2026-08-02 | user directive | Table renderer canon — Glide reinstated inside the `table` view kind (ADR-160) | User directive in-session: "Execute the plan including reinstating glide". Resolves the TASK-031 open question the audit left to the user. Glide returns as a canvas renderer INSIDE `<DataViews>` (TableView dispatches past 400 visible rows to the new GlideTableView), NOT as a new view kind — the `@bridge/tables` ViewKind grammar is unchanged, so this is a renderer choice, not a grammar widening. ADR-155 cell semantics extracted to the shared `dataviews/cell-format.tsx` consumed by both renderers. Recorded deviation: the canvas path does NOT paint red-flag glyphs — RedFlagControl is a DOM popover with no canvas equivalent, and a non-functional flag-looking mark would violate AP-021; the DOM path (which carries it) remains the default. `docs/raw/STACK.md` + `docs/wiki/stack.md` corrected — both had claimed glide-data-grid was the live grid throughout the two weeks it was orphaned by commit 928d66e. | APPLIED | user · 2026-08-02 | (this commit) |
| AP-094 | 2026-08-02 | session (opus) | Kernel taint-sink widened — every `integration` action is a credential-access sink (ADR-161) | Security correction inside TASK-031. `sinkForRequest` gated integration READS (`action !== "write"`) while tainted integration WRITES — reconnect account, rewrite connection config, rotate credential — resolved to `null` and reached the resource with no sink policy evaluation and no sink trace. Widened to every integration action. The 2026-08-02 audit's proposed fix (swap to `!== "read"`) was REJECTED: `credential_access` is semantically right for reads, so the swap would have closed the write gap by opening a read gap. Widening is strictly fail-safe — no previously-gated path loses its gate. Verified red-then-green via new `packages/core/test/integration-sink.test.ts` (2/3 fail pre-fix); core 485→488. Kernel-safety fix recorded for audit, not a canon reversal. | APPLIED | session · 2026-08-02 | (this commit) |
| AP-095 | 2026-08-03 | user directive | TASKS.md — new TASK-034..TASK-038 from the unfinished-work audit | User directive in-session: "resolve all 12 issues, either by creating new tasks (if smaller run, execute it instead of planning). If test is a blocker, add it to the test focussed task and continue." Approves queuing five tasks from `outputs/2026-08-03-unfinished-work-audit.md`: TASK-038 (plaintext OAuth tokens + the ownerless 2026-07-08 SECURITY M1-M6 multi-tenancy block; RENUMBERED from TASK-033 on 2026-08-04 after main claimed that id for Universal Commons capability archetypes), TASK-034 (production config correctness — OAuth redirect, rate limits, the in-memory-in-both-modes policyParams/evalStore), TASK-035 (unreachable Pages, unmanageable child Runs, unmounted ControlPanel, 52 client-less procedures), TASK-036 (green CI + the emit-only EventBus; designated absorber for test/CI blockers per the directive), TASK-037 (unlanded-work recovery). Smaller items were EXECUTED in the same pass rather than planned: the OAuth localhost redirect, the rate-limit vars, the /research nav link, and the AP-021 disabled-item explanations. Also under this directive: `claude/whatsapp-module-contact-extractor-9cfff3` was pushed to origin (52 commits previously existing only on local disk). | APPLIED | user · 2026-08-03 | (this commit) |

| AP-085 | 2026-07-28 | user directive | Serve the Second Brain full Graph (`graph.full`) through the public-cloud API | User reported "2nd brain is not loading" on the hosted pilot. Root cause: `graph.full` — the cross-Module full-Graph preset `SecondBrainPage` calls (ADR-110, `scope: full`) — was never added to `PUBLIC_CLOUD_PROCEDURES`, so `enforcePublicCloudBoundary` threw `PRECONDITION_FAILED` (412) on every call and the page rendered its honest "Full graph could not load" branch. Same omission class AP-082/AP-083 fixed for the Modules; Second Brain was missed because it is a nav preset, not a Module. Open `graph.full` in the public-cloud allowlist: it composes only `graphStore.listFullGraph` + `moduleStore.list` (`router.ts:11799`), i.e. the same `DrizzleGraphStore`/`DrizzleModuleStore` Cloud-Plane reads already served by `relationship.*` and `modules.list`, and stays `authenticatedProcedure` + pilot-Org guard + `bridge_app` RLS with no Local Plane, credential-vault, or raw-capture access. **`relationship.proposeSignalAction` stays CLOSED** — the Graph's per-node governed Action proposes with `dataScope: "private"` (`router.ts:9172`) and the public shell serves public-scope Actions only; the node Action continues to fail closed and is tracked as a known gap in `docs/BUGS.md`. See ADR-153. User directive: "2nd brain is not loading". | APPLIED | user · 2026-07-28 | (this change set) |
| AP-084 | 2026-07-28 | user directive | Module left-nav lands on the primary data Page, not the `/module/:name` capability inventory | Change the left-nav so clicking an installed Module lands on its **primary data Page** (the first manifest Page — DealPilot→`/dealpilot/deals`, JobPilot→`/jobpilot`, Relationship→`/module/relationship/signals`, Task Manager→`/task-manager`), which renders the sibling-toggle **buttons at the top**, instead of the manifest **capability inventory** at `/module/:name` (Module Detail / the "overview"). New `moduleNavTarget(moduleName)` helper (`@bridge/module-manifests`) derives `{ landing, base }` from each Module's Page routes; `Layout.tsx` links to `landing` and highlights the rail entry for `base` (the shared Page-route prefix) plus the Module's own `/module/:name`. The capability inventory is **not** removed — it stays reachable from every data Page's **Intelligence Section** ("Manage in Module Detail", AP-081) and the 3-dots **Control Panel**. This supersedes the VOCAB6/TASK-001 statement that "each Module links to `/module/:moduleName`" and aligns the rail with the dominant page-anatomy canon (data Page primary; Control Panel in 3-dots). Editability was already in place per AP-082/AP-083 (all four Modules persist to Supabase); this change surfaces those editable data Pages as the landing. See ADR-152. User directive: "The deals section should be the home page with the buttons at the top … same pattern for other modules … make other modules editable db." | APPLIED | user · 2026-07-28 | (this change set) |
| AP-083 | 2026-07-28 | user directive + approved plan | Serve DealPilot in the web app (Cloud-Plane record store) + editable modules + seeded demo data | Give DealPilot a **Cloud-Plane (Supabase) record store** so it works in the public-cloud web app: new `dealpilot_deals/sources/theses/relations` tables (migration `0033`, forced tenant RLS), a `DrizzleDealPilotStore` implementing the `DealPilotStore` record contract, and a public-cloud composite (`cloudRecordsDealPilotStore`) that routes Deal/Source/Thesis **Records** to Supabase while **refusing every capture/credential/Gmail op** with a desktop-only error. Boundary (`deployment-boundary.ts`) opens `dealpilot.module/records/detail/createDeal/createSource/createThesis/updateDeal/updateSource` + `jobpilot.create/transition`; `dealpilot.list/captures/commit/discoverDeals` stay closed, and `createSource`'s credential branch self-refuses in public cloud. Modules made **editable** end-to-end (DealPilot Deal/Source edit via `updateDeal`/`updateSource`; JobPilot stage moves via `transition`; the `DataViews` edit surface). **Dummy data** for the pilot Organization (DealPilot + JobPilot) is seeded idempotently at **boot** on the public-cloud deployment (not a SQL migration — the pilot Org is created at boot, so a seed migration would FK-fail on fresh DBs / collide with test fixtures; tracked in `docs/dummy.md`). **Canon preserved:** Source credentials + raw capture stay Local Plane (the "everything in the cloud" ask is deferred to a separately-governed Phase E with an encrypted cloud vault + server-side capture pipeline + security/privacy review). Every opened path stays `authenticatedProcedure` + pilot-Org guard + `bridge_app` RLS. See ADR-151. User chose "Everything in the cloud" + "Seed migration" + "Ship core first, secrets next" via clarifying questions and approved the plan. | APPLIED | user · 2026-07-28 | (this change set) |
| AP-082 | 2026-07-28 | user directive + approved plan | Serve Cloud-Plane module data through the public-cloud API (relax the fail-closed boundary) | Relax the `public-cloud` fail-closed boundary (`deployment-boundary.ts` `PUBLIC_CLOUD_PROCEDURES`) to also serve the **Cloud-Plane (Supabase/Drizzle)** module surfaces so installed Modules are usable on the hosted web app: **Task Manager** (`list`/`get`/`create`/`transition`/`decideProposal`), **Relationship** (`listPeople`/`createPerson`/`updatePerson`/`listCommunities`/`createCommunity`/`updateCommunity`/`listSignals`/`recordSignalAction`), and **JobPilot** (`list`/`definition`). Every opened path stays `authenticatedProcedure` + pilot-Organization guard + `bridge_app` RLS and resolves only to Drizzle stores (`graphStore`/`taskManager`/`jobpilotStore`), never the Local Plane, Source credential vault, or raw capture. **DealPilot stays fully closed** (its store is `LocalDealPilotStore` + Source credentials + raw bodies — no Cloud-Plane store exists) and now renders an honest "runs on the desktop app" state; Module Files, OAuth/Google, and Local-Plane Chat also stay closed. Also: the `DataViews` view switcher becomes a **dropdown** (same metadata-driven eligibility, no per-Page hardcoded list — compatible with TASK-014). This amends the public-cloud boundary recorded in `outputs/2026-07-21-render-free-deployment.md` (see ADR-150). The user chose "Serve data in the cloud" via a clarifying question and approved the implementation plan. | APPLIED | user · 2026-07-28 | (this change set) |
| AP-081 | 2026-07-27 | user directive | Shell/nav + panel + page-anatomy UX realignment to `7f37e17` | Realign the shell UX to the intended `7f37e17` baseline with five explicit deltas: (1) left-nav shows only Modules under Home with **Task Manager as a default Module**, and moves **Second Brain + Intelligence above Settings** at the bottom (Intelligence deep-links to Settings → Capabilities); (2) never render a "Modules unavailable" state; (3) drop the Second Brain "read-only projections" description banner; (4) standard page anatomy for every non-Home page = always-visible table (Notion-style empty state) → artefacts/Files section → **Intelligence section** with Agents · Automations · Integrations tabs; (5) drop the extend/full-screen panel control, keep a single collapse icon plus a persistent inner-edge double-arrow resize handle on both panels, show no extra icons when collapsed, and expand on empty-space click. This supersedes the UI-architecture Shell rule ("share expand/collapse/**extend** icons") and adds the per-page Intelligence section to page anatomy. | APPLIED | user · 2026-07-27 | (this change set) |
| AP-080 | 2026-07-27 | user directive | TASK-026 closure and main landing | Accept the completed real-desktop, managed-Qwen, shared-thread, governed-Skill, restart, responsive, accessibility, migration, hardening-review, and release-gate evidence; apply TASK-026 `in_progress` → `done`; reconcile its plan/wiki/bug/output/task projections; commit and push the completed change set directly to `main`; then verify the exact Render deployment and production Chat surface. Keep the pre-existing production dependency advisories attached to TASK-018 and do not misreport that unrelated gate as green. The user's explicit “post the changes, merge it into main” directive authorizes the status flip and landing. | APPLIED | user · 2026-07-27 | (this commit) |
| AP-079 | 2026-07-26 | user directive | Merge PR #48 and permit its reviewed project skill bundle | Merge “Add Claude Code skills and project config to repo,” resolve its conflicts against TASK-025, inspect and use relevant skills for TASK-026, then pull latest `main`. This explicitly supersedes ADR-146’s three-project-skill cap for the reviewed 44-skill bundle while keeping CLAUDE.md as sole policy authority, all four plugins disabled, 111 unrelated global skill overrides off, and CI closed to an unreviewed 45th skill or bundle growth beyond 512 KiB. Preserve exact source/provenance and required MIT/Apache notices. | APPLIED | user · 2026-07-26 | `11725b0` |
| AP-078 | 2026-07-26 | user directive + approved plan | Governed persistent Chat Panel and TASK-026 priority | Add TASK-026 as the P0 implementation of the user-reported placeholder-like right Chat Panel. Keep private desktop conversations in the Local Plane; hosted web accepts explicit public-safe turns only; bundle a managed llama.cpp runtime with a provenance-pinned Qwen3 4B first-use download; require fresh consent for each cloud turn; reuse RunContextAssembler, ModelProvider, Agent/Skill resolution, and the Universal Action Pipeline; unify the panel/page/Avatar thread and inline Proposal→Decision→Run→Result lifecycle. The user selected the residency/runtime choices, approved the implementation plan, then explicitly directed pulling latest and starting implementation. | APPLIED | user · 2026-07-26 | (this change set) |
| AP-077 | 2026-07-26 | user directive | Land completed TASK-025 token optimization on main | Commit the completed, reviewed TASK-025/AP-076 change set and push it directly to `main`; exclude the separate uncommitted right-chat-panel audit/output; leave PR #17/#48 state unchanged. The user's explicit “merge and push the changes to main” directive authorizes this landing. | APPLIED | user · 2026-07-26 | (this commit) |
| AP-076 | 2026-07-26 | user directive + approved plan | Tiered token-efficient agent workflow and TASK-025 priority | Optimize total token use in both Copilot CLI and Claude Code. Add a zero-ledger read-only tier and bounded routine tier while preserving full security/canon/production governance; compact the canonical instruction contract; track only minimal Claude skill scoping; add compact task navigation and a context-budget gate; benchmark before/after; do not commit, push, merge, close PRs, or change providers. The user selected the tiered option and approved the implementation plan in this session. | APPLIED | user · 2026-07-26 | (this commit) |
| AP-075 | 2026-07-25 | user directive | Close hosted natural-cold and Supabase Auth-refresh reliability evidence, then land on main | Complete the remaining natural-cold and live Auth-refresh checks; fix defects found in that path; preserve query-only replay and never replay a sent mutation; keep free Render and existing Supabase tiers/data/secrets unchanged; after all changes and evidence are complete, commit and push the result directly to `main`. The user's explicit “Push and merge the changes to main after all the changes are done” directive authorizes landing the completed bounded fix; live certification necessarily deploys that exact source to the existing manually deployed free services. | APPLIED | user · 2026-07-25 | `c0353e7` |
| AP-074 | 2026-07-25 | user directive | Deploy the completed hosted/desktop reliability working tree and certify the live Supabase-connected pilot | Deploy the exact local reliability changes completed under AP-073: first clean stale generated test output and review/validate the deployable source; then commit and publish it to the `manishsbhoopalam8498/relationship-os` mainline reported by both existing free Render services, manually deploy both services, and run read-only live Render/Supabase certification. Preserve the approved free-only public-cloud boundary and existing secrets/data; do not change provider tiers or mutate production data merely to prove health. The user's explicit “Deploy the local changes and then check the live supabase” directive is approval for the necessary commit, push, and deployment. | APPLIED | user · 2026-07-25 | `015716c` |
| AP-073 | 2026-07-24 | user directive | Hosted/desktop reliability boundaries, portable runtime, and durable Onboarding profile | Continue the recovered “Complete p0 queue” reliability pass in the existing dirty checkout: preserve Local Plane data; complete the recorded Onboarding choices; make hosted wake recovery replay reads only; package the desktop API/Node runtime without secrets or repository/system-runtime fallback; keep unsupported Windows installers fail-closed; sign native macOS code from Frameworks; persist Onboarding preferences as private Local Plane Memory; update the canonical reliability decision/evidence; do not deploy, change provider tiers, commit, or push. The current directive to continue that exact prior work is the approval. | APPLIED | user · 2026-07-24 | uncommitted working tree (per user boundary) |
| AP-072 | 2026-07-22 | user directive | TASK-017 remaining runtime/package correctness backlog completion and canonical closure | From `origin/main@14689cf`, complete the remaining TASK-017 backlog in reviewed slices merged to main: D1 lint carve-out, D3 `@bridge/core/server` `node:vm` split, D5 dedicated install ResourceTypes, D6 server-derived Helpdesk topics, D8 durable idempotent `action.reconcileApproved`, D9 sensor-coverage scoping, D10 injection-safe server-side Relationship View filter/sort, D11 broadened taint-test reconciliation (api Relationship test + 14 `@bridge/core` tests); mark stale globals.css / package-store / pins bugs RESOLVED and fix the stale VOCAB5 manifest test. No DB migration required (high-water stays `0030`). Apply TASK-017 `ready` → `done` on code/build/test verification (not a live browser walkthrough). File — not fix — the two pre-existing gaps found during review: payment-blocked GitHub Actions CI, and `capability.approve`/`organization.blueprint.activate` mutating on a `rejected` governed proposal. The user's explicit directive to implement all remaining tasks and merge after each is the approval. | APPLIED | user · 2026-07-22 | `21a4ca3` / `5c5ddbe` / `e70a5f8` / `49a9fff` / `5674b05` |
| AP-071 | 2026-07-21 | user directive | TASK-016 complete database/migration correctness prototype and canonical closure | Start from exact `origin/main@5857cb95d98c53928103df801cfc4ff30a73e8e3`; complete the five remaining database BUGS, preserve the landed Supabase runtime-role/RLS slice and TASK-015 taint schema, allocate `0030` only for a proven runtime schema correction, certify supported DB-package concurrency, apply TASK-016 `ready` → `done`, commit/push/open one focused PR/merge normally/verify `origin/main`, and stop before TASK-017. This task's explicit user directive authorizes the status flip and landing. | APPLIED | user · 2026-07-21 | (this commit) |
| AP-070 | 2026-07-21 | user directive | TASK-015 RT0–RT4 implementation and canonical closure | Complete runtime taint tracking end-to-end from exact `origin/main@2e0cb730ab573c298b804e664106936411e7509f`; allocate migration `0029`; preserve TASK-022 Plane/tier/usage and TASK-023 web-research behavior; prove lattice, propagation, source/sink inventory, quarantine, declassification, migration/RLS, restart replay, red-team, and approval trace UI; apply TASK-015 `ready` → `done` and RT0–RT4 → `done`; close the root BUG only after exact targeted proof; commit, push, focused PR, normal merge, verify `origin/main`, then stop without starting TASK-016. This task's explicit user directive authorizes the status/plan flips and landing. | APPLIED | user · 2026-07-21 | (this commit) |
| AP-069 | 2026-07-21 | user directive | TASK-023 canonical closure | Accept PR #44 source `6e33f051b7e001efe25c949d4038730aee6a1292` / merge `b8e1db0b808806d45dd904270902dd77b132541c` plus the real durable governed Parallel prototype as the TASK-023 exit proof; apply TASK-023 `ready` → `done`; retain Jina/DuckDuckGo rights blocks, GitHub billing-blocked CI wording, and candidate B as superseded but untouched; update closure docs and generated Task projection only; commit, push, merge normally, verify `origin/main`, then stop. This task's explicit user directive authorizes the status flip. | APPLIED | user · 2026-07-21 | (this commit) |
| AP-068 | 2026-07-21 | user directive | TASK-023 current-main reconciliation, governed web research, and normal landing | Preserve candidate A, inspect candidate B read-only, normally merge current `origin/main@5122695d8dbf3a9b74bebd79e89618771126af92`, and reconcile onto current Organization/Module/Event/Result/File, TASK-007, TASK-011, TASK-015, TASK-022, and signed Module contracts. Ship only current-rights-verified anonymous Parallel Search MCP; keep Jina, DuckDuckGo, paid, credentialed, Tier-2, Tier-3, and self-hosted providers unregistered. Require bounded requests, shared net-guard, ContentGuard quarantine, Agent/Goal/Task/Module authority, citations/provenance, and `untrusted_external` Result/Memory/Event evidence. Run focused checks, a bounded live smoke, and one changed-scope security review; commit, push, open and normally merge the focused PR; leave TASK-023 status/order unchanged. This task's explicit user directive is approval. | APPLIED | user · 2026-07-21 | (this merge) |
| AP-067 | 2026-07-21 | user directive | TASK-022 recovered implementation milestone, external live-cache blocker, and main landing | Preserve the paused dirty inference-cost work, normally merge current `origin/main`, reconcile current Organization/Module/Commons contracts, finish typed tiers/cache usage/cost receipts and Local Plane fail-closed behavior, fix bounded review findings, and land through a focused PR. Apply TASK-022 `ready` → `blocked` when no already-authorized Anthropic credential and explicit live-spend authorization exist: deterministic protocol/governance evidence may land, but exact closure still requires two sequential real public-safe CoS turns with non-zero second-call cache-read tokens. Do not fabricate evidence or start TASK-023. This task's explicit user directive is approval. | APPLIED | user · 2026-07-21 | (this merge) |
| AP-066 | 2026-07-21 | user directive + external PASS | TASK-021 and Task Manager plan canonical closure | Accept Corporate-training-sims PR #104/evidence `123e72b`/merge `f3443acc3ce34fdce29ed2147fe3d608b861a696` as the required external PASS for Bridge PR #41/merge `0087f89`; apply TASK-021 `in_progress`→`done` and Task Manager plan `active`→`done`; update only canonical closure evidence, BUGS resolution confirmation, wiki/log/handoff/output, and generated Task projection; keep GitHub billing-blocked CI wording honest; commit, push, merge normally, and verify origin/main. This task's explicit user directive authorizes both status flips. | APPLIED | user · 2026-07-21 | (this commit) |
| AP-065 | 2026-07-21 | user directive | TASK-021 Corporate PR #103 Local Plane restart remediation and main landing | Consume Corporate merge `4d6ae1c`; attach and fix four blockers: durable-local Automation Run persistence, durable signed Module/commonsSource persistence, UUID-compatible Module installation identity with explicit legacy mapping, and semantic projection updates preserving unchanged completed timestamps. Run an exact two-instance PGlite certifier, targeted affected checks, and one bounded review; keep TASK-021 `in_progress` pending external PASS; commit, push, merge normally, verify origin/main, and return the recertifier contract. This task's explicit user directive is approval. | APPLIED | user · 2026-07-21 | `689fca0` + (this commit) |
| AP-064 | 2026-07-21 | user directive | TASK-021 external-certification blocker remediation and main landing | Use Corporate-training-sims certification merge `d24e76ba9413e2ca757768af5f61d4f413714a69` as regression evidence; implement durable governed projection reconcile, real completed-bay sweep Automation, and signed root Module Commons install; preserve TASK-004/005 Skill paths, privacy/RLS/provenance, and TASK-021 `in_progress`; run only targeted checks plus one review; commit, push, open/merge the focused PR, verify origin/main, and return the exact recertifier contract. This task's explicit user directive is approval. | APPLIED | user · 2026-07-21 | `a0da415` + (this commit) |
| AP-063 | 2026-07-21 | user directive | Free Render API/static deployment, public-cloud residency boundary, live certification, and main landing | Keep the existing Supabase project as Postgres/Auth in US East; deploy one free Render Docker web service plus one free static site in US East; accept free sleep/wake limits; connect the private repository only through visible provider UI; store secrets only in Render/Supabase; prohibit any Render volume/private Local Plane data or credential path; add an explicit public-cloud fail-closed API boundary; preserve exact pilot JWT/RLS/service-role/CORS controls; certify live URLs and targeted surfaces; keep TASK-006 blocked on the separate authorized Google/Source credential gate; do not close TASK-016/017 without their full prototypes; commit, push, open a focused PR, merge normally, and verify `origin/main`. This task's explicit user directive is approval. | APPLIED | user · 2026-07-21 | (this commit) |
| AP-062 | 2026-07-21 | user directive | TASK-021 TM0–TM6 Bridge implementation milestone, external certification gate, and main landing | Implement Task Manager from exact `origin/main@a47b781` with migration `0027`; land the Bridge repository implementation and self-hosting evidence; keep TASK-021 `in_progress`; replace the historical external certifier with the exact remaining dependency `badami-vikas/Corporate-training-sims`; do not change that repository in this session; commit, push, open a focused PR, merge normally, and verify `origin/main`. The user's explicit directive in this task is the approval. | APPLIED | user · 2026-07-21 | `c708017` + (this commit) |
| AP-061 | 2026-07-21 | user directive | TASK-013 private archive, destructive cleanup, canonical closure, and main landing | Preserve exact pre-cleanup `origin/main@7f37e17e1132ebbbee74f1c612f0e83b211139ef` on a verified branch inside the same PRIVATE repository; remove unopened stale/private legacy trees from the main working tree without rewriting history; migrate root audit docs; converge built-in Module manifests/routes under `platform/modules/`; delete superseded runtime fixture surfaces; reconcile dummy/bug/task/codemap evidence; apply TASK-013 `ready` → `done` only after the exact prototype and affected-neighbour gates pass; commit, push, open a focused PR, merge normally, and verify both refs. The user's explicit directive in this task is the approval. | APPLIED | user · 2026-07-21 | (this commit) |
| AP-060 | 2026-07-21 | user directive | TASK-006 canonical status/evidence correction and main landing | Preserve the landed Supabase/Auth/RLS/Local Plane milestone, record the immediately rotated Keychain-only runtime password without its value, and apply TASK-006 `in_progress` → `blocked` because the remaining exact prototype evidence requires unavailable external inputs: an authorized real Source credential plus configured/authorized Google OAuth for live BizBuySell Deal discovery and credential reveal/copy/revoke/expiry/wrong-Human proof. Commit, push, open a focused PR, merge normally, and do not start another task. This task's explicit user directive is approval. | APPLIED | user · 2026-07-21 | (this commit) |
| AP-059 | 2026-07-20 | user directive | TASK-012 final compatibility deletion, plan completion, status closure, and main landing | Complete the strictly sequential closure milestone from `origin/main@d725691`: atomically rename DealPilot/JobPilot Module package paths; delete completed VOCAB1–VOCAB6 aliases/dual reads; preserve signed Commons bytes only inside an explicit trust/migration boundary; allocate `0024` only for genuine stored Result backfill; repair canonical GraphStore/Onboarding tests; prove recovery/RLS/API/browser evidence; apply TASK-012 `in_progress` → `done` only if every exit criterion passes; commit, push, open the focused PR, and merge normally. This task's explicit user directive is approval. | APPLIED | user · 2026-07-20 | `cd0ad97` / `e139d88` / PR #33 |
| AP-058 | 2026-07-20 | user directive | TASK-012 VOCAB6 residual completion and main landing | Audit current `origin/main@18175b0` rather than rebuilding completed TASK-001/007/009/014 work; finish only residual installation-driven Module shell, manifest Runs, Agent-only Skill invocation, symmetric Panel Control, full Graph Module/Agent/source Actions, retired Tool/Knowledge runtime deletion, exact 375px proof, evidence, focused PR, and normal merge. Keep TASK-012 `in_progress` for the separately named final compatibility deletion and full prototype certification. Migration `0024` is allocated only if a genuine schema/backfill is required. The user's explicit directive in this task is the approval. | APPLIED | user · 2026-07-20 | `e657cc8` / PR #32 |
| AP-057 | 2026-07-20 | user directive | TASK-012 VOCAB5 milestone completion and main landing | Complete VOCAB5 from current `origin/main@611c9ad`: converge Signals, People, Communities, Relations, Interactions, Introductions, Help Request routing, Sources, and Automations under the installed Relationship Module; remove standalone Helpdesk package/routes/client stores after verified migration; retain Signal as participant-linked Event with reason and governed Action; prove desktop and exact 375px routes; lower the ratchet without family/kind growth; keep TASK-012 `in_progress` for VOCAB6 and final compatibility deletion; then commit, push, open a focused PR, and merge normally into `main`. The user's explicit directive in this task is the approval. | APPLIED | user · 2026-07-20 | (this merge) |
| AP-056 | 2026-07-20 | user directive | TASK-012 VOCAB4 milestone completion and main landing | Complete VOCAB4 from current `origin/main@dc50c33`: collapse Touchpoint/Timeline/Signal occurrence storage into typed Events plus participant/evidence Relations; retain Signal only as an Event projection with reason and safe Action; map non-file outputs to Result and durable user-visible content to File; preserve local Files paths while indexing Module writes into canonical `files`/`file_refs`; allocate migration `0023` with fresh/upgrade/no-drift/data-preservation/RLS evidence; lower the ratchet without family/kind growth; keep TASK-012 `in_progress` for VOCAB5–VOCAB6 and final compatibility deletion; then commit, push, open a focused PR, and merge normally into `main`. The user's explicit directive in this task is the approval. | APPLIED | user · 2026-07-20 | (this merge) |
| AP-055 | 2026-07-20 | user directive | TASK-012 VOCAB3 milestone completion and main landing | Complete VOCAB3 from current `origin/main`: preserve verification and content-hash pins for signed pre-VOCAB3 Commons entries while projecting canonical Organization/Module/Record manifests; read the prior registry directory without hiding entries or weakening trust; reconcile moved ratchet fingerprints without baseline growth; run only targeted Commons, Module/Organization/Record API/store/RLS/schema/browser checks; keep TASK-012 `in_progress` for VOCAB4–VOCAB6 and compatibility deletion; then commit, push, open a focused PR, and merge normally into `main`. The user's explicit directive in this task is the approval. | APPLIED | user · 2026-07-20 | (this merge) |
| AP-054 | 2026-07-19 | user directive | Recover stalled Supabase deployment session and integrate it into local `main` | Audit session `0f3e2f14-7fd2-4d07-8f3e-9c86c7c5480a`, document its five stale background workers, preserve completed work, reconcile it normally with newer `origin/main`, port the deployment slice to canonical Organization/Module/Record vocabulary, update TASKS and Progress-from-Manish records, and complete the local merge without pushing or provisioning cloud resources. The user's explicit requests to “merge those and document changes into main” and “expedite the process and merge the changes” are the approval. | APPLIED | user · 2026-07-19 | (this merge) |
| AP-053 | 2026-07-19 | user directive | TASK-024 GitHub Pages publication rollback | Revert the TASK-024 artifact from `badami-vikas/badami-vikas.github.io` and restore the older `zazoo.me` experience without removing the merged website source from `relationship-os`. Preserve the Pages history, custom-domain `CNAME`, and historical auxiliary pages; record the rollback in TASKS, the durable output, log, and Progress-from-Manish handoff files. The user's explicit directive in this turn ("revert the github.io deployment. let it go back to the older experience") is the approval. | APPLIED | user · 2026-07-19 | (this commit) |
| AP-052 | 2026-07-19 | user directive | TASK-024 Zazoo public website completion and main integration | Skip the optional independent code-review pass and merge the already validated TASK-024 implementation into `main`. Apply TASK-024 `ready` → `done` because the ten-scene storyboard contract, approved-copy boundary, pointer/keyboard/touch interactions, reduced-motion behavior, desktop and exact 375×812 layouts, tests, lint, typecheck, and production build passed; normally integrate current `origin/main`, push, and merge without altering the unrelated dirty central checkout. | APPLIED | user · 2026-07-19 | (this commit) |
| AP-051 | 2026-07-19 | user decision | TASK-014 Map privacy boundary and ADR-124 | After the final TASK-014 review found that real People, Communities, JobPilot, and DealPilot Location fields contain place labels, choose “Local/private coordinates only, plus an opt-in geocoder port” over Mapbox cloud or public Nominatim. Apply a local bundled basemap, prohibit automatic public geocoding/tile egress, accept structured/local coordinate values, expose a Human-triggered Local Plane geocoder port, fail honestly when no provider is configured, and keep all persistence on the ordinary Record update path. | APPLIED | user · 2026-07-19 | (this merge) |
| AP-050 | 2026-07-19 | user directive | TASK-014 View Grammar and converged TASK-009 Graph closure | Implement every remaining canonical task sequentially by priority, including in-progress/blocked work, while skipping the separately owned TASK-005 session. Apply TASK-014 and TASK-009 `ready` → `done` only after their exact shared-registry, Calendar-retirement, Tree, authenticated cross-Module Graph, Record navigation, Relation filter, governed Action, privacy, desktop, and 375px tests pass; preserve adjacent legacy/File-index defects as explicit future-task evidence rather than hiding them. | APPLIED | user · 2026-07-19 | (this merge) |
| AP-049 | 2026-07-19 | user directive | TASK-011 (JobPilot culture research, JP3B) completion and closure | The user directed merging the fully validated TASK-011 branch into `main` and updating `docs/TASKS.md`/Progress-from-Manish docs accordingly. Apply TASK-011 `ready` → `done` because the exact Prototype test passed (Learning gathers permitted cited evidence for a real target company; Internal Strategist separates fact/opinion/theme/contradiction/inference; the user sees citations and a rights/access warning before recommendations), 13+ rounds of independent/coordinator security review closed every raised defect (SSRF/redirect/DNS-rebinding hardening, cross-instance CAS, distributed cancellation, lease/recovery, artifact retention/taint/expiry, grounding provenance, and a final central-merge review closing the last 2 blockers — durable child-Run terminal audit repair and full-lineage artifact purge/redaction), and a fresh independent review of the final merge reconciliation found no defects. No schema migration was added by this task. | APPLIED | user · 2026-07-19 | (this merge) |
| AP-048 | 2026-07-19 | user directive | TASKS.md - new TASK-024 (Zazoo public website cinematic implementation) | Add one canonical website implementation task at the end of the execution queue, governed by the completed storyboard specification. Its exact prototype test requires the continuous day-in-the-life film at desktop and 375px, pointer/keyboard/touch and reduced-motion behavior, strict Copy-only visible text, the corrected dictionary with no Origin entry, and rejection of the delivered PDF's static compositions and invented copy. User's explicit instruction in this turn ("Update the website associated task (if none exists create one). Then commit, push and merge") is the approval; no re-rank or DONE transition is implied. Renumbered from AP-044 during integration because current `main` assigned AP-044 through AP-047 to concurrent TASK-006/TASK-010/TASK-005 work. | APPLIED | user · 2026-07-19 | (this merge) |
| AP-047 | 2026-07-18 | user directive | TASK-005 exact combined demo certification and closure | The user directed completion of all P0 work, assigned canonical TASK-005 as P0, required the exact clean desktop and 375px Onboarding→Avatar→Module→Commons→attributable-Agent→provenance→correction/undo sequence, and explicitly confirmed TASK-003 was done and implementation should continue. Apply TASK-005 `blocked`→`done` only after the combined sequence, native desktop launch, responsive/error/vocabulary/dummy-data checks, and affected-neighbour gates pass. The same directive authorizes fixes required by that exit test: exact signed/runtime Skill matching, owner isolation for private proposals, coordinated Organization/Files-root rename, real-Module-only inventory, and physically reachable 375px Settings/Approvals controls. Final review further required the signed Skill to declare its private Signal read, eliminate runtime egress by consuming only an approved local recommendation, remove the visible binding on registry/trust drift, and preserve ambiguous Files-rename intents fail-closed. The post-fix certification and final gates passed, so this row records the current-task directive as the approval context for ADR-121/ADR-122 and the canonical transition. Renumbered from AP-046 during final integration because current `main` assigned AP-046 to TASK-010. | APPLIED | user · 2026-07-18 | `166a01b` |
| AP-046 | 2026-07-19 | user directive | TASK-010 live certification, same-surface repair, and closure | Continue the remaining queue in priority order, including in-progress work. During exact live certification, repair the missing JobPilot default-table Red Flag controls, preserve real persisted Record anchors and governed learning/audit behavior, certify desktop focus/hover plus 375px touch paths, and apply TASK-010 `in_progress` → `done` only after the exact Prototype test passes. | APPLIED | user · 2026-07-19 | (this commit) |
| AP-045 | 2026-07-19 | user directive | TASK-006 validated branch integration | Merge the fully validated and pushed TASK-006 durability/vault branch into `main`. Preserve TASK-006 as `in_progress`; retain the honest live Google/BizBuySell and verified OS/application Human re-authentication blockers; preserve TASK-010 migration `0016`; add no TASK-006 numbered migration. | APPLIED | user · 2026-07-19 | `7f44186` |
| AP-044 | 2026-07-18 | user directive | TASK-006 durable Local Plane and explicit OS credential-vault decision | Replace process-local DealPilot Record/capture/Gmail continuation state with Organization-isolated atomic Local Plane storage; use a maintained approved OS credential-vault adapter with opaque Organization/Source-bound references and fail-closed runtime configuration; preserve governed Agent/Skill, Gmail, rights, membership, re-authentication, audit, spend, dedupe, and two-phase acknowledgement invariants; add restart/concurrency/non-disclosure evidence; obtain only host-supported provider/keychain/re-auth/desktop/375px evidence and never fabricate unavailable evidence; do not flip canonical TASK-006 status. ADR-117 records the implementation decision. The user's explicit implementation directives in this task are the approval. Renumbered from this session's AP-041 because current `main` already used AP-041 through AP-043. | APPLIED | user · 2026-07-18 | `2f85dc7` |
| AP-043 | 2026-07-19 | user directive | TASK-008 validated branch integration | Merge the completed, pushed TASK-008 Relationship hardening branch into `main`. Preserve TASK-008 as `in_progress`, preserve TASK-010 migration `0016` unchanged, and retain the final correctness/security verdict and remaining RM6/evaluation scope. | APPLIED | user · 2026-07-19 | (this merge) |
| AP-042 | 2026-07-18 | user directive | TASK-008 central-review hardening and ADR-116 | Resolve the 16 named Relationship integration blockers before merge: self-only browser delegation; no private PII in canonical tables; owner-protected private Event content; legacy Touchpoint filtering; durable Google/capture effects and identity convergence; capture-to-Event materialization; RFC3339-offset/date-only time normalization; serialized Introduction/Memory transitions; explicit nullable clears; pending-first meeting preparation; Community membership composition; route-safe UI state, local datetime defaults, and bounded context pagination. Keep TASK-008 `in_progress`, add no migration, merge current `origin/main` normally only after validation and independent review, and push without force. This explicit directive approves ADR-116's refinement of ADR-115 and routine TASK/evidence updates, not a DONE transition. Renumbered from AP-041 during integration because current `main` already used AP-041 for TASK-003 certification. | APPLIED | user · 2026-07-18 | `71368fe` |
| AP-041 | 2026-07-18 | user validation | TASK-003 human physical-input certification and closure | After the remaining checklist was stated explicitly, the user confirmed “all these are working fine”: physical pointer drag across displays with quit/relaunch restoration, physical VoiceOver activation of close/minimize/fullscreen controls, and physical external-display detach/reconnect. Apply TASK-003 `blocked`→`done`, resolve its two user-reported bug rows, and retain TASK-005 as `blocked` on its separate combined desktop+375px demo. | APPLIED | user · 2026-07-18 | (this commit) |
| AP-040 | 2026-07-18 | user directive | Recover and finish reviewed TASK-003 work from the failed certification session | Start from current `origin/main`; keep the failed worktree read-only; independently review and port only accepted code, regression tests, and truthful evidence; rerun affected and neighbouring gates; keep TASK-003 `blocked` pending human physical pointer drag/relaunch, physical VoiceOver activation, and literal cable/power detach if the acceptance wording requires it; commit and push the recovery branch without merging `main`. This approves the implementation and routine evidence update, not a false DONE transition. | APPLIED | user · 2026-07-18 | (this commit) |
| AP-039 | 2026-07-17 | user directive | Learning Agent LA3 web-research provider roadmap + TASK-023 (renumbered from this session's original AP-038, which collided with main's parallel-landed AP-038 for the inference-optimization task) | Survey 178 Parallel.ai FindAll candidates (46 matched + 132 unmatched, all reviewed) for relevance to a Learning Agent recon/web-search capability; classify into Tier 1 (free, direct access, no account — Parallel Search MCP, Jina AI keyless, DuckDuckGo Instant Answer), Tier 2 (33 providers, free tier requires signup/API key — Exa, Tavily, Firecrawl, Apify, Browserbase, etc.), Tier 3 (paid or self-hosted-only — Perplexity Sonar, Bright Data, Webz.io, Klue, Contify, SearXNG, Crawl4AI, etc.); discard 112 irrelevant candidates with grouped reasoning (coding agents, inference infra, chat-assistant vendors, dev-tooling frameworks, unrelated verticals, duplicates/rebrands, FindAll generator noise). Add `docs/raw/learning-agent-roadmap-2026-07.md` §7 (provider table + 3-phase rollout: Tier 1 first at $0, then 2-4 proven Tier 2 providers, Tier 3 only behind a cost/ROI + approval gate). Add TASK-023 (Learning Agent governed `web-research` Skill, `SearchProvider` port) to `docs/TASKS.md`, appended at queue end. ADR-111 records the decision. Full itemized survey: `outputs/2026-07-17-learning-agent-recon-search-integrations.md`. User's explicit directive in this turn ("use find all... create a roadmap to provide these capabilities to learning agent and update its task... commit, push and merge to main") = the approval. | APPLIED | user · 2026-07-17 | (this commit) |
| AP-038 | 2026-07-17 | session (sonnet) | TASKS.md — new TASK-022 (inference cost optimization: prompt caching + model tiering) | User directive in-session: "You have my approval. Update the tasks." Approves queuing TASK-022 into `docs/TASKS.md` per the audit in `outputs/2026-07-17-llm-inference-optimization-audit.md` (zero runtime prompt optimization: no `cache_control`, no batching, no tier routing, model selection = `providers[0]`). Appended at the end of the execution-order list — does not reorder the TASK-001→005 prototype gate. | APPLIED | user · 2026-07-17 | (this commit) |
| AP-037 | 2026-07-17 | user directive | Graph view gains cross-Database scope selector; Second Brain collapses into Graph view at full scope (TASK-014 + TASK-009) | Extend Graph view (§3 of `docs/raw/brd-dataengine-views-2026-07.md`) with a scope selector: single_database (default) / multi_database (user-selected) / full (all permitted Databases = Second Brain). Rewrite §4 of the BRD from "Second Brain is not a View kind" to "Second Brain IS the Graph view at full scope." Second Brain nav entry stays as a preset/shortcut to scope:full — not a separate component. Update TASK-009 Outcome/Prototype-test to reflect convergence: building the real Graph renderer with scope-selector support (TASK-014) simultaneously delivers Second Brain. ADR-110 records the reversal of ADR-108's "never merge" stance. User's explicit directive in this turn = the approval. | APPLIED | user · 2026-07-17 | (this commit) |
| AP-036 | 2026-07-17 | user directive | View Grammar BRD + TASK-014 scope update (Calendar/Graph as View kinds, not Modules) | Add `docs/raw/brd-dataengine-views-2026-07.md`: canonical 8-kind View Grammar (table/board/gallery/form/calendar/map/graph/tree), eligibility rules, feature list per kind, Calendar/Integration decoupling rule, Second-Brain-vs-Page-Graph distinction, 2026-07-17 code audit of the duplicate-implementation state. Update TASK-014's Outcome/Prototype-test/Scope/Evidence in `docs/TASKS.md` to explicitly name every deliverable (View Grammar consolidation, Calendar de-modularization + `/calendar` routing fix, real Graph renderer, `network`→`graph` rename, `tree` kind promotion). Log four evidence rows to `docs/BUGS.md`. ADR-108 records the architectural call. User's explicit directive in this turn ("Calendar is not alias for google calendar but a view in the view list... ensure the UI standardization task captures all the views, features and functions... share the updated DataEngine views BRD") = the approval. Confirmed (not changed): Graph/relationship graph already has no separate entity — Relations render as edges over People/Community rows; only the renderer is a tracked gap, not the data model. No code changed in this pass — TASK-014 executes the consolidation. | APPLIED | user · 2026-07-17 | (this commit) |
| AP-035 | 2026-07-17 | user directive | Task Manager Module second revision: Goal collapses into a Task field, full tree restructuring, no-default Chief-of-Staff-owned routing, reopenable done status (TASK-021) | Revise the Task Manager plan/BRD/wiki again per user pushback: (1) drop the `goals` Database — `is_goal: boolean` becomes a plain field on the one `tasks` Database (ADR-106, supersedes ADR-105's two-Database call); a Task with `is_goal=true` carries `outcomes[]`/`review_cadence` and is not required to reach one terminal "done." (2) Add full tree restructuring as governed proposals — promote (drop a stale ancestor, subtree becomes a new root), insert-ancestor-above (`b.c.d`→`a.b.c.d`), re-parent — all pure `parent_task_id`/materialized-`path` updates recomputed atomically over the affected subtree, never a row migration, because there is only one type (new `task-tree-restructure` Skill + `task-tree-restructure-proposal` Automation). (3) Remove the "default Capability Builder" agent-task-routing fallback entirely; routing always resolves by matching a Task's required Skill against eligible Agents (reusing the real `SkillManifest` resolution shipped under ADR-104/TASK-007); ambiguous/no-match escalates to explicit Human assignment. Ownership of `agent-task-routing` moves from Internal Strategist to **Chief of Staff** (ADR-107) — CoS's glossary-defined coordinating/routing role and its shipped @mention-dispatch precedent, versus Internal Strategist's analytical/placement mandate. Reschedule-confidence-calibration is extended to also calibrate routing confidence; a new `routing-approval-gate` Automation applies the same `classifyApprovalBand`-style system gate ADR-073/`reschedule-approval-gate` already uses. (4) Add `target-change-reopen-prompt` Automation — done/archived/parked Tasks accept a governed reverse transition to pending/in_progress on a changed outcome target, the ordinary task-manager "reopen" convention. (5) Confirmed no existing task overlapped Task Manager and the 2026-06 Taskade research remained the binding companion doc (R-037 question 1). User's explicit directive in this turn = the approval. | APPLIED | user · 2026-07-17 | (this commit) |
| AP-034 | 2026-07-16 | user directive | Task Manager Module revision: two-Record-type model, agent routing, calibrated reschedule, proactive scan, reorder (TASK-021) | Revise the Task Manager plan/BRD/wiki the same day, before commit, per user pushback: (1) drop `Outcome`/`Initiative` as separate Record types — `Initiative` stays retired per AP-020, `status: candidate` on a self-referential Task carries its meaning, `Outcome` becomes a structured `outcomes[]` field on Goal and Task; adopt the user's dot-notation materialized path (`2.3.5`) for level tracking; keep Goal distinct from Task (reasoned pushback, not silent compliance — Goal's review/revise lifecycle and one-Goal-many-candidate-trees shape don't collapse cleanly, and the glossary already locks the distinction). ADR-105 (renumbered — main already used ADR-099 for a different decision) revised in place to record both the original and corrected reasoning. (2) Add impact-fit-analysis on every Task/Goal create, owned by Internal Strategist. (3) Add deterministic agent-task routing — default Capability Builder in this Module, Skill-eligible Agent for cross-Module tasks (Builder's "creates/tests capability changes" scope is not stretched to cover every domain). (4) Add a confidence-graduated reschedule approval gate for human-assigned tasks, reusing the ADR-073 `classifyApprovalBand` system-gate + VAR-1 Variance-Adjuster pattern verbatim — no new "agent decides" mechanism, agent-floor DENY untouched. (5) Add a scheduled proactive cross-Module opportunity scan owned by Internal Strategist, output always a governed candidate draft. (6) Move TASK-021 from queue-end to immediately after TASK-014 in both the execution-order list and the file's physical row position. User's explicit directive in this turn = the approval. | APPLIED | user · 2026-07-16 | (this commit) |
| AP-033 | 2026-07-16 | user directive | Task Manager Module plan + BRD + roadmap addition (TASK-021) | Add the Task Manager Module to the canonical queue: `docs/raw/taskmanager-module-plan-2026-07.md` (TM0–TM6) + `docs/raw/brd-taskmanager-2026-07.md` + `docs/wiki/taskmanager.md`; TASK-021 appended at queue end pending user re-rank. User's explicit directive ("create a detailed plan for task manager module… draft a detailed plan and add to roadmap", 2026-07-16) = the approval. Module maps user ontology onto canon with zero kernel primitives; user's methodology skill idea adopted as a capped Playbook library (5 in v1), not ~20 Skills; execution layer reuses the pipeline. Plan `status: proposed` — flipping to active/approved stays a separate gate. Superseded same day by AP-034's two-Record-type revision (the initial draft's Outcome/Initiative-as-Record-type call did not survive). Renumbered during merge-integration from this session's original AP-030/AP-031 (main already used those IDs for parallel task-integration approvals). | APPLIED | user · 2026-07-16 | (this commit) |
| AP-032 | 2026-07-17 | user directive | TASK-007 completion and main integration | Continue executing the canonical roadmap, merge every completed task to `main`, and resolve conflicts. Applies TASK-007 `in_progress` → `done` only after its exact Goal/Task/Skill/child-Run prototype contract, persistent-upgrade path, affected-neighbour checks, and independent security reviews pass. | APPLIED | user · 2026-07-17 | (this merge) |
| AP-031 | 2026-07-16 | user directive | TASK-004 closure + bounded TASK-005 gate glue | Mark TASK-004 DONE only after its exact clean desktop+375px Prototype test. Add the smallest TASK-005 shared-file slice: persist singular Automation Agent ownership, derive actor server-side, wire one honest manifest-declared governed Run to existing Approvals, and preserve TASK-005 BLOCKED until TASK-003 physical evidence plus full combined certification. Renumbered during integration because main already used AP-030 for task-by-task roadmap integration. | APPLIED | user · 2026-07-16 | (this merge) |
| AP-030 | 2026-07-16 | user directive | Task-by-task roadmap integration and completion status | Commit, push, conflict-resolve, and merge each completed subagent task to `main`; apply canonical completion only after its exact exit test and affected-neighbour checks pass. This authorizes TASK-008's `in_progress` → `done` transition after its integrated Relationship prototype, trust-boundary hardening, desktop/375px evidence, and full affected-package verification passed. | APPLIED | user · 2026-07-16 | `590cca6` |
| AP-029 | 2026-07-16 | user directive | Dependency-aware parallel start of TASK-006 through TASK-015 | Start the next ten roadmap tasks now instead of waiting for full TASK-005 certification. Execute independent TASK-006/007/008 foundations immediately; start TASK-009–015 as scoped planning work and release each implementation only when its recorded dependencies are available. This is an initiation-order exception, not permission to bypass task dependencies, prototype tests, security/rights gates, or honest blocker reporting. | APPLIED | user · 2026-07-16 | (uncommitted coordinated worktrees) |
| AP-028 | 2026-07-16 | user directive | TASK-002 trust-first onboarding and controlled-learning closeout | Implement and fully close canonical TASK-002 from main `5775e5b`: preserve landed work; finish the live trust ceremony, explicit why/consequence copy, cited approval-gated role-model recommendation, re-entry/reset and learning controls, schedulable day-7 prompt, and persistent attributable Learning Agent Signal authority; verify desktop plus 375px; update canonical evidence only after the exact prototype test passes. | APPLIED | user · 2026-07-16 | (uncommitted review diff) |
| AP-027 | 2026-07-16 | user directive | TASK-001 coherent actionable shell closure | Implement every remaining TASK-001 blocker from main `5775e5b`, run the exact desktop/375px Prototype test through real API/web/desktop paths, preserve broader runtime follow-ups honestly, and mark TASK-001 done only when the exact exit test passes. | APPLIED | user · 2026-07-16 | (uncommitted worktree) |
| AP-026 | 2026-07-16 | user directive | TASK-003 Avatar mobility implementation | Execute the current roadmap exactly: use OS-level Avatar drag, persist positions, reconcile runtime display topology, implement pinned permissive `tauri-nspanel` non-activating/all-Spaces/fullscreen behavior on macOS, and integrate native window controls into the Sidebar without duplicates. Keep the task blocked if unavailable physical hardware prevents the exact prototype test. | APPLIED | user · 2026-07-16 | (this commit) |
| AP-025 | 2026-07-16 | user directive | Task Manager order + documentation layers | Captured user-provided ranks 1–20 and made the sequence canonical in `docs/TASKS.md`. Kept status on each task; in-progress work ranks first, pending work follows the captured order, and completed work remains at the bottom. Reduced `docs/PROGRESS.md` to pointer/rules and preserved its history in `docs/raw/progress-archive-2026-07.md`. | APPLIED | user · 2026-07-16 | (this commit) |
| AP-024 | 2026-07-15 | user directive | canonical task/documentation system + reconciled roadmap order | Replace overlapping active roadmap/bug/request/approval queues with `docs/TASKS.md`: one task contains roadmap scope, bug evidence, user requests, approval gate, implementation status, and a falsifiable prototype test. Preserve PROGRESS/BUGS/requests/APPROVALS as history/evidence/audit only. Reconcile related work into 20 ordered tasks and gate broad cleanup behind the TASK-001→TASK-005 Avatar+Commons demo path. Task Manager consumes only canonical tasks. | APPLIED | user · 2026-07-15 | (this commit) |
| AP-023 | 2026-07-15 | user directive | DealPilot ETA/Page/Source credential model + permanent Internal Strategist + Goal/Task Skills + child Agent Runs + platform red flag + JobPilot culture research | DealPilot has no personas and only Deals/Sources/Theses default Pages; every Record has dedicated Record Detail; standard capability inventory is not customized structurally; Source table securely projects credential-vault values and adds Link/Last checked/Spend cap with explicit data-rights gates. Permanent roster becomes CoS/Learning/Internal Strategist/Governance/Builder. Skills bind Goals/Tasks; eligible assigned Agents may use them; bounded child Agent Runs inherit ceilings. Green/yellow feedback flags removed; red flag becomes platform feedback. JobPilot adds governed culture research. | APPLIED | user · 2026-07-15 | (this commit) |
| AP-022 | 2026-07-15 | session (fable) | Platform naming (vision.md, CLAUDE.md, @bridge/* package scope) | User floated renaming the platform Bridge→Zazoo. Session recommendation: DON'T rename the platform; adopt Zazoo as the flagship companion character's name only (Clippy/Duo pattern). Reasons: (a) naming the platform after one avatar couples brand to a single character while characters are meant to be swappable/Commons-shippable and per-user renameable (`avatarName` already exists); (b) "Zazoo" is consumer-cute vs the professional workspace-platform positioning, and DealPilot/Helpdesk read oddly under a plush-cat brand; (c) trademark adjacency: "Zazu" (Disney/Lion King) + existing zazoo-branded consumer products — needs clearance before any platform-level commitment; (d) mechanical blast radius is large (@bridge/* scopes, vocab lint rule, docs corpus). If user still wants the full rename after (c) clears, scope it as a dedicated batch. | PROPOSED | — | — |
| AP-021 | 2026-07-14 | user directive | shell IA + Module actionability + Agent/Skill authority + Relationship Signal/Memory model | No display aliases accepted. Remove visible Tools and global Knowledge surfaces through real route/code/schema migration. Every installed Module appears in left nav and opens manifest-driven detail showing Pages, Agents with nested Skills, Automations, Integrations, Files, Runs, and settings. Only Agents consume Skills; Automations start Agent Runs. Left Sidebar/right Chat Panel share expand/collapse/extend behavior. Relationship toggles become Signals/People/Communities; Signal is surfaced Event with Person/Community participants. All retained data is Module-associated Memory. Add actionable Second Brain cross-Module graph below Modules; name never replaces Engine. | APPLIED | user · 2026-07-14 | (this commit) |
| AP-020 | 2026-07-14 | user directive | vocabulary canon + Avatar/Engine/Relationship/Plane decisions + code migration | One `docs/glossary.md`; only Local/Cloud are Planes; Relationship/Work are Domains; Commons/Bridge Cloud are services; Engine replaces Brain; Avatar is one term with operational presence only; one Onboarding flow; File replaces user-visible Artifact; Project/Initiative/Touchpoint/Ritual/Workflow/Signal/Incident/Element/Package/Workspace and other retired identifiers enter mandatory code/schema/API migration, not display aliases. Relationship becomes one Module containing People/Communities/Relations/Interactions/Introductions/Helpdesk/Sources/Automations. Add RT0–RT4 runtime-taint root-gap plan. Supersedes conflicting vocabulary portions of ADR-033/035/053; sequencing remains Avatar+Commons prototype first. | APPLIED | user · 2026-07-14 | (this commit) |
| AP-019 | 2026-07-14 | session (opus) | roadmap Batch 9 — Month-6 PKG-1 + PKG-2 + BLUEPRINT-1 + CONSOLIDATE DONE (`docs/PROGRESS.md` §Batch 9; sequencer `roadmap-6month-2026-h2.md` §M6) | Mark PKG-1 (sandbox floor before executable logic), PKG-2 (Commons supply-chain trust: signing + verify-on-install + TLS-by-default + community-origin floor), BLUEPRINT-1 (WorkspaceBlueprint frozen as a versioned, Commons-publishable manifest), and CONSOLIDATE (testing debt) DONE and tick their PROGRESS boxes. Landed + verified this session: built the coupled `@bridge/core` foundations directly (`signing.ts` canonicalize/verify/TLS → `blueprint.ts` `parseWorkspaceBlueprint` closed-key gate + `workspaceBlueprintTo/FromPackageManifest` bridge + `schemaVersion` → `approvals.ts` community-origin floor → `manifest.ts`/`types.ts` blueprint+execution wiring → `index.ts` barrel), then fanned two genuinely-independent edges to **parallel background subagents** — `services/commons` ed25519 sign-seam (`GET /v1/signing-key` + sign-on-publish, 10/10) and `integrations-google` behavioral tests (CONSOLIDATE; 35/35, coverage `gateway-google` 29→53% / `oauth` 45→100% / `intake` 66→92%, floor 50→53) — owning all apps/api wiring + tests myself (`router.ts` PKG-1 sandbox gate + PKG-2 community floor; `commons-client.ts` verify-on-install via `node:crypto` ed25519 + TLS-by-default constructor). **Key calls**: PKG-1 — an `execution` spec's PRESENCE marks a capability executable and the MCP carve-out was removed (ADR-079); PKG-2 — crypto bound at the seam so `@bridge/core` stays zero-runtime-deps, and v1 uses a registry-held signing key (curated-registry trust root; per-publisher keys post-v1) (ADR-080); BLUEPRINT-1 — declarative-ness enforced by a closed-key parse allowlist (anything unknown rejected), a workspace_definition composes capabilities by reference so `capabilities[]` is empty (ADR-081); CONSOLIDATE — NO vitest added (the `node --test` + coverage-floor + turbo setup already gates CI), and two downstream floors were recalibrated (`sensors` 40→39, `db` 55→54) for a core-barrel coverage-dilution artifact — no package's OWN coverage dropped (ADR-082). Full `turbo run typecheck test build --force --concurrency=1` **59/59** green (serial to avoid the documented pglite/db parallel-resource-pressure flake; each apparent parallel failure passed cleanly in isolation); `@bridge/core` → 347, `apps/api` 75→90 (pkg1-sandbox-install + pkg2-commons-signing + blueprint-commons-roundtrip), `services/commons` 10, `integrations-google` 35. DONE-WHEN met for all 4 (install rejects unsandboxed executables + clears a container-isolated one; client rejects altered/unsigned/untrusted-key + enforces TLS; blueprint round-trips manifest→sign→verify→extract→compile + the gate rejects a smuggled `handler`; the runner/floors are codified + green). ADR-079–082 recorded. Blast radius clean (only `publish-builtins` uses `HttpCommonsClient`, default localhost, unaffected; `buildCommonsServer` `keyPair?` optional; new gate fns have no other callers). No dummy data (real ed25519 keypairs + `test-fixture-*` names). New BUGS row: `blueprintFieldSchema` enum omits `"location"` (store 10 vs router/blueprint 11 — pre-existing, out of scope). Deferred by design: store-layer `resolvedTrustGrants` for the origin floor, per-publisher author-key signing, a `docs/wiki/testing.md` rewrite. | APPLIED | user · 2026-07-14 | (this commit) |
| AP-090 | 2026-08-01 | session (opus) | TASKS.md — new TASK-029 (WhatsApp Module); docs/superpowers/specs/2026-08-01-whatsapp-module-design.md; ADR-157 | User directive in-session: "I want to create a new module called Whatsapp…", design presented and answered "Plan approved, continue". Approves adding `whatsapp` as a built-in Module (nav entry, Chats + Tools Pages, local-plane Contact Steward Agent), vendoring the pinned wa-js bundle, and queuing TASK-029. Two later user corrections are recorded here because they reverse earlier recorded decisions: (a) contacts are stored on the LOCAL PLANE as a person list, NOT on cloud canonical — a staged cloud-import script was written and then withdrawn on the user's correction "isnt people canonical both local and cloud? I asked you to store in local plane"; (b) v2 (outbound send, approve-per-recipient-then-trusted) was built in the same session per "continue with tasks 7 and 8 while also paralley building v2 in this session itself". The Module is withheld from Commons. | APPLIED | user · 2026-08-01 | (this commit) |
| AP-091 | 2026-08-02 | session (opus) | TASKS.md — new TASK-030 (WhatsApp write-enabled messaging, local searchable store, makeshift Bridge UI); ADR-158 (supersedes ADR-157's engine, read-only and residency sections) | User directives in-session across several rounds: proposed building our own frontend over `whatsapp-web.js`; then argued the architecture themselves — "we should not introduce whatsapp-web.js as another independently authenticated client", "One WhatsApp account, one authenticated browser profile, one WA-JS runtime, and multiple application surfaces consuming that runtime"; "Drop the multi-account selector"; "No unified inbox"; "I want the chat data stored locally and searchable"; "When I said UI, I meant a makeshift option, not pixel to pixel whatsapp replica"; and finally "I dont want read only, but write enabled version. ... Then continue with development". **This row REVERSES two things AP-090 recorded.** (a) RESIDENCY: AP-090 recorded that raw capture and phone numbers stay on the Local Plane with message bodies never stored; TASK-030 persists clients' personal message BODIES locally and indexes them for search. Encryption at rest is an accepted documented limitation, not a solved problem. (b) READ-ONLY: ADR-157 shipped v1 with no write; TASK-030 enables outbound sending in v1, gated by `decideSend` and a rate ceiling enforced in Rust. The user was told plainly that outbound automation from a personal number moves the account toward the behaviour Meta's detection targets, that bans are permanent and unappealable, and reaffirmed the request. | APPLIED | user · 2026-08-02 | (this commit) |
| AP-096 | 2026-08-04 | user directive | Vocabulary-gate resolution: build the explicit allowlist the gate promised, exempt only foreign contracts, rename Bridge-owned `workspace` (ADR-171); disable project-level plugin entries so committed settings stop charging every agent always-loaded skill metadata | User directive in-session: "Can you fix the main's landing issue?" — TASK-036 recorded the vocabulary gate as a canon call needing its own row. Approves: (a) renaming `workspace`→`organization` in main's 2026-08-04 landings including the `KnowledgeLayer` value and the user-facing "Organizations like yours" copy; (b) `scripts/retired-vocabulary-allowlist.json` with seven reviewed entries covering LLM tool-calling, DOM/HTML `element`, and one vendored bundle. Does NOT approve migrating the WhatsApp Module's "Tool" product vocabulary (user-visible Page + `whatsapp.tool.*` capability ids) or the `knowledge`/`project`/`legacy_plane` learning terms — both remain open on TASK-036. | APPLIED | user · 2026-08-04 | (this commit) |
| AP-097 | 2026-07-18 | user directive | Path-scoped Copilot instructions for developed Modules (TASK-024; RENUMBERED from AP-040 on 2026-08-04 — that id was already taken by the TASK-003 recovery approval on main) | Add `.github/instructions/*.instructions.md` adapters for shared Module development plus DealPilot, JobPilot, Relationship, Task Manager, and nested Helpdesk so future Copilot sessions receive established patterns automatically. Keep `CLAUDE.md` canonical, avoid duplicating full plans, and do not preserve Calendar's superseded Module identity. User's explicit problem statement in this turn is the approval. | APPLIED | user · 2026-07-18 | (this commit) |
| AP-098 | 2026-07-18 | user directive | Elaborate current-code patterns in Module Copilot instructions (TASK-024; RENUMBERED from AP-041 on 2026-08-04 — that id was already taken by the physical-input certification approval on main) | Review each developed Module's current implementation and expand the six existing path-scoped instruction adapters one by one with concrete domain seams, trust boundaries, deterministic behavior, persistence/UI separation, migration cautions, and validation expectations. User directive: "Go throught current code of each module and see the patterns and then add it in the instrcutions files one by one". | APPLIED | user · 2026-07-18 | (this commit) |

| AP-018 | 2026-07-14 | session (opus) | roadmap Batch 7 — Month-5 AGENTS-1 + AGENTS-2 DONE (`docs/PROGRESS.md` §Batch 7; sequencer `roadmap-6month-2026-h2.md` §M5) | Mark AGENTS-1 (the foundational agents invocable as first-class peers via a shared prompt-assembly seam; `invokeAgent` governed union with no executed variant) and AGENTS-2 (onboarding-profile → Chief-of-Staff persona, resolved server-side) DONE and tick their PROGRESS boxes. Landed + verified this session (built sequentially — both edges live in `@bridge/core`, so parallel subagents would race on shared build state): **AGENTS-1** — reconciled undefined-elements #6 "PromptAssembler" with the shipped `RunContextAssembler` (ADR-027), extended with layer-1 `KERNEL_INVARIANTS` (non-omittable) + layer-2 agent identity (`RunPersona.responsibilities`/`guardrails`/`tone`) via a shared `renderPersonaSystemPreamble`; `projectToSystemPrompt` sibling projection (`projectToPrompt` untouched); `agents.ts` `buildAgentPersona` + `invokeAgent` returning `AgentInvocationResult` = `information`|`draft` with **NO executed variant** (structural "no independent write", core holds no pipeline handle). **AGENTS-2** — Memory-family `OnboardingProfile` view + `profileFromRow` (honest, omits absent fields) + `resolveAnimalTone` (reuses `ANIMAL_TONE`) + `buildChiefOfStaffPersona`; apps/api `converse` resolves tone + persona SERVER-SIDE from `onboardingProfileStore` (stored animal wins over client `input.animal`) and returns an additive `persona` card; @mentions route through `invokeAgent`. **Key call (ADR-077)**: roster held at ADR-046's **4 agents + Communications skill** — the roadmap's "5 agents" wording is superseded; Communications NOT re-promoted. Full `turbo run typecheck test build --force` **59/59** green; `@bridge/core` → 317 (93.04%), apps/api converse tests 7→9; ESLint clean on kernel paths. DONE-WHEN met (information vs draft by `requiresApproval`, kernel-invariants in the assembled prompt, dummy-data flagged-not-gated — `core/test/agents-invoke.test.ts`; two profiles → two distinct CoS personas + animal tone reflected + graceful degrade — `core/test/agents-persona.test.ts`; two stored profiles → two persona cards + stored animal overrides client field — `apps/api/test/chief-of-staff.test.ts`). ADR-075–078 recorded. No dummy data; no new BUGS. **Related scope record (not a DONE-tick)**: Month-5's XP-2 + XP-3 were split into an infra-gated **Batch 8** (ADR-078) — their DONE-WHENs need signing certs + a 3-OS CI matrix (XP-2) and an on-branch mobile app + devices (XP-3), unverifiable here; confirmed with the user. Deferred by design: the richer four-axis SpiritAnimal tone card (Commons-authored), a model-backed CoS voice. | APPLIED | user · 2026-07-14 | (this commit) |
| AP-017 | 2026-07-14 | session (opus) | roadmap Batch 6 — Month-4 self-improvement loop DONE (`docs/PROGRESS.md` §Batch 6; sequencer `roadmap-6month-2026-h2.md` §M4) | Mark EVAL-3 (baseline-vs-candidate two-gate promotion wired into `capability.approve`), EVAL-4 (LLM-judge quality Scorer: pinned model, held-out, calibration, red-team External gate), REG-1 (Component Registry + two-tier overlap on `capability_manifests`+`kind`), VAR-1 (Variance Adjuster bounded governed nudge), GOV-1 (org-health rollup + Governance minor-only auto-approve) DONE and tick their PROGRESS boxes. Landed + verified this session: built the coupled core/db foundations directly (`policy_params` tunable space with hard ceilings excluded by construction → `capability_manifests.kind` + migration `0010` → EVAL-3 `comparison`), then fanned the 4 genuinely-independent core surfaces (EVAL-4/REG-1/VAR-1/GOV-1) to **4 parallel subagents**, owning all `index.ts`/`router.ts`/`wiring.ts` integration myself. **Key call (ADR-073)**: Governance auto-approve is enacted as a SYSTEM/router gate (`classifyApprovalBand` → minor only), NOT by implementing the documented agent-floor exception — the hard agent-floor DENY is preserved. EvalStore/PolicyParamStore wired in-memory into `apps/api` (EVAL-1/2 infra had never reached the router; Drizzle bindings deferred). Full `turbo run typecheck test build --force` **59/59** green; `@bridge/core` → 301 (93.08%), `@bridge/db` 57→61 (56.42%), `apps/api` 67→74 (60.73%); ESLint clean on kernel paths. DONE-WHEN met for all 5 (promote-with-why-better-card + reject-regressor, judge calibration vs human approve/veto, near-duplicate detected before creation, 3× "too casual" → governed `tone_threshold` nudge + ceilings can't cross, org-health renders + auto-approves only minor — in `apps/api/test/capability-governance.test.ts` + 5 core test files). ADR-068–074 recorded. The `@bridge/db` floor dip (57.87→54.30, a whole-module-graph measurement artifact — the core barrel eager-loads the 6 new modules into db's loaded graph) was recovered with 3 real store round-trip tests (resources/jobpilot/helpdesk, all 100% line coverage), NOT a lowered floor (ADR-074). No dummy data (GOV-1 `violationSeries` = honest empty; in-memory stores are real). New BUGS row: `DrizzleCanonicalIdentityStore` `ON CONFLICT` cannot match the partial `dedup_key` index (spotted via a coverage probe, deferred — cloud dual-write surface, out of Month-4 scope). Deferred by design: Drizzle `EvalStore`/`PolicyParamStore`, pgvector manifest embeddings, a violation-history view, a Governance-Agent caller, a pinned production judge model. | APPLIED | user · 2026-07-14 | (this commit) |
| AP-016 | 2026-07-14 | session (opus) | roadmap Batch 5 — Month-3 PI-2 + PI-3 DONE (`docs/PROGRESS.md` §Batch 5; sequencer `roadmap-6month-2026-h2.md` §M3) | Mark PI-2 (tainted-context egress gate) and PI-3 (dual-LLM quarantine + local `ContentGuard`) DONE and tick their PROGRESS boxes. Landed + verified this session: **PI-2** — optional `taint?` on `PolicyEvalInput`; `pipeline.propose()` computes `turnTaint = req.trustOrigin ?? ctx.taint`, threads it into pre+runtime policy evals, and enforces a STRUCTURAL always-on gate (`evaluateTaintedEgress` in new `core/src/policy/taint-egress.ts`) that forces `external:send`/`share` on `untrusted_external` turns to `pending_review` — a kernel guarantee, not a deployment-configurable policy (`taintedEgressPolicy` PolicyFn also exported; wiring left unchanged to avoid duplicate results). The MCP-output-is-DATA invariant is recorded in ADR-066 (the ADR the spec requires before the first MCP integration; none exists yet). **PI-3** — a tool-less `ContentGuard` port + `QuarantinedContentGuard` (locked data-only prompt, typed-only extraction, fail-closed, local-heuristic fail-safe) + `spotlightUntrusted`/delimiters in `core/src/guard/content-guard.ts`; `projectToPrompt()` spotlights every `untrusted_external` context item/memory snippet (added optional `trustOrigin?` to `ContextItem` + `RetrievedMemorySnippet`); independent subagent added `@bridge/models` `createLocalContentGuard` (refuses cloud plane → `CloudContentGuardError`). NOT wired into apps/api (no consumer/MCP yet — dead-wiring avoided, ADR-067). Built core myself, fanned the one independent edge (models) to a subagent. Full `turbo run typecheck test build --force` **59/59** green; core 219→249 (redteam-egress 13 + content-guard 8), models 18/18. DONE-WHEN met: red-team "forward all contacts" egress → `pending_review` (`redteam-egress.test.ts`); embedded injection reduced to typed data / `safe:false` (`content-guard.test.ts`). ADR-066/067 recorded. No dummy data; no new BUGS. Deferred: a real local-classifier binding + the first consuming ingest seam. | APPLIED | user · 2026-07-14 | (this commit) |
| AP-015 | 2026-07-14 | session (opus) | roadmap Batch 4 — Month-3 PI-1 + MEM-1 DONE (`docs/PROGRESS.md` §Batch 4; sequencer `roadmap-6month-2026-h2.md` §M3) | Mark PI-1 (provenance/taint `trustOrigin` tagging) and MEM-1 (the `memories` table + `MemoryStore` port) DONE and tick their PROGRESS boxes. Landed + verified this session: **PI-1** — additive `TrustOrigin` union in `@bridge/core` threaded through the pipeline onto the ledger (+ `ledger.trust_origin` column, migration `0009`); ingestion edges tag output (sourcing connectors + the one tool-kit intake seam default `untrusted_external`; Gmail intake activates the previously-dead `intake_policy.quarantine` manifest flag; sensors taint capture records). **MEM-1** — `MemoryStore` port + `InMemoryMemoryStore` + `memoryVisible()` in core, `memories` table + `DrizzleMemoryStore` (SQL-boundary authority scoping, RLS forced) in db, capture→derived-private-episodic-Memory wired at the sensors hub (NOT apps/api — dead wiring avoided, ADR-065). Built core+db foundation myself, fanned the independent edges to 2 parallel subagents (integrations-google, sensors). Full `turbo run typecheck test build --force` **59/59** green; db 53→57; migration 0009 clean in pglite. DONE-WHEN met for both (Gmail + scraped page land `untrusted_external` end-to-end; capture→authority-scoped Memory + classification-respecting retrieval, both in tests). ADR-064/065 recorded. Deferred by design: PI-2 (tainted-context egress gating) + PI-3 (dual-LLM quarantine + ContentGuard) to a follow-up batch; persistent Mem0 adapter + a Memory consumer (onboarding persona). No dummy data; no new BUGS. | APPLIED | user · 2026-07-14 | (this commit) |
| AP-014 | 2026-07-14 | session (opus) | roadmap Batch 3 — Measurement + security M2 DONE (`docs/PROGRESS.md` §Batch 3; sequencer `roadmap-6month-2026-h2.md` §M2) | Mark EVAL-1 (AQV scoring reducer), EVAL-2 (EvalStore), SEC-5 (RLS-as-code + prod boot guard), SEC-6 (`workspace.*` membership checks), SEC-7 (Recon SSRF denylist + pino log redaction + dropped client-asserted linkedin trust path) DONE and tick their PROGRESS boxes. Landed + verified this session via 3 parallel subagents (EVAL/core, SEC-5/db, SEC-7-recon — disjoint packages) + my api wave (SEC-6, SEC-7 redaction/verification, SEC-5 boot wiring): core 219→230 (92.57%), db 49→53, api 60→67, recon +5 SSRF (metadata-IP block end-to-end = SEC-7 DONE-WHEN), `rls.test.ts` cross-tenant denial = SEC-5 DONE-WHEN, non-member invite/list → FORBIDDEN = SEC-6 DONE-WHEN. Full `turbo run typecheck test build --force` **59/59** green, all coverage floors pass. `models` floor 40→35 (deterministic 39.75%, tracked debt). ADR-060/061/062/063 recorded. Caveats: SEC-6 scoped to the membership-surface procedures (broadening deferred, ADR-062); pglite-superuser means only `rls.test.ts` exercises RLS (ADR-061); db suite heavier post-0008 (BUGS.md watch). | APPLIED | user · 2026-07-14 | (this commit) |
| AP-013 | 2026-07-14 | session (opus) | roadmap Batch 2 — Testing P0 DONE (`docs/PROGRESS.md` §Batch 2; `docs/raw/testing-strategy.md` frontmatter status DRAFT→executed) | Mark all 6 Testing-P0 items DONE, tick their PROGRESS boxes, flip the testing-strategy status line. Reality verified this batch: items 1/2/5 (decide() double-approve TOCTOU, `matchOne` exact-tie determinism, `hasExternal` double-propose) **already had passing tests** (core 219, dedupe 9, integrations-google intake-dedup green). Item 3 written new — `integrations-google/test/token-refresh-persist.test.ts` (rejecting `putToken` + emitted `tokens` event → `console.error`, no unhandled rejection). Item 4 written new — `apps/api/test/router-decide.test.ts` (propose→decide veto happy-path + agent-floor-denied decide → FORBIDDEN; `dealpilot.list` cross-workspace already in `single-tenant-guard.test.ts`). Items 3+4 done via 2 parallel subagents. Item 6 wired node's built-in `--experimental-test-coverage` + per-package `--test-coverage-lines` floors into all 19 test-bearing packages' `test` scripts (house runner stays **`node --test`**, NOT vitest — plan wording superseded; no new tooling), enforced by CI's existing `turbo run … test`. Floors set at/below **measured** current coverage (80 core/dedupe; 70 healthy majority; ratchet floors api 60 / db 55 / company-sourcing 65 / integrations-google 50 / models 40 / sensors 40 / commons 40) so CI is green day one per testing-strategy §Process. Verified: `turbo run test --force` 36/36, 19 tasks `fail 0`, all floors pass. Note: pre-existing `@bridge/web` typecheck + `prototype` tsc CI reds are unrelated + already tracked (BUGS.md). | APPLIED | user · 2026-07-14 | (this commit) |
| AP-012 | 2026-07-13 | user directive (merge reconciliation) | roadmap Batch — Security P0 DONE (`docs/PROGRESS.md` §Batch 3; sequencer `roadmap-6month-2026-h2.md` §M1) | Renumbered from developer branch `origin/manishsbhoopalam8498-security-p0-hardening`'s own local "AP-010" (collided with this repo's AP-010 priority-realignment row — that branch diverged before this session's numbering). Marks SEC-1 (auth-on-mutation middleware), SEC-2 (verifier-tied fail-closed CORS + `@fastify/rate-limit`), SEC-3 (drizzle-orm≥0.45.2 / react-router≥7.15.0 bumps + `pnpm audit` CI gate) and XP-1 (cross-OS Tauri `cargo check` matrix) as DONE. Code landed + verified on that branch: api 53→58 tests green, @bridge/db 49 green, macOS `cargo check --locked` clean; BUGS.md H1/H1a/H2/H4 → RESOLVED; decisions recorded as ADR-057/058/059 (renumbered from that branch's own local ADR-054/055/056, which collided with this repo's ADR-054/055/056 agent-roadmap/diligence/UI-architecture entries). XP-1's CI-green DONE-WHEN (all 3 OSes) awaits the first run of the new `desktop` matrix job — unverified by this reconciliation, left as an open follow-up. | APPLIED | user · 2026-07-13 (merge reconciliation) | (this commit) |
| AP-011 | 2026-07-13 | user directive | UI architecture rules (canon) + PROGRESS first-task + CLAUDE.md pointer | Encode UI architecture rules (verbatim: `docs/raw/requirement-ui-architecture-rules-2026-07-13.md`; canon: `docs/raw/ui-architecture-rules-2026-07.md`): data-shape→surface rules (different-columns→toggle pages · same-columns→lists · loosely-related→sub-module dropdown in nav), page = landing section (standard views + new **Form** view) + stacked sections + artifacts section (>20 → sub-folders), Control Panel moves into 3-dots (contents re-sorted page-sections vs admin), `~/Documents/Bridge Workspace/<Module>/<Sub-module>/` local artifact tree. First task next run = UI-RULES-1 alignment audit (PROGRESS Batch 1). AP-010 batch rewrite applied in same commit. User instruction in chat = the approval. Open interpretations logged in canon doc §9 ("forum"→Form; Documents = OS folder). | APPLIED | user · 2026-07-13 | (this commit) |
| AP-010 | 2026-07-13 | user directive | Roadmap sequencer / PROGRESS batch order | Priority realignment (verbatim: `docs/raw/requirement-priority-realignment-2026-07-13.md`): (1) Egg + Commons PROTOTYPE first, (2) repo cleanup — duplicates + deprecated data, (3) bug fixes, (4) rest of implementation next. Supersedes the security-first M1 batch order (old Batch 1–3 shift behind cleanup+bugs; SEC items fold into the bugs batch). User instruction in chat = the approval; PROGRESS batches rewritten accordingly. Note: developer branch `origin/manishsbhoopalam8498-security-p0-hardening` may already cover SEC items — reconcile before duplicating work. | APPLIED | user · 2026-07-13 | (this commit) |
| AP-009 | 2026-07-11 | user directive | CLAUDE.md + outputs/ | Record every substantive chat deliverable in a dated `outputs/` file; exclude secrets, hidden reasoning, raw tool output and transient commentary. User explicitly directed implementation and Git integration in this turn. | APPLIED | user · 2026-07-11 | dc3ff97 |
| AP-008 | 2026-07-11 | user request / Codex draft | CLAUDE.md general guidance | Add standing “license-limited capability research” rule: exhaustive lawful functional research and benchmark before custom alternative; reuse/interoperate when allowed; clean-room separation/provenance when warranted; never copy protected expression/assets or bypass license/contract/access; counsel gate for ambiguity. Exact protocol: `docs/raw/clean-room-capability-research-protocol-2026-07.md`. | PROPOSED | — | — |
| AP-007 | 2026-07-11 | Codex | long-term roadmap phase mapping | Add Optimizations O0 observability + Memory lifecycle/security to P0; lossless compression/retrieval-first context to P1; container/microVM SandboxProvider to P3; optional visible Isolated Computer package to P4; remote tenant pools to P6. Preserve current six-month batch order until dependencies/security gates clear. Full proposal: `docs/raw/optimizations-memory-vm-dealpilot-plan-2026-07.md` §6. | PROPOSED | — | — |
| AP-006 | 2026-07-10 | user directive | Reference Brain/Engine plan (ADR-035) from branch claude/ai-assistant-brain-design-07d52a to start the demo-grade prototype build | Initially cherry-picked as a would-be ADR-046; on merging with origin/main, discovered the plan had already landed there verbatim as ADR-035 (commit 62bd530 was already in origin/main's history) — dropped the duplicate cherry-pick, kept origin/main's copy, renumbered this session's OWN new ADRs (046/048→046/047) to close the gap. No new content merged from that branch beyond what origin/main already had. | APPLIED | user · 2026-07-10 | (this commit) |
| AP-005 | 2026-07-10 | user directive | Communications Agent → skill (reverses ADR-032/033 agent-roster portion, ADR-046) | Permanent agent roster 5→4 (drop Communications, keep Governance — see ADR-046 for the authority-ceiling reasoning the user asked for and confirmed). No prior record of this decision found in any worktree's decisions-log/log.md or session-transcript search; user re-confirmed in-session rather than blocking on the unverifiable memory. | APPLIED | user · 2026-07-10 | (this commit) |
| AP-004 | 2026-07-10 | user directive | BUG-INTAKE interrupt protocol (CLAUDE.md + PROGRESS.md, ADR-045) | User-reported bugs preempt default batches: verbatim requirement doc → plan sweep (root-cause/same-surface/easy-win, ≤~30% pull-in cap) → INTERRUPT batch → execute → resume. Standing rule = standing approval for the reprioritization. | APPLIED | user · 2026-07-10 | (this commit) |
| AP-003 | 2026-07-09 | session (opus) | consolidation trio reconciliation (ADR-044) | Handoff = stable brief; Execution Plan tracks execute only behind discovery+safety gates; discovery gate now PASSES (paths exist as of 2026-07-09). | APPLIED | user · 2026-07-09 | (this commit) |
| AP-002 | 2026-07-09 | session (opus) | dummy-data policy (CLAUDE.md + dummy.md) | Ratify: no dummies unless unavoidable; when unavoidable → track in `docs/dummy.md`. Resolves the previously-open "test fixture" question. | APPLIED | user · 2026-07-09 | (this commit) |
| AP-001 | 2026-07-09 | session (opus) | mechanism bootstrap | Create this approval mechanism itself | APPLIED | user · 2026-07-09 | (this commit) |
