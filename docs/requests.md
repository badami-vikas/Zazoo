# Requests — intent ledger

> This append-only file preserves user intent and fulfillment history. It is not an execution queue. Partial/open intent must map to exactly one canonical item in [`docs/TASKS.md`](TASKS.md), where related requests may share a task.

All explicit product and engineering requirements from Vikas, recorded verbatim or close-paraphrase with date and status.

---

## Open

| # | Date | Request | Status | Notes |
|---|------|---------|--------|-------|
| R-001 | 2026-07-07 | Offline capability — desktop app must work without internet; data stays local | **Done** | ADR-031 (merged 44dc255): desktop shell spawns @bridge/api as managed Node child (free port, /health gate, killed on exit); injected `__BRIDGE_API_URL__`; in-memory default (honest: data lost on quit), DATABASE_URL passthrough for local Postgres. cargo check + builds green. GUI runtime launch NOT yet verified (headless env) — needs a real `tauri dev` run. |
| R-002 | 2026-07-07 | Floating desktop companion overlay — avatar/egg must be an OS-level floating window, NOT embedded inside the main app window | **Done** | ADR-031: second Tauri window `overlay` (96×96, transparent, always-on-top, skip-taskbar, bottom-right) with own Vite entry reusing Creature+avatar-store; state machine v1 = collapsed/hover/expanded_idle/working (result_ready/dismissing typed, not yet driven); in-page overlay suppressed under Tauri. Same GUI-runtime caveat as R-001. |
| R-003 | 2026-07-07 | DealPilot, JobPilot, Helpdesk must be real add-on packages — not hardcoded into routes/shell | **Done** | Built-in packages seeded on API boot (built-in-packages.ts); Intelligence → Tools derives from packages.list state (commit 9a30e8c). Route-level gating still coarse (routes registered statically). |
| R-004 | 2026-07-07 | Commons — start ground work; can begin as local server, migrate to cloud later | **Done** | ADR-030 (merged 609747d): `platform/services/commons` Fastify registry (port 4780, FS JSON store behind CommonsStore port), permanent v1 contract, knowledge-only publish gate (422 + offending paths), CommonsRegistry port in core + HttpCommonsClient in api, publish-builtins script. Marketplace = Commons WEBSITE surface (user call) — web app consumes installed Modules only. |
| R-005 | 2026-07-07 | dummy.md — track any unavoidable dummy data; state reason + real replacement before creating | **Done** | docs/dummy.md created with protocol. Currently empty (real-data policy holds). |
| R-006 | 2026-07-07 | requests.md — record all user requirements here | **Done** | This file. |
| R-007 | 2026-07-07 | Intelligence tab: add Packages toggle before Tools | **Done** | Committed in 4e68d85. Wired to real packages.list tRPC procedure. |
| R-008 | 2026-07-07 | Egg version: minimal, bare-bone, not bulky | **Partial** | Onboarding is 2-question minimal. Bulk concern is likely overlay being page-embedded not floating (R-002). Revisit after R-002 lands. |
| R-009 | 2026-07-07 | Standard page shell for all pages except Home/Settings: centered toggle → Lists → toolbar (view dropdown/search/filter/3-dot) → filter chips (if selected) → dashboard (analytics row) → content (table/kanban/card per view). Signals screenshot = canonical reference. No separate tool-name row — the toggle IS the identity element. | **Done** | Shell-v2 (as revised by R-011/012/013) rolled to 14 pages incl. DealPilot/Signals retrofits (merged 0e439f8). Detail/create pages + PublicHelpdesk + WorkspacePage intentionally excluded (single-record / external / generated-view surfaces). |
| R-010 | 2026-07-07 | Faithful port of the ENTIRE prototype (bridge-ai-1ay.pages.dev = "Design Bridge AI Interface (Copy)") into apps/web FIRST — HomePage and every missing page/surface — before further shell restyling | **Done** | Merged 2026-07-07 (agent commit 9c3feba): 22 pages incl. HomePage (now index), WorkPage, ItemDetail, DataEngine network view; 15 new routes; deps added (motion, glide-data-grid, leaflet, tesseract.js, supabase-js, …). Kept real-data pages + shared shell. Ported fixture data logged in dummy.md — removal condition: shell-v2 real-endpoint wiring. Skipped (shell owned by real Layout): prototype Sidebar/AgentPanel/Header/AuthGate/standalone routes. Verified: typecheck+build clean, HomePage + /work confirmed live. |
| R-011 | 2026-07-07 | Lists becomes a DROPDOWN in the toolbar row (not a pill row): first item = currently-open list, up to 5 visible then scrollable, "Add list" always pinned as the 6th omnipresent slot | **Done** | ListDropdown shipped + rolled to 14 pages (merged 0e439f8); ListBar deleted. Verified live on /dealpilot. |
| R-012 | 2026-07-07 | Filter chips + dashboard analytics form ONE collapsible section — expand/collapse arrow next to the 3-dots, expanded by default | **Done** | CollapsibleInsights shipped across same 14 pages; chevron sits after 3-dots. The intervening Control Panel slot was later superseded by AP-011 and removed 2026-07-14. |
| R-013 | 2026-07-07 | Single-section pages: no toggle chrome — just the name, Title case, centered in header | **Done** | shared/Header renders a plain centered title when tabs.length === 1. |
| R-014 | 2026-07-07 | AI chat panel identity = the avatar, not a Bridge monogram: header icon (done earlier today) AND the collapsed rail must show the avatar | **Done** | AvatarIcon in header (commit 0f37579) + collapsed rail now leads with AvatarIcon. |
| R-015 | 2026-07-07 | Align ALL architecture docs, plans, roadmaps with the f87dd61 primitive-ontology refinement (docs/wiki/ontology.md + raw authority-model / runtime-pipeline / capability-evolution / primitive-specifications) | **Done** | Merged 68367d2 (ADR-028): 13 wiki pages mapped, raw docs bannered, frontmatter repaired. Code renames out of scope for this pass. |
| R-016 | 2026-07-07 | Continue primitive + platform development via subagents — FIRST preference: offline desktop app (R-001 sidecar + R-002 floating overlay) and Commons (R-004) | **Done** | All four subagent branches merged 2026-07-07 (desktop 44dc255, commons 609747d, shell-v2 0e439f8, settings/nav 8e7b754). |
| R-017 | 2026-07-07 | Settings = PLATFORM-wide admin only. Rename Workspace → Organization. Settings nav = Organization / Team & Permissions / Knowledge / Intelligence / Governance / Notifications / Billing & Plan / Security / API Keys / Help & Support. Knowledge answers "what does the platform know?", Intelligence answers "what can it do with what it knows?" | **Done** | ADR-029 (merged 8e7b754): ten sections live, real data (workspace.list, listMembers+invite, google.list, packages.list, action.listPending), honest empty states for Notifications/Billing/Security/API Keys. Verified live. |
| R-018 | 2026-07-07 | Initiative Control Panel: per-Initiative admin ONLY (never cross-Initiative). ⚙ icon ALWAYS between Filters and the 3-dot menu in the header/toolbar. Opens a unified resource table (Category / Name / Status / Source Module / Version / Scope / Actions) — single admin surface, no scattered config screens | **Done; placement superseded** | Route `/initiative/:id/control-panel` + unified table shipped (ADR-029). AP-011 superseded the dedicated ⚙ slot: Initiative detail now exposes Control Panel inside the page 3-dots menu. HONEST GAP (BUGS.md): API has no per-initiative resource binding yet — rows show "Organization-wide" scope until that lands. |
| R-019 | 2026-07-07 | Apple-like product architecture: nav = Home + Initiatives as first-class items + "+ New" (always below initiatives, above Settings) + Settings. NO intermediate Initiatives page. Four concepts only: Initiative / Module / Knowledge / Intelligence. Marketplace lives in COMMONS (website), not the web app. "+ New" → installed-Module picker → Chief of Staff analyzes overlap → recommendation (new / extend / merge / reject) with reasoning, user can override. CoS continuously optimizes environment (reduce duplication, maximize reuse) | **Partial** | Nav (Home→Initiatives→+New→Settings) + Module picker + real chiefOfStaff.converse recommendation shipped (merged 8e7b754). OPEN: CoS decision ENGINE (structured overlap analysis, new/extend/merge/reject verdicts, continuous optimization) = next kernel wave. |
| R-020 | 2026-07-07 | NO deprecated terminology or legacy primitives anywhere user-facing — labels, placeholders, help text, empty states, navigation. Ontology vocab only (Initiative/Module/Knowledge/Intelligence/Assistant/Skill/Automation/Workflow). Progressive disclosure; users never see internal architecture unless in configuration mode | **Done** | Sweep merged (38319c9, 23 web files): Ritual→Workflow, Workspace→Organization/Bridge, Project→Initiative, blueprint→"proposed setup" in all user-facing copy; identifiers/routes/procedures untouched. Verified live: zero "workspace" strings on screen, onboarding = "Set up Bridge". OPEN TAIL (BUGS.md-worthy): ~15 apps/api error strings still legacy-vocab (ritual/workspace_definition/capability manifest) — surface in error toasts; server copy pass pending. |
| R-021 | 2026-07-08 | Fix: no "sort" affordance between Filter and 3-dots — that slot is for Control Panel only | **Done** | Removed dead no-op onSortClick/Sort button from JobPilotPage + deleted the prop entirely from StandardToolbar (commit 40cb569) so it can't reappear. |
| R-022 | 2026-07-08 | Home page: drop the middle "AI chat"-style prompt bar — the right-hand AgentPanel is already omnipresent, this was redundant | **Done** | Removed HomePage's "Re-shape" intent input form + its state (40cb569). Verified live: no form on Home. |
| R-023 | 2026-07-08 | Adding an Initiative via "+ New" must actually appear in the left nav bar | **Done** | Root cause: NewModuleDialog's confirm() only navigated, never created anything. Now calls createInitiative with moduleTo/packageName (dedup on repeat launches); Layout links nav items to moduleTo when set. Verified live: picking DealPilot from + New adds "DealPilot" to nav immediately (40cb569). |
| R-024 | 2026-07-08 | Retain Intelligence link in primary nav, positioned directly above Settings | **Done** | Added back to Layout's bottom-chrome block, above Settings (40cb569). Verified live. |
| R-025 | 2026-07-08 | Match the font size of the top-left workspace/organization header from bridge-ai-1ay.pages.dev | **Done** | Added an Organization brand header at the top of the sidebar (prototype had none in the current build) sized like the prototype's Sidebar brand block: text-sm font-bold name + text-xs uppercase label (40cb569). |
| R-026 | 2026-07-08 | Commons Module Creation & Evolution System (Component Registry, similarity detection, lightweight evaluation, versioned Module updates + propagation governance, community signal aggregation) — user then corrected the approach mid-thread to an AI-led, confidence-tiered, non-interrogating flow (v2 supersedes v1's shape) | **Documented, Open for build** | `docs/raw/module-evolution-system-2026-07.md` + `docs/wiki/module-evolution.md`, ADR-032. v2 flow adopted (high/medium/low confidence tiers, max 3 chunked options, never a big form). v1 substance (registry/eval/versioning) retained as design, not yet built — sequenced AFTER R-028's Day-1 slice per user ruling. |
| R-027 | 2026-07-08 | Learn from ServiceNow AI Agent Studio/Build Agent, Dust Agent Builder Sidekick, and LangSmith evals (as a Commons-hosted capability) — apply relevant patterns | **Done (synthesis)** | ADR-032: ServiceNow's governance-inside-creation-flow adopted (writes to the existing Approvals ledger); Dust's introspect-and-suggest builder pattern adopted (becomes Chief of Staff's ongoing-optimization behavior); LangSmith's eval-and-compare PATTERN adopted, vendor itself rejected (Commons hosts its own harness, same reasoning as the prior OpenFGA/OPA rejection). |
| R-028 | 2026-07-08 | Day-1 capability (explicit priority over next-wave items in R-026): onboarding agent integrated with the Groq API (corrected from "Grok" — user typo), with all 5 agents inbuilt and ready, so that in real time during onboarding new Modules get built and shipped for approval / mapped to existing Modules. "If not present, start with that before executing next wave." | **Open — top priority (corrected)** | CORRECTED 2026-07-08: `GroqProvider` already exists (`platform/packages/models/src/groq-provider.ts`, wired in `wiring.ts` behind `GROQ_API_KEY`), built 2026-07-06 — before this row was first written; the earlier "not present" claim was wrong (codebase wasn't checked). Still open: no 5-agent team wired into onboarding (only single chiefOfStaff.classifyIntent/converse) — the 5 agents are Chief of Staff, Learning Agent, Communications Agent, Governance Agent, Capability Builder (docs/raw/roadmap-v2-universal-commons.md). See R-029 for the fuller, corrected spec superseding this row's Day-1 bar. |
| R-029 | 2026-07-08 | "Bridge Foundational Agents" + "Bridge Onboarding Proposal (Day 1)" — full 5-agent architecture spec (CoS non-deletable/spirit-animal identity/@-mention direct access, Learning/Communications/Governance/Capability-Builder roles, organizational evolution rules, external-agent wrapping) and full 14-step onboarding flow (account/OTP/spirit-animal/egg-animation/Gmail-or-manual/CoS-naming/Browser-Companion/LinkedIn-verify/progressive Knowledge-Intelligence-Calendar unlock). Course-correction preserved: no separate onboarding agent, CoS runs onboarding itself. Also asked: data/prompt architecture for personalization, emotional tone matched to spirit animal, sub-agents used where necessary. | **Partial — 4 agents wired** | Docs: `docs/raw/bridge-foundational-agents-onboarding-2026-07.md` + `docs/wiki/foundational-agents.md`, ADR-033. Built (2026-07-08): `packages/core/src/agents.ts` (FOUNDATIONAL_AGENTS registry, parseMention, buildAgentSystemPrompt) + `chiefOfStaff.converse` now detects a leading `@mention` and dispatches directly to Learning/Communications/Governance (direct reply, no side effects) or Capability Builder (always drafts through `pipeline.propose`, same governed contract as a routed action — never executes bare). AgentPanel shows which agent answered + a `@learning/@communications/@governance/@builder` hint. Verified live in preview: `@learning` → direct_reply, no proposal; `@builder` → route + "proposal pending in Approvals". 3 new backend tests pass (turbo build + node --test green). Tone-mapping shipped (2026-07-08 slice 2): `ANIMAL_TONE` map (6 entries, matching today's 6 real animals) + `buildAgentSystemPrompt(id, animalTone?)` additive param; client passes its `loadAvatarPrefs().animal` through `chiefOfStaff.converse`'s new optional `animal` field so @mentioned agents flavor their voice to the chosen animal, never changing WHAT they say. Confirmed genuinely NOT present (not just unwired): no Memory/Knowledge storage port exists anywhere in `@bridge/core` — the onboarding-profile schema needs a new kernel primitive (store interface + in-memory/persistent impls + wiring), which is bigger than a slice and matches CLAUDE.md's already-known "Schema v2 pass pending, EXTENDED" item, not improvised here. Progressive nav-gating shipped (2026-07-08 slice 3): `Layout.tsx` gained Knowledge (`/knowledge-base`) and Calendar (`/calendar`) as primary nav items alongside the existing Intelligence/Settings — both visible from day one, muted + lock icon until real thresholds are met (Knowledge: 2+ connected sources via `integration.list` count + `google.list` connection; Calendar: Google specifically), never a dead end (always clickable). `KnowledgeBasePage.tsx` gained the spec's verbatim "Insufficient information on your work for me to unlock magic" banner + a link to Intelligence when under 2 sources; confirmed live that its existing People/Communities/Resources/Initiatives toggle (built earlier under ADR-023, Resources already real-data-backed — 175 live entries) already satisfies "people/community/resources as toggle tabs" — no new Resources page needed, it already exists. `CalendarPage.tsx` already had honest not-connected messaging, untouched. Still open, each needing its own scoping conversation (real SMS/OTP provider choice, real browser-extension build, LinkedIn scraping approach, 10 new hand-illustrated animals + their tone entries): account/phone-OTP, Gmail-vs-manual as a distinct onboarding step, LinkedIn/Browser-Companion, Memory/Knowledge storage port + onboarding-profile schema, 14-animal reconciliation (still 6, art is hand-drawn SVG not emoji). |
| R-030 | 2026-07-08 | Continuing R-029's open tail: "phone-otp (use dummy flow for now), animal illustrations i need 3d if possible, rest go ahead" | **Partial — animals + dummy OTP + onboarding profile store shipped; Browser Companion extension deferred** | ADR-034. **14-animal reconciliation**: `avatar-store.ts`'s `SpiritAnimal` union extended from 6 to 16 (all 14 spec animals — Lion/Fox/Dog/Cat/Panda/Butterfly/Dolphin/Owl/Turtle/Peacock/Elephant/Eagle/Horse/Beaver — plus the 2 pre-existing bonus extras crane/wolf kept, not removed); `AvatarOverlay.tsx`'s `Creature` gained ear/snout silhouettes for the 10 new animals in the same hand-authored geometric-SVG style as the original 6 (honest scope note: no 3D-modeling toolchain exists in this environment — "3D" was delivered as a radial-gradient body fill + `feDropShadow` filter applied to every animal, a genuinely more dimensional/glossy look within the existing "no image assets" architecture, not fabricated 3D-rendered assets). `agents.ts`'s `ANIMAL_TONE` extended to all 16 so newly-added animals also get a Communications-Agent voice. **Dummy phone-OTP** (explicit user-authorized dummy, labeled as such in the UI — "Demo mode — no SMS was actually sent"): new `onboarding.verifyPhoneOtp` tRPC mutation (`apps/api/src/router.ts`) accepts any 6-digit code; new `PhoneOtpVerify` component in `OnboardingDialog.tsx`; new `phone_otp` + `linkedin_choice` question kinds in `questions.ts`, inserted as the FIRST onboarding step (verify-before-personalize, matching spec order). LinkedIn option opens the REAL `/integrations` page (existing social OAuth registry, `apps/api/src/social/registry.ts`) in a new tab — no fabricated scrape/connect. **Onboarding profile store** (answers R-029's open "where is this data stored" question, narrowly): new `packages/core/src/onboarding-profile.ts` (`OnboardingProfileStore` port + `InMemoryOnboardingProfileStore`), wired into `wiring.ts` (in-memory in both modes — no persistent impl yet, same honest-gap pattern as `capabilityBudgets`), exposed via `onboarding.getProfile`/`onboarding.saveProfile` router procedures; `OnboardingDialog.tsx` best-effort saves animal/answers/verification method server-side on submit (never blocks the governed workspace-activation outcome). This is explicitly the onboarding-SCOPED slice of the still-larger, still-absent general Memory/Knowledge kernel primitive (vector store, embeddings, retrieval) — not a claim that the bigger primitive now exists. Build green (`turbo run build` across core/api/web) + existing `chief-of-staff.test.ts` suite still passes. **Deferred, explicitly NOT built**: the Browser Companion browser-extension (no extension scaffold exists anywhere in the repo; a real one is a separate packaged codebase with its own manifest/permissions/store-listing surface — building a fake one would violate the no-dummy-integration posture the user granted only for phone-OTP, not for this). Live-preview click-through of the new onboarding steps not yet re-verified in browser (build-verified only this slice, given session cost). |
| R-035 | 2026-07-16 | Task Manager Module: "Similar to other modules, I want you to create a detailed plan for task manager module with necessary skills, agents, automations, etc. Also share a BRD… Draft a detailed plan and add to roadmap." Includes the game-designs use case (agent-first single tasks.md ledger — stable TASK-nnn IDs, exit tests, WIP-1, read budget, capped completed bay, CI-enforced sweep) and a skill idea: planning methodologies (OKR, Design Thinking, story mapping, backward planning, rolling wave, SMARTER, BHAG, PACT, pre-mortem, North Star, Atomic Habits, GTD) + a Goal→Outcome→Initiative→Task→Subtask node ontology with strategic/planning/execution layers. | **Done — plan + BRD delivered, TASK-021 on roadmap; revised same day by R-036** | Plan: `docs/raw/taskmanager-module-plan-2026-07.md`. BRD: `docs/raw/brd-taskmanager-2026-07.md`. Wiki: `docs/wiki/taskmanager.md`. Roadmap: TASK-021 in `docs/TASKS.md` queue (AP-033 applied, then AP-034 — renumbered during merge-integration; main used AP-030/AP-031 for parallel task-integration approvals). See R-036 for the same-day model revision. |
| R-036 | 2026-07-16 | Follow-up on R-035, four questions + a directive: (1) does Task Manager need a separate task or does it overlap existing/Taskade research; (2) on every new task, Internal Strategist should run an impact/fit/reschedule pass — agent-assigned tasks all go to Builder Agent, human-assigned reschedules need approval until the agent learns human patterns with confidence, then auto-executes minor changes and still seeks approval for significant ones; (3) pushback — why does the BRD mention Initiative if it was deprecated, can Goal proxy for Initiative since a Goal has multiple Outcomes, isn't Goal itself a Task, can the whole thing just be a Task hierarchy with dot-notation levels (e.g. `2.3.5`); (4) Internal Strategist should proactively analyze platform data and propose new value-adding tasks. Directive: place TASK-021 immediately after TASK-014; commit, push, merge once done. | **Done — model revised, placed, shipped** | (1) Confirmed no existing task overlaps (checked TASK-009 Second Brain, closest neighbor, is a different concern); the 2026-06 Taskade research was already a binding companion doc, unchanged. (2) Added: `impact-fit-analysis` Skill + `task-created-impact-analysis` Automation (Internal Strategist, runs on every Task/Goal create); `agent-task-routing` Skill + `agent-task-routing-on-assign` Automation (default Capability Builder in this Module, Skill-eligible Agent for cross-Module tasks — pushback recorded: Builder's glossary scope is capability/code changes, so it is the default/fallback, not an unconditional override); `reschedule-confidence-calibration` Skill + `reschedule-approval-gate` Automation, explicitly reusing the shipped ADR-073 `classifyApprovalBand` system-gate + VAR-1 Variance-Adjuster pattern rather than inventing a new "agent decides" mechanism — agent-floor DENY untouched. (3) Agreed and applied: `Initiative` was indeed a mistake to revive (AP-020 retired it as a kernel identifier, not a display alias); collapsed to **two Record types** — Goal (kept separate, with reasoning: reviewed/revised not "done", and one Goal can own many candidate Task-trees) and a single self-referential Task type with an auto-maintained materialized dot-path (`2.3.5`) for level tracking, carrying `outcomes[]` directly; `status: candidate` replaces the Initiative concept at any level. ADR-105 (renumbered — main used ADR-099 for a different decision) revised in place with both the original and corrected reasoning. (4) Added: `proactive-opportunity-scan` Skill + `proactive-scan-cadence` Automation (Internal Strategist, scheduled, cross-Module data → candidate Goal/Task drafts). TASK-021 moved to immediately after TASK-014 in both the execution-order list and the file's physical position. Shipped per the ship-to-prod workflow. |
| R-037 | 2026-07-17 | Second follow-up: (1) don't allocate all agent tasks to Builder — let Chief of Staff or Internal Strategist decide, and pick which one is the right owner; (2) further pushback — a Goal already has branching structure so its need is still unclear, plus a requirement for full tree flexibility: an existing branch must be able to gain a new ancestor above it (`b.c.d`→`a.b.c.d`, demoting the old root), and a branch must be able to be promoted to a new root while dropping a stale ancestor (`b.c.d`→`c.d`, keeping that branch's identity); also, like any task manager, a done goal/task must be reignitable to in-progress/pending if intent or targets change; (3) launch a localhost link to see task progress, and confirm the Status column is in the table view. | **Done — Goal collapsed further, routing reassigned, reopen supported, local preview shown** | (1) Agreed and applied: removed the "default Capability Builder" fallback entirely (ADR-107). Routing now always resolves an agent-assigned Task's executor by matching its required Skill against eligible Agents — reusing the real `SkillManifest`/eligible-Agent resolution shipped under ADR-104 (TASK-007) — and escalates to explicit Human assignment on ambiguity/no-match rather than guessing. Ownership assigned to **Chief of Staff**, not Internal Strategist: CoS's glossary definition is literally "its routing role is a product composition," and the shipped `chiefOfStaff.converse` @mention dispatch to Learning/Communications/Governance/Builder is direct precedent; Internal Strategist's mandate (analytical synthesis, comparison, scenario modeling) fits the impact-fit and tree-restructure *placement* questions it already owns, not the *dispatch* question. (2) Agreed and applied: the user's structural point was correct — a root Task already supports multiple candidate child-trees, so that no longer justified a separate Goal type. Collapsed `goals` into the `tasks` Database entirely: `is_goal: boolean` is now a plain field that travels with a row through any re-parenting, so promote/insert-ancestor-above/re-parent (all three now specified as governed proposals, new `task-tree-restructure` Skill) are pure `parent_task_id`/`path` updates, never a type migration — this is exactly what the requested flexibility needed. ADR-106 records this (supersedes ADR-105's two-Database call; ADR-105's Initiative/Outcome reasoning still stands). Reopening: added explicitly as an invariant + `target-change-reopen-prompt` Automation — done/archived/parked accept a governed reverse transition to pending/in_progress, one status graph for every Task since there's only one type. (3) Found the Task Manager UI already exists (`platform/apps/web/src/app/pages/TaskManagerPage.tsx` → `PendingWorkPage.tsx`, live at `/task-manager`, predating this Module) and confirmed the Status column is already wired (screenshot evidence). Launched it via the already-running dev server on the main checkout (`localhost:5174`) rather than disturbing that checkout's separate uncommitted work; regenerated `pending-work.generated.json` from the merged `docs/TASKS.md` (21 records) but that specific running process serves a different working directory that hasn't pulled the merge, so TASK-021 doesn't show there yet — not something to force. Attempted a second server from this worktree on port 5175 to show TASK-021 live; hit a pre-existing, unrelated `@bridge/core` build break from the just-landed TASK-007 orchestration work (`InMemoryAgentStore` no longer satisfies `AgentQuery`) that blocks any fresh checkout's `platform-web` from starting — logged to `docs/BUGS.md` (2026-07-17), out of scope to fix here. |

| R-038 | 2026-07-17 | View Grammar / DataEngine BRD request: (1) confirm Calendar is not an alias for Google Calendar but a view in the view list, and that no other Calendar reference should exist — Google Calendar is just an Integration, nothing to do with the platform beyond that; (2) confirm Calendar view and Graph view are standard views, and that the relationship graph has no separate entity — only People/Community nodes and Relation-type edges, one of the views over underlying data; (3) ensure the UI standardization task (TASK-014) captures all the views, features, and functions; (4) share the updated DataEngine views BRD with all features and how each is reflected in which UI view. Directive: commit, push, merge once done. | **Done — BRD delivered, TASK-014 scope updated, bugs logged** | (1) Confirmed a real violation by code audit: `/calendar` route currently resolves to Task Manager (routing bug), the real Google Calendar UI sits at a second route `/calendar/google`, and Calendar is coded as an installed Module/Tool in 3+ places (`routes.tsx` `InstalledModuleBoundary packageName="calendar"`, `moduleRoutes.ts`, `tools.ts`, `IntelligencePage.tsx`) — matching the docs' own "one pinnable Tool + one primary global-nav item" framing, which is exactly what's being corrected. Four independent, non-shared calendar renderers also found. Target state recorded in `docs/raw/brd-dataengine-views-2026-07.md` §1/§6 and ADR-108: Calendar becomes purely `kind: "calendar"` in the View Grammar; Google Calendar becomes a plain Integration with zero page/route/nav identity. (2) Confirmed correct already: no separate relationship-graph table exists anywhere in code or docs — Relations render as typed edges over Person/Community rows (`RELATIONSHIP_NODE_TYPES = ["edge"]`), a pure projection gated on a relation-kind column, same eligibility pattern as Calendar's date-column gate. One tracked gap, not an architecture problem: the actual `GraphView.tsx` renderer is currently a placeholder (table + banner). Second Brain (cross-Module graph) is explicitly documented as a distinct, wider surface from a Page's own Graph view — never to be merged. (3) TASK-014's Outcome/Prototype-test/Scope/Evidence rewritten in `docs/TASKS.md` to explicitly name the View Grammar consolidation, the Calendar de-modularization, the real Graph renderer, the `network`→`graph` vocabulary rename, and the new `tree` kind. (4) `docs/raw/brd-dataengine-views-2026-07.md` — 8 canonical View kinds (table/board/gallery/form/calendar/map/graph/tree) each with eligibility rule, feature list, write path, and worked "reflected in which Page" examples; a full current-state code audit; the Second-Brain-vs-Page-Graph distinction; the Integration decoupling rule. Four evidence rows logged to `docs/BUGS.md`. AP-036/ADR-108 recorded. No code changed in this pass — this is the plan TASK-014 executes, matching how the Task Manager Module's ADR-105/106 preceded its own build slices. |

---

## Resolved

*(entries move here once fully shipped and verified)*

---

*This file is maintained by the assistant. Every explicit requirement goes here before work starts. Status: Open → In Progress → Done → Resolved.*

## R-039 — Graph view cross-module scope; Second Brain collapse (2026-07-17)

> "I want even normal graphs to be allowed to be cross module scoped, so technically no difference between second brain and relationship graphs except the underlying data. Once done, commit, push and merge to main"

Verbatim. Mapped to TASK-014 (Graph renderer + scope selector) and TASK-009 (Second Brain, now converged). AP-037, ADR-110.
