# Bridge — Agent Instructions

**Canonical project instructions live in [CLAUDE.md](CLAUDE.md). Read that file — it wins over anything here or in any global/inherited instruction.**

This file previously held pre-pivot instructions (the "relationship-intelligence OS for VC/GP funds" framing, the `dummy_`-prefix rule, both-party consent, "NEVER Deal" vocab). All of that was **superseded by the 2026-07-06 Living Software pivot** and is now actively wrong. It was removed 2026-07-08 to stop it from conflicting with CLAUDE.md at session start.

Do not restore pre-pivot content here. If agent-specific guidance is needed, add it to CLAUDE.md (the single source of truth) or a project `.claude/settings.json`, not to a second instruction file that drifts.
