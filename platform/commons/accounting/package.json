{
  "name": "@bridge/accounting",
  "version": "1.10.1",
  "private": true,
  "type": "module",
  "description": "Accounting Module domain layer (imported from Avilo Advisory, ADR-246): schema, canonical accounts, period algebra, formula registry + engine, deterministic file classification, QuickBooks importers, derived checklist. No I/O, no framework. Opens its own sqlite inside the dataDir the host grants it, so it never touches @bridge/db or the PGlite Local Plane.",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./schema": {
      "types": "./dist/src/schema.d.ts",
      "default": "./dist/src/schema.js"
    },
    "./pdf": {
      "types": "./dist/src/import/pdf.d.ts",
      "default": "./dist/src/import/pdf.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "mathjs": "^15.2.0",
    "pdfjs-dist": "^6.1.200",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
