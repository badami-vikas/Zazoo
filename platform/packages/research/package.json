{
  "name": "@bridge/research",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded multi-step background Research Run engine (TASK-028). Port-driven and side-effect free: search, page reading, element location, and browser actuation are all injected, so the whole authority model (green autonomous / amber proposal-gated / red refused), the bounds, the evidence ledger, and the injection defense are unit-testable without a network or a browser.",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit false",
    "test": "node --test dist/test/*.test.js",
    "test:coverage": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@bridge/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
