{
  "name": "@bridge/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Drizzle schema mirroring docs/raw/SCHEMA.sql + Postgres/Supabase client + core port bindings.",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./schema": {
      "types": "./dist/src/schema.d.ts",
      "default": "./dist/src/schema.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "node --experimental-test-coverage --test-coverage-exclude='**/test/**' --test-coverage-lines=54 --test dist/test/*.test.js",
    "typecheck": "tsc -b --noEmit false",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@bridge/core": "workspace:*",
    "@electric-sql/pglite": "^0.2.17",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.7.3"
  }
}
