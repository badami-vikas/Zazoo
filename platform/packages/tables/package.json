{
  "name": "@bridge/tables",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Notion-parity table engine — schema-driven columns, views-as-data, and a swappable PersistencePort. Extracted from the prototype's DataEngine (docs/raw/tool-standardization-plan.md, Phase 1). The prototype itself cuts over once it joins this workspace (Phase 5); until then this package is the tested contract other consumers (and eventually DataEngine) build against.",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit false",
    "test": "node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
