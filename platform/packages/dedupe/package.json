{
  "name": "@bridge/dedupe",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Key-id exact match + blocking rules + fuzzy scoring + tiered review-queue escalation. One implementation shared by recon/people-sourcing/company-sourcing match governance and DealPilot/JobPilot dedupe (docs/raw/tool-standardization-plan.md, Phase 1).",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit false",
    "test": "node --experimental-test-coverage --test-coverage-include='dist/src/**' --test-coverage-exclude='**/test/**' --test-coverage-lines=80 --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
