{
  "name": "@bridge/sourcing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SourceConnector port + tiered cost waterfall orchestrator (free -> forms -> email -> browser-agent -> human). Shared long-tail sourcing engine behind tools/people-sourcing, tools/company-sourcing, DealPilot, and JobPilot (docs/raw/tool-standardization-plan.md, Phase 1).",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit false",
    "test": "node --experimental-test-coverage --test-coverage-exclude='**/test/**' --test-coverage-lines=70 --test dist/test/*.test.js"
  },
  "dependencies": {
    "@bridge/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
