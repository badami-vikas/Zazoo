{
  "name": "@bridge/models",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Real ModelProvider implementations (Ollama local, Anthropic cloud) + createModelRouter binding resolution for @bridge/tool-kit's modelBinding manifest slot. The @bridge/core ModelProvider port stays types-only/zero-deps; this package is where the actual fetch calls live.",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit false",
    "test": "node --experimental-test-coverage --test-coverage-exclude='**/test/**' --test-coverage-lines=40 --test dist/test/*.test.js"
  },
  "dependencies": {
    "@bridge/core": "workspace:*",
    "@bridge/tool-kit": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
