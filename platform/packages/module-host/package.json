{
  "name": "@bridge/module-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Module contract and single-process host. A module declares an id and a tRPC router; the host owns the port, the paths and the namespace, so a second module is a registry key rather than a merge. No domain knowledge lives here.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fastify/static": "^10.1.2",
    "@trpc/server": "^11.0.0",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
