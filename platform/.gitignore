node_modules/
dist/
.turbo/
*.tsbuildinfo
.env
.env.local

# LOCAL plane (pglite) — private OAuth tokens + Gmail/Calendar bodies. NEVER commit.
.bridge-local/

# Universal Commons local registry data (services/commons) — local-first store, never committed.
.commons-data/

# Stryker mutation testing (ADR-163): scratch dir + generated reports
.stryker-tmp/
**/reports/mutation/
