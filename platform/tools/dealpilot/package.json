{
  "name": "@bridge/dealpilot",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "External tool (UI surface): DealPilot. First composition proof — composes company-sourcing + dedupe + facts + recorder rather than owning any of that engine code (docs/raw/tool-standardization-plan.md, Phase 3 + section 7 standalone-build contract). This package is the manifest/registration anchor; the DealPilot feature build itself may proceed in a separate session per section 7.",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit false",
    "test": "node --experimental-test-coverage --test-coverage-exclude='**/test/**' --test-coverage-lines=70 --test dist/test/*.test.js"
  },
  "dependencies": {
    "@bridge/company-sourcing": "workspace:*",
    "@bridge/dedupe": "workspace:*",
    "@bridge/facts": "workspace:*",
    "@bridge/integrations-google": "workspace:*",
    "@bridge/people-sourcing": "workspace:*",
    "@bridge/recorder": "workspace:*",
    "@bridge/sourcing": "workspace:*",
    "@bridge/tables": "workspace:*",
    "@bridge/capability-kit": "workspace:*",
    "@napi-rs/keyring": "1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
