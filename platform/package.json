{
  "name": "bridge-platform",
  "private": true,
  "version": "0.0.0",
  "description": "Bridge AI platform monorepo — substrate, governance spine, capability runtime.",
  "packageManager": "pnpm@10.33.3",
  "engines": { "node": ">=20" },
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "lint": "eslint .",
    "check:no-dummy-runtime": "if rg -n \"dummy_\" --glob '!node_modules/**' --glob '!dist/**' --glob '!**/*.test.*' --glob '!**/*.spec.*' --glob '!**/test/**' --glob '!**/tests/**' --glob '!**/__tests__/**' --glob '!/eslint.config.js' --glob '!/package.json' --glob '!tools/eslint-rules/**' .; then exit 1; else exit 0; fi"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.3",
    "eslint": "^9.18.0",
    "typescript-eslint": "^8.62.1"
  }
}
