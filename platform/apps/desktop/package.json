{
  "name": "@bridge/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bridge desktop shell (Tauri v2) \u2014 the first-built of the three thin clients (Notion model). HOSTS apps/web (devUrl in dev, apps/web/dist in prod) and adds what only a native shell can: the Rust capture core implementing the @bridge/sensors context-provider SPI (desktop subset) + the overlay avatar. No kernel logic lives here.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "prepare:dev": "node scripts/prepare-dev.mjs",
    "prepare:bundle": "node scripts/prepare-bundle.mjs",
    "verify:bundle-inputs": "node scripts/prepare-bundle.mjs --verify-only",
    "verify:macos-bundle": "node scripts/verify-macos-bundle.mjs",
    "test:bundle": "node --test scripts/prepare-bundle.test.mjs scripts/build-tauri.test.mjs",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "build:tauri": "node scripts/build-tauri.mjs",
    "build:tauri:egg": "VITE_BRIDGE_PROFILE=egg BRIDGE_PROFILE=egg node scripts/build-tauri.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
