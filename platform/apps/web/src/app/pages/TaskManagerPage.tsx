import { PendingWorkPage } from './PendingWorkPage';

/** Task Manager is the renamed Calendar destination for planning work. */
export function TaskManagerPage() {
  return <PendingWorkPage taskView />;
}
